/* ==========================================================================
   线性代数 / 6 二次型 / 正定性判定
   —— "恒大于零"这件事的五种等价说法，以及怎么查。
      化标准形见 quadratic/standard；惯性指数见 quadratic/congruence。
   ========================================================================== */

KM.page({
  path: 'linear-algebra/quadratic/definite',
  title: '正定性判定',
  subtitle: '正定 = **除原点外恒取正值**。判据有五个，考场上只用两个：**特征值全正**与**顺序主子式全正**',
  tags: ['小题', '大题', '证明题', '高频'],
  updated: '2026-08-21',

  blocks: [

    { t: 'key', id: 'def', title: '定义与四种类型', c: String.raw`
      设 $f(x)=x\T Ax$（$A$ 实对称）。

      | 类型 | 定义 | 特征值 |
      |---|---|---|
      | **正定** | ==对一切 $x\ne0$== 有 $f(x)>0$ | 全 $>0$ |
      | **半正定** | 对一切 $x$ 有 $f(x)\ge0$ | 全 $\ge0$ |
      | **负定** | 对一切 $x\ne0$ 有 $f(x)<0$ | 全 $<0$ |
      | **不定** | $f$ ==既能取正值又能取负值== | 有正有负 |

      ==定义里"$x\ne0$"不能省==：$x=0$ 时任何二次型都取 $0$，
      把它算进去就没有正定的二次型了。

      **正定与负定的关系**：
      $$f\ \text{负定}\iff -f\ \text{正定}\iff -A\ \text{正定}.$$
      ==所以负定的一切判据都由正定的取负号得到==，不必单独记。

      **半正定与正定只差一点**：半正定允许在某些非零点取 $0$，
      对应==有零特征值==，即 $\abs A=0$。
      $$\boxed{\ \text{正定}=\text{半正定}+\abs A\ne0\ }$$
    ` },

    { t: 'key', id: 'equiv', title: '★ 正定的五个等价条件', c: String.raw`
      设 $A$ 为 $n$ 阶==实对称==矩阵，下列命题两两等价：

      1. $f(x)=x\T Ax$ ==正定==；
      2. $A$ 的特征值==全部大于零==；
      3. $A$ 的==顺序主子式全部大于零==（$n$ 个不等式）；
      4. $A$ 与 $E$ ==合同==，即存在可逆 $C$ 使 $C\T AC=E$；
      5. 存在==可逆==矩阵 $D$ 使 $A=D\T D$；
      6. $A$ 的正惯性指数 $p=n$（==规范形是 $z_1^{2}+\cdots+z_n^{2}$==）。

      **各条的用途分工**：

      | 条件 | 什么时候用 |
      |---|---|
      | ② 特征值全正 | ==抽象证明==、已知特征值时 |
      | ③ 顺序主子式全正 | ==具体矩阵、含参数的判定==（主力） |
      | ④⑤ 合同于 $E$ | 证明题里构造用 |
      | ⑥ $p=n$ | 与[惯性定理](#/linear-algebra/quadratic/congruence?at=inertia)联动 |

      =="实对称"这个前提不能丢==：非对称矩阵谈正定没有意义
      （二次型的矩阵[按约定就是对称的](#/linear-algebra/quadratic/standard?at=form-matrix)）。

      **由 ② 立刻得到的必要条件**（==用来快速排除==）：
      $$\abs A>0,\qquad a_{ii}>0\ (\forall i),\qquad \tr(A)>0.$$
      ==看到对角线上有非正元素，立刻判定不正定==，不用算了。
      **但这些只是必要条件**，全满足也未必正定。
    ` },

    { t: 'method', id: 'leading-minors', title: '顺序主子式判别法：怎么查', c: String.raw`
      **顺序主子式** $D_k$ $=$ 取 $A$ 的==前 $k$ 行前 $k$ 列==所成的行列式，$k=1,\dots,n$。

      $$\boxed{\ A\ \text{正定}\iff D_1>0,\ D_2>0,\ \dots,\ D_n>0\ }$$

      $$\boxed{\ A\ \text{负定}\iff D_1<0,\ D_2>0,\ D_3<0,\ \dots
      \ \text{即}\ (-1)^{k}D_k>0\ }$$

      ==负定是"正负交错，从负开始"==，记法：$-A$ 正定时 $D_k(-A)=(-1)^{k}D_k(A)>0$。

      **三个必须注意的点**：

      1. ==必须是"顺序"主子式==（从左上角开始的那些），
         不是任意的主子式。左上角这个顺序是固定的，==不能换行换列==；
      2. ==正定要求全部 $n$ 个都大于零==，少查一个就可能出错；
      3. ==半正定不能用顺序主子式判==！
         "顺序主子式全 $\ge0$"==推不出半正定==。
         反例：$A=\begin{pmatrix}0&0\\ 0&-1\end{pmatrix}$，
         $D_1=0\ge0$、$D_2=0\ge0$，但 $f=-x_2^{2}$ ==显然不是半正定==。
         **半正定要查全部主子式（含非顺序的）$\ge0$，或者直接看特征值。**

      **含参数的题的标准流程**：写出 $D_1,\dots,D_n$ 关于参数的表达式，
      ==解 $n$ 个不等式再取交集==。
      $D_1$ 通常最简单，==先用它把范围缩小==，能减少后面的讨论。
    ` },

    { t: 'key', id: 'derived', title: '正定矩阵的常用性质', c: String.raw`
      设 $A,B$ 正定（同阶实对称），$k>0$，则下列==均正定==：
      $$A\inv,\qquad A^{*},\qquad A^{m}\ (m\in\Z^{+}),\qquad kA,\qquad A+B$$

      **各自的一行理由**：

      - $A\inv$：特征值变成 $\frac1\lambda>0$；
      - $A^{*}=\abs A\,A\inv$：$\abs A>0$ 且 $A\inv$ 正定；
      - $A^{m}$：特征值 $\lambda^{m}>0$；
      - $A+B$：$x\T(A+B)x=x\T Ax+x\T Bx>0$，==用定义最快==。

      ==$A+B$ 那条只能用定义证==，因为"和的特征值"与"各自的特征值"没有简单关系。
      **凡是涉及两个矩阵相加的正定性问题，一律回到定义。**

      **两条常考的构造**：

      - $A$ 为 $m\times n$ 实矩阵时，==$A\T A$ 半正定==（$x\T A\T Ax=\norm{Ax}^{2}\ge0$）；
        且 ==$A\T A$ 正定 $\iff\rank(A)=n$==（列满秩，此时 $Ax=0$ 只有零解）；
      - $C$ 可逆时，$A$ 正定 $\Rightarrow C\T AC$ 正定（==合同保持正定性==）。

      ==第一条是"正定矩阵总能写成 $D\T D$"的来源==，也是等价条件 ⑤。

      **一条容易记反的**：$A,B$ 正定 ==推不出 $AB$ 正定==——
      $AB$ 甚至可能不对称。
      ==只有 $AB=BA$ 时 $AB$ 才对称，此时才谈得上正定。==
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '一、例题' },

    { t: 'example',
      id: 'ex-param',
      title: '含参数的正定判定',
      source: '标准例题（高频）',
      level: 3,
      problem: String.raw`
        问 $t$ 取何值时，二次型
        $$f(x_1,x_2,x_3)=x_1^{2}+x_2^{2}+5x_3^{2}+2tx_1x_2-2x_1x_3+4x_2x_3$$
        是正定的？
      `,
      idea: String.raw`
        **先写矩阵**（交叉项对半分）：
        $$A=\begin{pmatrix}1&t&-1\\ t&1&2\\ -1&2&5\end{pmatrix}.$$

        **用[顺序主子式](#/linear-algebra/quadratic/definite?at=leading-minors)**：
        具体矩阵含参数，这是它的主战场（用特征值要解含 $t$ 的三次方程，不现实）。

        **按 $k$ 从小到大算**：
        $D_1=1>0$ ==恒成立==，白送；
        $D_2=1-t^{2}>0$ 给出 $-1<t<1$，==范围已经很窄==；
        $D_3$ 再算，与前面取交集。

        **先缩小范围再算 $D_3$ 是有意义的**：
        $D_3$ 是三次式，若 $D_2$ 已经把 $t$ 限死在 $(-1,1)$，
        ==解 $D_3>0$ 时只需在这个区间里讨论==，可能省掉一些根。
      `,
      solution: String.raw`
        二次型的矩阵为
        $$A=\begin{pmatrix}1&t&-1\\ t&1&2\\ -1&2&5\end{pmatrix}.$$

        **$D_1$**：$D_1=1>0$，恒成立。

        **$D_2$**：
        $$D_2=\begin{vmatrix}1&t\\ t&1\end{vmatrix}=1-t^{2}>0
        \ \Longrightarrow\ -1<t<1.$$

        **$D_3$**：按第一行展开
        $$D_3=\begin{vmatrix}1&t&-1\\ t&1&2\\ -1&2&5\end{vmatrix}
        =1\cdot(5-4)-t\cdot(5t+2)+(-1)\cdot(2t+1)$$
        $$=1-5t^{2}-2t-2t-1=-5t^{2}-4t=-t(5t+4).$$

        令 $D_3>0$：$t(5t+4)<0$，即
        $$-\frac45<t<0.$$

        **取交集**：$(-1,1)\cap\left(-\frac45,0\right)=\left(-\frac45,0\right)$。

        故当 ==$-\dfrac45<t<0$== 时 $f$ 正定。

        **验算**（取 $t=-\frac12$）：
        $D_2=1-\frac14=\frac34>0\ \checkmark$；
        $D_3=-(-\frac12)(5\cdot(-\frac12)+4)=\frac12\cdot\frac32=\frac34>0\ \checkmark$
      `,
      comment: String.raw`
        **注意最终答案完全由 $D_3$ 决定**（$(-\frac45,0)\subset(-1,1)$），
        但==$D_2$ 那一步不能省==：
        判据要求==三个都大于零==，写解答时必须三个都列出来。
        ==只算 $D_3$ 就下结论，即使答案对也会扣过程分。==

        **$t=0$ 是边界**：此时 $D_3=0$，$A$ 奇异，
        $f$ ==是半正定而非正定==（有一个零特征值）。
        ==边界点要单独说明归属==，本题两个端点都取不到。

        **一个能快速排除的观察**：若题目把某个平方项系数改成负数或零
        （比如 $-x_1^{2}$），==由 $a_{ii}>0$ 这条必要条件==
        可以一秒判定不正定，连行列式都不用算。

        **同型变体：问"何时负定"。**
        判据变成 $D_1<0,D_2>0,D_3<0$。
        本题 $D_1=1>0$ 恒成立，==所以对任何 $t$ 都不可能负定==，
        一句话就答完。
        ==看到 $D_1$ 是正的常数，负定这个选项直接划掉。==

        **变体：问"何时半正定"。**
        ==不能用顺序主子式==（见[判别法的第 3 点](#/linear-algebra/quadratic/definite?at=leading-minors)），
        要么算特征值、要么查所有主子式，==难度陡增==，
        考研里这种问法通常会给更简单的矩阵。
      `,
    },

    { t: 'example',
      id: 'ex-proof',
      title: '★ 抽象证明：$A\\T A$ 与正定的构造',
      source: '经典证明题',
      level: 4,
      problem: String.raw`
        1. 设 $A$ 为 $m\times n$ 实矩阵，证明 $A\T A$ 半正定；
           并证明 $A\T A$ 正定当且仅当 $\rank(A)=n$。
        2. 设 $A$ 为 $n$ 阶正定矩阵，$B$ 为 $n$ 阶实对称矩阵，
           证明当 $t$ 充分大时 $tA+B$ 正定。
      `,
      idea: String.raw`
        **第 1 问用定义最直接**。关键的一步变形是
        $$x\T(A\T A)x=(Ax)\T(Ax)=\norm{Ax}^{2}\ge0,$$
        ==把二次型变成了一个向量的模长平方==，非负性一目了然。

        取等条件：$\norm{Ax}=0\iff Ax=0$。
        于是"正定"（$x\ne0$ 时严格大于零）等价于
        ==$Ax=0$ 只有零解==，即[列满秩](#/linear-algebra/linear-systems/solvability?at=three-cases) $\rank(A)=n$。

        **第 1 问还要先验对称性**：$(A\T A)\T=A\T(A\T)\T=A\T A$，==一行==。
        ==谈正定之前必须确认对称，这是踩分点。==

        **第 2 问的思路**：$tA+B$ 的特征值与 $A,B$ 的特征值没有简单关系，
        ==所以不能走特征值==。回到定义。

        对 $x\ne0$，$x\T(tA+B)x=t\,x\T Ax+x\T Bx$。
        $x\T Ax>0$，但它随 $x$ 变化，==要找一个统一的正下界==——
        这就要用到"单位球面上连续函数取到最小值"，
        即[瑞利商](#/threads/lines/quadratic?at=eigen-extremum)那一套：
        限制在 $\norm x=1$ 上，$x\T Ax$ 的最小值是 $\lambda_{\min}(A)>0$。

        ==把问题限制到单位球面是这道题的关键动作==，
        因为二次型是齐次的，单位球面上成立就处处成立。
      `,
      solution: String.raw`
        **(1)** 先验对称：$(A\T A)\T=A\T(A\T)\T=A\T A$，故 $A\T A$ 实对称。

        对任意 $x\in\R^{n}$，
        $$x\T(A\T A)x=(Ax)\T(Ax)=\norm{Ax}^{2}\ge0,$$
        故 $A\T A$ ==半正定==。

        **正定的充要条件**：$A\T A$ 正定
        $$\iff\ \forall x\ne0:\ \norm{Ax}^{2}>0
        \ \iff\ \forall x\ne0:\ Ax\ne0
        \ \iff\ Ax=0\ \text{只有零解}\ \iff\ \rank(A)=n.$$

        **(2)** $tA+B$ 显然实对称（两个实对称矩阵的实系数组合）。

        由 $A$ 正定，其特征值全正，记 $a=\lambda_{\min}(A)>0$；
        记 $b=\lambda_{\min}(B)$（$B$ 实对称，特征值全实，$b$ 可正可负）。

        由[单位球面上二次型的最值就是特征值](#/threads/lines/quadratic?at=eigen-extremum)，
        对一切 $\norm x=1$ 有
        $$x\T Ax\ge a>0,\qquad x\T Bx\ge b.$$
        于是当 $\norm x=1$ 时
        $$x\T(tA+B)x=t\,x\T Ax+x\T Bx\ge ta+b.$$

        取 $t>\dfrac{\abs b+1}{a}$，则 $ta+b>\abs b+1+b\ge1>0$。

        对一般的 $x\ne0$，令 $u=\dfrac{x}{\norm x}$（$\norm u=1$），由二次型的齐次性
        $$x\T(tA+B)x=\norm x^{2}\,u\T(tA+B)u>0.$$

        故当 $t>\dfrac{\abs b+1}{a}$ 时 $tA+B$ 正定。$\blacksquare$
      `,
      comment: String.raw`
        **第 1 问的结论要背下来**，它有三个直接用途：

        - ==$A\T A$ 与 $A$ 同秩==：$\rank(A\T A)=\rank(A)$（本题给了正定的情形，一般情形也成立）；
        - 它是[正定等价条件 ⑤](#/linear-algebra/quadratic/definite?at=equiv)
          "$A=D\T D$"的来源；
        - 数理统计里的最小二乘、概率论里的协方差矩阵半正定，==都是这条==。

        **第 2 问的"齐次性"那一步不能省**。
        二次型满足 $f(kx)=k^{2}f(x)$，==所以在单位球面上验证就够了==。
        很多人直接说"$x\T Ax\ge a$ 对一切 $x$ 成立"——==这是错的==，
        $x$ 很小时 $x\T Ax$ 也很小，==正确的说法必须带上 $\norm x=1$ 或写成
        $x\T Ax\ge a\norm x^{2}$==。

        **不能用特征值直接做的理由**再强调一次：
        $\lambda_{\min}(tA+B)\ne t\lambda_{\min}(A)+\lambda_{\min}(B)$，
        ==两个矩阵之和的特征值不是特征值之和==。
        本题绕开这个障碍的办法是==在二次型层面相加==（这一层是线性的），
        最后才转回特征值。

        **同型题**：证明"$A$ 正定 $\Rightarrow A+E$ 正定"——
        这个简单得多，==直接用特征值 $\lambda+1>0$ 即可==，
        因为 $A$ 与 $E$ 有公共的特征向量。
        ==只有在两个矩阵没有公共特征向量时才必须回到定义。==
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '二、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **用顺序主子式判半正定**：==不成立==，
         有[两阶的反例](#/linear-algebra/quadratic/definite?at=leading-minors)。
      2. **只查部分顺序主子式**：正定要求==全部 $n$ 个都大于零==。
      3. **查了非顺序的主子式**：必须是==从左上角开始==的那些。
      4. **负定的符号记错**：是 $(-1)^{k}D_k>0$，==从负开始交错==。
      5. **谈正定前不验对称**：$A\T A$、$tA+B$ 这类构造==要先说明对称==。
      6. **由 $a_{ii}>0$ 或 $\abs A>0$ 断言正定**：==这些只是必要条件==。
      7. **认为 $A,B$ 正定则 $AB$ 正定**：==$AB$ 未必对称==。
      8. **$x\T Ax\ge\lambda_{\min}$ 不带模长**：
         正确的是 ==$x\T Ax\ge\lambda_{\min}\norm x^{2}$==。
      9. **边界值不讨论**：使某个 $D_k=0$ 的参数值要单独说明（通常是半正定）。
      10. **把二次型的矩阵写成非对称的**：正定性只对实对称矩阵定义。
    ` },

  ],
});
