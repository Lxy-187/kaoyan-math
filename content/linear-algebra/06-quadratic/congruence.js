/* ==========================================================================
   线性代数 / 6 二次型 / 合同、惯性定理与规范形
   —— 全书收尾：把等价、相似、合同三种关系摆在一起，各自守住什么。
      化标准形见 quadratic/standard；正定见 quadratic/definite。
   ========================================================================== */

KM.page({
  path: 'linear-algebra/quadratic/congruence',
  title: '合同、惯性定理与规范形',
  subtitle: '标准形不唯一，**规范形唯一**。合同关系守住的恰好就是那对正负惯性指数——全书三种等价关系的收官',
  tags: ['小题', '概念辨析', '高频'],
  updated: '2026-08-21',

  blocks: [

    { t: 'key', id: 'def', title: '合同的定义', c: String.raw`
      称 $n$ 阶矩阵 $A$ 与 $B$ **合同**，若存在==可逆==矩阵 $C$ 使
      $$\boxed{\ B=C\T AC\ }$$

      **它从哪来**：二次型作可逆线性变换 $x=Cy$ 时
      $$x\T Ax=y\T(C\T AC)y,$$
      ==所以"同一个二次型的不同矩阵"之间的关系就是合同==，
      见[标准形那一节](#/linear-algebra/quadratic/standard?at=standard-form)。

      **合同是等价关系**：

      - 自反：$A=E\T AE$；
      - 对称：$B=C\T AC\Rightarrow A=(C\inv)\T BC\inv$；
      - 传递：两次变换复合，$C=C_1C_2$ 仍可逆。

      **合同保持的东西**：

      | 保持 | 不保持 |
      |---|---|
      | ==秩== | 特征值 |
      | ==正负惯性指数== | 迹 |
      | ==对称性==（$A$ 对称则 $B$ 对称） | 行列式（==只保号==） |

      ==行列式只保号不保值==：$\abs B=\abs C\T\abs A\abs C=\abs A\cdot\abs C^{2}$，
      而 $\abs C^{2}>0$，==所以 $\abs A$ 与 $\abs B$ 同号==。
      这条偶尔能用来快速排除选项。
    ` },

    { t: 'key', id: 'inertia', title: '★ 惯性定理：标准形不唯一，正负个数唯一', c: String.raw`
      **惯性定理**：实二次型 $f=x\T Ax$ 无论用哪个可逆线性变换化为标准形，
      ==标准形中正系数的个数 $p$ 与负系数的个数 $q$ 都是确定的==，
      与所用的变换无关。

      $$p=\text{正惯性指数},\qquad q=\text{负惯性指数},\qquad p+q=r=\rank(A)$$

      **规范形**（唯一）：
      $$f=z_1^{2}+\cdots+z_p^{2}-z_{p+1}^{2}-\cdots-z_{p+q}^{2}$$
      ==由 $(p,q)$ 完全确定==，所以规范形唯一而标准形不唯一。

      **一个具体的对照**（[配方法那道例题](#/linear-algebra/quadratic/standard?at=ex-lagrange)）：
      同一个二次型，配方法给出 $2z_1^{2}-2z_2^{2}+6z_3^{2}$，
      正交变换会给出三个特征值作系数，==数值完全不同==，
      但==都是两正一负==，$(p,q)=(2,1)$。
      **能带走的只有符号分布，数值是随变换而变的。**

      **怎么求 $p,q$**：

      | 已知 | 做法 |
      |---|---|
      | $A$ 的特征值 | ==数正数个数与负数个数==（最直接） |
      | 二次型的表达式 | 配方法化标准形，==数符号== |
      | 只需比较两个二次型 | 各求 $(p,q)$ 再比 |

      ==$A$ 实对称时特征值全实，所以"数符号"总是可行的==。
    ` },

    { t: 'key', id: 'criterion', title: '合同的判据（实对称矩阵）', c: String.raw`
      $$\boxed{\ A\ \text{与}\ B\ \text{合同}\iff \text{它们的正、负惯性指数分别相同}\ }$$
      （$A,B$ 均为 $n$ 阶==实对称==矩阵。）

      **等价说法**：$A\sim_{\text{合同}}B\iff$ 两者有==相同的规范形==。

      **实操**：算出两个矩阵的特征值，==数正负个数==，一比就知道。
      ==不必真的构造出 $C$。==

      **两条推论**：

      - $A$ 正定 $\iff$ $A$ 与 $E$ 合同（$p=n$，$q=0$）；
      - ==秩相同还不够==：$\diag(1,1)$ 与 $\diag(1,-1)$ 秩都是 $2$，
        但 $(p,q)$ 分别是 $(2,0)$ 与 $(1,1)$，==不合同==。

      **注意与"矩阵等价"的区别**：
      [等价](#/linear-algebra/matrix/rank?at=rank-invariance)只要求秩相同，
      ==合同还要求符号分布相同==，是更强的关系。
    ` },

    { t: 'compare',
      id: 'three-relations',
      title: '★ 全书三种关系的总对照（考前必看）',
      cols: ['', '等价', '相似', '合同'],
      rows: [
        ['定义', '$B=PAQ$，$P,Q$ 可逆', '$B=P\\inv AP$', '$B=C\\T AC$，$C$ 可逆'],
        ['来自', '初等变换', '换基看同一变换', '二次型换元'],
        ['保持', '==秩==', '==特征值、迹、行列式、秩==', '==秩、正负惯性指数=='],
        ['判据（同型）', '秩相等 $\\iff$ 等价', '秩、迹相等==只是必要==', '实对称时 $(p,q)$ 相同 $\\iff$ 合同'],
        ['标准形', '$\\begin{pmatrix}E_r&O\\\\O&O\\end{pmatrix}$', '若可对角化则 $\\Lambda$', '$\\diag(1,\\dots,-1,\\dots,0)$'],
        ['三者关系', '最弱', '与合同==互不包含==', '比等价强'],
      ] },

    { t: 'key', id: 'overlap', title: '相似与合同什么时候重合', c: String.raw`
      **一般来说这是两回事**：

      - $A=\begin{pmatrix}1&0\\0&2\end{pmatrix}$ 与
        $B=\begin{pmatrix}1&0\\0&1\end{pmatrix}$：
        ==合同==（都是 $p=2,q=0$），但特征值不同，==不相似==；
      - $A=\begin{pmatrix}1&1\\0&1\end{pmatrix}$ 与 $E$：
        既不相似也不合同（$A$ 不对称，谈合同要小心）。

      **唯一的重合点是正交变换**：$Q$ 正交时 $Q\T=Q\inv$，于是
      $$Q\T AQ=Q\inv AQ,$$
      ==同一个式子既是合同又是相似==，
      这正是[实对称那一页](#/linear-algebra/eigen/symmetric?at=similar-and-congruent)讲的事，
      也是==二次型偏爱正交变换==的原因：
      标准形的系数既是特征值（相似的信息），又能读出惯性指数（合同的信息）。

      **一条常考的单向结论**：
      $$A,B\ \text{实对称且相似}\ \Longrightarrow\ A,B\ \text{合同}$$
      因为相似 $\Rightarrow$ 特征值相同 $\Rightarrow$ 正负个数相同 $\Rightarrow$ 合同。
      ==反过来不成立==（上面第一个例子就是反例）。

      **口诀**：==实对称时，相似比合同强==；一般情形两者没有包含关系。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '一、例题' },

    { t: 'example',
      id: 'ex-judge',
      title: '判断合同与相似',
      source: '标准例题（概念型高频）',
      level: 3,
      problem: String.raw`
        设
        $$A=\begin{pmatrix}1&0&0\\ 0&2&0\\ 0&0&-3\end{pmatrix},\qquad
        B=\begin{pmatrix}2&0&0\\ 0&5&0\\ 0&0&-1\end{pmatrix},\qquad
        C=\begin{pmatrix}1&0&0\\ 0&2&0\\ 0&0&3\end{pmatrix}.$$
        判断 $A$ 与 $B$、$A$ 与 $C$ 是否相似？是否合同？
      `,
      idea: String.raw`
        **三个都是对角阵，特征值直接读对角线**，
        ==所以相似性一眼可判==（[相似要求特征值相同](#/linear-algebra/eigen/similarity?at=invariants)）。

        **合同性看 $(p,q)$**：数对角线上正数、负数的个数。

        - $A$：$1,2,-3$ $\Rightarrow$ ==两正一负==，$(p,q)=(2,1)$；
        - $B$：$2,5,-1$ $\Rightarrow$ ==两正一负==，$(2,1)$；
        - $C$：$1,2,3$ $\Rightarrow$ ==三正==，$(3,0)$。

        **预判**：$A$ 与 $B$ 特征值不同（$\set{1,2,-3}$ vs $\set{2,5,-1}$）$\Rightarrow$ 不相似；
        但 $(p,q)$ 都是 $(2,1)$ $\Rightarrow$ ==合同==。
        $A$ 与 $C$ 特征值不同 $\Rightarrow$ 不相似；$(p,q)$ 也不同 $\Rightarrow$ ==不合同==。

        ==这道题是"合同与相似互不蕴含"的最干净的演示==。
      `,
      solution: String.raw`
        三个矩阵均为实对称（对角阵），特征值即对角元。

        **$A$ 与 $B$**：

        - **相似**：$A$ 的特征值为 $1,2,-3$，$B$ 的为 $2,5,-1$，
          ==特征值不同，故不相似==。
        - **合同**：$A$ 的正负惯性指数 $(p,q)=(2,1)$，
          $B$ 的 $(p,q)=(2,1)$，==相同，故合同==。

          （具体的 $C$ 也容易给出：取
          $C_0=\diag\left(\sqrt2,\sqrt{\tfrac52},\tfrac{1}{\sqrt3}\right)$，
          则 $C_0\T AC_0=B$。）

        **$A$ 与 $C$**：

        - **相似**：特征值 $\set{1,2,-3}\ne\set{1,2,3}$，==不相似==。
        - **合同**：$A$ 的 $(p,q)=(2,1)$，$C$ 的 $(p,q)=(3,0)$，
          ==不同，故不合同==。

        **结论**：$A$ 与 $B$ ==合同但不相似==；$A$ 与 $C$ ==既不相似也不合同==。
      `,
      comment: String.raw`
        **这道题给出了"合同但不相似"的最短例子**。
        反方向的例子也要会举：
        ==两个相似的实对称矩阵一定合同==（[单向结论](#/linear-algebra/quadratic/congruence?at=overlap)），
        所以"相似但不合同"的实对称例子==不存在==；
        要造这种例子必须跳出实对称的范围（那时合同本身就不好谈）。

        **注意 $A$ 与 $C$ 秩相同（都是 $3$）却不合同**——
        ==秩相同只保证"等价"，不保证合同==。
        这三个矩阵两两都等价（秩都是 $3$，都可逆），
        ==但合同关系把它们分成了两类==：$\set{A,B}$ 与 $\set{C}$。
        **等价 $\subsetneq$ 合同这个包含关系，本题演示得很清楚。**

        **考试中的常见问法变体**：

        | 问 | 看什么 |
        |---|---|
        | 是否相似 | ==特征值（含重数）== |
        | 是否合同 | ==$(p,q)$== |
        | 是否等价 | ==秩== |
        | 是否都正定 | $p=n$ |

        ==拿到题先把三个矩阵的"特征值 / $(p,q)$ / 秩"列成一张小表，
        三个问题一次答完。==

        **陷阱提醒**：若矩阵不是对角阵，==必须先求特征值==，
        不能直接看对角元。
        比如 $\begin{pmatrix}0&1\\1&0\end{pmatrix}$ 对角元全是 $0$，
        但特征值是 $\pm1$，$(p,q)=(1,1)$。
      `,
    },

    { t: 'example',
      id: 'ex-inertia',
      title: '由二次型求惯性指数与规范形',
      source: '标准例题',
      level: 3,
      problem: String.raw`
        求二次型
        $$f(x_1,x_2,x_3)=x_1^{2}+4x_2^{2}+4x_3^{2}-4x_1x_2+4x_1x_3-8x_2x_3$$
        的秩、正负惯性指数与规范形。
      `,
      idea: String.raw`
        **两条路都能走**：

        - 求特征值数符号；
        - 配方法化标准形数符号。

        **先写矩阵看看**：
        $$A=\begin{pmatrix}1&-2&2\\ -2&4&-4\\ 2&-4&4\end{pmatrix}.$$
        ==第 2 行是第 1 行的 $-2$ 倍，第 3 行是第 1 行的 $2$ 倍==，
        所以 $\rank(A)=1$！

        ==秩为 $1$ 立刻决定了一切==：$r=1$，
        所以 $p+q=1$，只能是 $(1,0)$ 或 $(0,1)$。
        再由[秩 $1$ 矩阵的特征值](#/linear-algebra/matrix/rank?at=rank1)
        是 $\tr(A)=1+4+4=9>0$ 与两个 $0$，==得 $(p,q)=(1,0)$==。

        **更直接的看法**：秩为 $1$ 的对称矩阵形如 $\pm\alpha\alpha\T$，
        对应的二次型就是==一个完全平方==。
        试着凑：$x_1-2x_2+2x_3$ 的平方展开正好是原式，
        ==一眼就得到标准形==。

        ==这道题的题眼是"看出秩为 1"，看出来就三十秒，看不出来要配方或求特征值。==
      `,
      solution: String.raw`
        **写出矩阵**：
        $$A=\begin{pmatrix}1&-2&2\\ -2&4&-4\\ 2&-4&4\end{pmatrix}.$$

        观察到第 $2,3$ 行分别是第 $1$ 行的 $-2$ 倍与 $2$ 倍，故 $\rank(A)=1$，即
        $$r=\rank(f)=1.$$

        **直接配方**（秩 $1$ 意味着完全平方）：
        $$(x_1-2x_2+2x_3)^{2}
        =x_1^{2}+4x_2^{2}+4x_3^{2}-4x_1x_2+4x_1x_3-8x_2x_3=f.$$

        故取可逆线性变换
        $$\begin{cases}y_1=x_1-2x_2+2x_3\\ y_2=x_2\\ y_3=x_3\end{cases}
        \qquad\text{（系数矩阵行列式为 }1\ne0\text{，可逆）}$$
        得标准形
        $$f=y_1^{2}.$$

        **结论**：
        $$r=1,\qquad p=1,\qquad q=0,$$
        规范形为
        $$f=z_1^{2}.$$

        **验算（用特征值）**：$A$ 秩为 $1$，特征值为 $\tr(A)=9$ 与 $0,0$，
        ==一个正、两个零==，故 $r=1$、$p=1$、$q=0$。$\checkmark$
      `,
      comment: String.raw`
        **两条路线给出的标准形不同，规范形相同**：

        - 配方法：$f=y_1^{2}$；
        - 正交变换：$f=9y_1^{2}$（系数是特征值 $9$）。

        ==系数 $1$ 与 $9$ 不同，但都是"一正两零"==，
        规范形都是 $z_1^{2}$——[惯性定理](#/linear-algebra/quadratic/congruence?at=inertia)在此得到最直观的印证。

        **这个二次型是半正定的**：$f=(x_1-2x_2+2x_3)^{2}\ge0$，
        但在 $x_1-2x_2+2x_3=0$ 这个平面上取 $0$，
        ==所以不是正定==（[正定要求 $p=n=3$](#/linear-algebra/quadratic/definite?at=equiv)，这里 $p=1$）。

        **"看出秩为 $1$"的两个信号**（值得训练）：

        1. 矩阵各行成比例；
        2. 二次型能整体写成一个一次式的平方（或它的负倍数）。

        ==只要一个二次型的秩是 $1$，它必然是 $\pm(\text{一次式})^{2}$==，
        反之亦然。这条对应关系在选择题里很好用。

        **变体**：若把 $f$ 改成 $-(x_1-2x_2+2x_3)^{2}$，
        则 $(p,q)=(0,1)$，规范形是 $-z_1^{2}$，==半负定==。
        ==秩仍是 $1$，但合同类变了==，与原来的 $f$ ==不合同==。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '二、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **合同写成 $C\inv AC$**：合同是 ==$C\T AC$==，相似才是 $C\inv AC$。
      2. **由秩相同断言合同**：==还要 $(p,q)$ 相同==，
         $\diag(1,1)$ 与 $\diag(1,-1)$ 是反例。
      3. **由合同断言相似**：==合同不保特征值==。
      4. **由相似断言合同**：==实对称时才成立==，一般矩阵不行。
      5. **认为标准形唯一**：==只有规范形唯一==。
      6. **把配方法的系数当特征值**：数值随变换而变，==只有符号分布不变==。
      7. **非对角阵直接看对角元数符号**：==必须先求特征值==。
      8. **忘了 $p+q=r$**：秩等于非零系数个数，==零特征值不计入 $p$ 或 $q$==。
      9. **合同判据用在非实对称矩阵上**：判据的前提是==两者都实对称==。
    ` },

    { t: 'md', c: String.raw`
      ---

      **线性代数六章到此结束。**
      回头看，全书其实在反复回答同一个问题：==这个矩阵"有多少信息"、"能化到多简"==。

      $$\underbrace{\text{行列式}}_{\text{退化与否}}
      \ \to\ \underbrace{\text{秩}}_{\text{信息量}}
      \ \to\ \underbrace{\text{向量组 / 方程组}}_{\text{谁多余、解有几维}}
      \ \to\ \underbrace{\text{特征值}}_{\text{不变的方向}}
      \ \to\ \underbrace{\text{二次型}}_{\text{符号分布}}$$

      而三种等价关系正好对应三种"化简的自由度"：
      [等价](#/linear-algebra/matrix/rank?at=rank-invariance)最松（只剩秩），
      [相似](#/linear-algebra/eigen/similarity?at=invariants)最紧（保住全部特征值），
      合同居中（保住秩与符号）。
      ==选哪种变换，取决于你想保住什么。==
    ` },

  ],
});
