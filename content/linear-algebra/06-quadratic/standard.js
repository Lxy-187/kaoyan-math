/* ==========================================================================
   线性代数 / 6 二次型 / 化标准形（配方法 / 正交变换）
   —— 全书最后一章的入口：二次型 ⟺ 实对称矩阵。
      正定性见 quadratic/definite；合同与惯性定理见 quadratic/congruence。
   ========================================================================== */

KM.page({
  path: 'linear-algebra/quadratic/standard',
  title: '化标准形（配方法 / 正交变换）',
  subtitle: '二次型就是**实对称矩阵换了个写法**。化标准形只做一件事：**消掉交叉项**——两条路，各有各的代价',
  tags: ['大题', '计算题', '高频'],
  updated: '2026-08-21',

  blocks: [

    { t: 'key', id: 'form-matrix', title: '二次型与实对称矩阵一一对应', c: String.raw`
      $n$ 元**二次型**是一个二次齐次多项式，可以唯一地写成
      $$f(x_1,\dots,x_n)=x\T A x,\qquad A\T=A.$$

      **写 $A$ 的规则只有两句**：
      $$\boxed{\ \text{平方项系数放对角线};\qquad \text{交叉项系数**对半分**到两个对称位置}\ }$$

      **例**：$f=x_1^{2}+3x_2^{2}-2x_1x_2+4x_2x_3$ 对应
      $$A=\begin{pmatrix}
      1&-1&0\\ -1&3&2\\ 0&2&0
      \end{pmatrix}.$$
      ==$-2x_1x_2$ 的系数 $-2$ 拆成两个 $-1$，$4x_2x_3$ 拆成两个 $2$==。

      **为什么一定要取对称的 $A$**：不对称的矩阵也能表示同一个 $f$
      （比如把 $-2$ 全塞进 $a_{12}$、$a_{21}$ 取 $0$），
      ==但那样对应就不唯一了==，而且[实对称矩阵的一切好性质](#/linear-algebra/eigen/symmetric?at=four-properties)
      （特征值全实、必可正交对角化）都用不上。
      =="二次型的矩阵"默认且只能指对称的那一个。==

      **反向读也要熟**：给一个对称矩阵写出二次型时，
      ==非对角元要乘 $2$==：$a_{ij}$ 与 $a_{ji}$ 合起来贡献 $2a_{ij}x_ix_j$。

      **秩**：$\rank(f):=\rank(A)$，==它在可逆线性变换下不变==。
    ` },

    { t: 'key', id: 'standard-form', title: '标准形与规范形', c: String.raw`
      作**可逆线性变换** $x=Cy$（$C$ 可逆），二次型变为
      $$f=x\T Ax=(Cy)\T A(Cy)=y\T(C\T AC)y,$$
      ==所以新矩阵是 $C\T AC$，不是 $C\inv AC$==。
      这个关系叫 $A$ 与 $C\T AC$ **[合同](#/linear-algebra/quadratic/congruence?at=def)**。

      | 名称 | 形状 | 系数 |
      |---|---|---|
      | **标准形** | $d_1y_1^{2}+\cdots+d_ny_n^{2}$ | ==只要求无交叉项==，$d_i$ 任意实数 |
      | **规范形** | $z_1^{2}+\cdots+z_p^{2}-z_{p+1}^{2}-\cdots-z_r^{2}$ | ==系数只能是 $1,-1,0$== |

      **标准形不唯一**（不同方法、不同变换给出不同的 $d_i$），
      ==但规范形唯一==——这是[惯性定理](#/linear-algebra/quadratic/congruence?at=inertia)的内容。

      **两个不变量**（无论怎么变换都不变）：

      - **秩** $r$：非零系数的个数；
      - **正惯性指数** $p$：正系数的个数（负惯性指数 $q=r-p$）。

      ==化标准形的一切方法，本质都是在寻找那个 $C$。==
    ` },

    /* ================================================================== */
    { t: 'h', id: 'methods', c: '一、两条路线' },

    { t: 'compare',
      id: 'compare-methods',
      title: '正交变换 vs 配方法：先看题目要什么',
      cols: ['', '正交变换法', '配方法（拉格朗日）'],
      rows: [
        ['变换矩阵', '$Q$ ==正交==（$Q\\T=Q\\inv$）', '$C$ 只要求==可逆=='],
        ['标准形系数', '==就是特征值==', '任意实数，==不是特征值=='],
        ['保住什么', '既[合同又相似](#/linear-algebra/eigen/symmetric?at=similar-and-congruent)，==保长度和角度==', '只合同，==只保正负惯性指数=='],
        ['计算量', '大（要求特征值、特征向量、正交化）', '小（纯代数配方）'],
        ['题目关键词', '"用**正交变换**"、"求正交矩阵 $Q$"、几何定型', '"用**可逆线性变换**"、只问标准形'],
      ] },

    { t: 'md', c: String.raw`
      ==读题时抓"正交"两个字==：出现了就必须走正交变换法（配方法拿不到正交矩阵）；
      没出现就用配方法，==能省一大半时间==。

      若题目问的是"曲面是什么类型""求 $x\T Ax$ 在单位球面上的最值"，
      ==也必须用正交变换==——因为只有它不改变几何形状，
      见[二次型主线](#/threads/lines/quadratic?at=surface-type)。
    ` },

    { t: 'method', id: 'orthogonal-method', title: '正交变换法：五步', c: String.raw`
      1. **写出对称矩阵 $A$**（交叉项对半分）；
      2. **求特征值** $\lambda_1,\dots,\lambda_n$（解 $\abs{\lambda E-A}=0$）；
      3. **求特征向量**，==不同特征值的自动正交，同一重根内部要[施密特正交化](#/linear-algebra/eigen/symmetric?at=schmidt)==；
      4. **单位化**，拼成正交矩阵 $Q$；
      5. **写结论**：令 $x=Qy$，则
         $$f=\lambda_1y_1^{2}+\lambda_2y_2^{2}+\cdots+\lambda_ny_n^{2}.$$

      ==这就是[实对称的正交对角化](#/linear-algebra/eigen/symmetric?at=procedure)==，
      只是把 $Q\T AQ=\Lambda$ 换了个说法。

      **三个必查的点**：

      - ==$Q$ 的每一列模长是否为 $1$==（漏单位化是头号错误）；
      - ==特征值与列的顺序是否对应==（第 $j$ 列配 $\lambda_j$）；
      - ==$\sum\lambda_i=\tr(A)$== 验算一次。

      **写答案时 $Q$ 和标准形要同时给出**，
      而且要写明"作正交变换 $x=Qy$"这句话，==它是踩分点==。
    ` },

    { t: 'method', id: 'lagrange', title: '★ 配方法：两种情形', c: String.raw`
      **情形一：含平方项**（比如 $x_1^{2}$ 的系数非零）。

      1. 把==所有含 $x_1$ 的项==（$x_1^{2}$ 和一切 $x_1x_j$）集中起来，配成完全平方；
      2. 剩下的部分==不再含 $x_1$==，对 $x_2$ 重复；
      3. 直到没有交叉项。

      $$f=a_{11}\left(x_1+\frac{a_{12}}{a_{11}}x_2+\cdots\right)^{2}+(\text{不含 }x_1\text{ 的二次型})$$

      ==每一步"提出一个平方"之后，剩下的变量数就少一个==，
      所以最多做 $n-1$ 步。

      **情形二：没有任何平方项**（全是交叉项，如 $f=2x_1x_2+2x_1x_3-2x_2x_3$）。

      ==先做一次"造平方"的换元==：
      $$\begin{cases}x_1=y_1+y_2\\ x_2=y_1-y_2\\ x_3=y_3\end{cases}
      \ \Longrightarrow\ 2x_1x_2=2(y_1^{2}-y_2^{2}),$$
      ==平方项就出来了==，再转回情形一。

      **这一步的变换必须可逆**：上面那个换元的矩阵
      $\begin{pmatrix}1&1&0\\ 1&-1&0\\ 0&0&1\end{pmatrix}$
      行列式为 $-2\ne0$，==可逆，合法==。
      ==这是配方法唯一需要小心的地方，很多人写成 $x_1=y_1,x_2=y_1-y_2$ 之类，
      看着像但要验一下可逆性。==

      **最后要写出变换矩阵 $C$**：把配方过程中的换元==反解==成 $x=Cy$。
      ==注意方向：题目要的是 $x$ 用 $y$ 表示==，而配方时自然得到的往往是 $y$ 用 $x$ 表示，
      要==求一次逆==（或者从一开始就顺着写）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '二、例题' },

    { t: 'example',
      id: 'ex-orthogonal',
      title: '正交变换化标准形',
      source: '标准例题（高频大题）',
      level: 3,
      problem: String.raw`
        用正交变换把二次型
        $$f(x_1,x_2,x_3)=x_1^{2}+x_2^{2}+x_3^{2}+4x_1x_2+4x_1x_3+4x_2x_3$$
        化为标准形，并写出所用的正交变换。
      `,
      idea: String.raw`
        **写矩阵**：平方项系数都是 $1$ 放对角线，
        每个交叉项系数 $4$ ==对半分成两个 $2$==，得
        $$A=\begin{pmatrix}1&2&2\\ 2&1&2\\ 2&2&1\end{pmatrix}.$$

        ==这个矩阵在[特征值那一页](#/linear-algebra/eigen/basics?at=ex-compute)算过==：
        特征值 $5,-1,-1$，特征向量 $(1,1,1)\T$ 与平面 $x_1+x_2+x_3=0$。
        所以标准形必然是 $5y_1^{2}-y_2^{2}-y_3^{2}$。

        **本题真正的工作在第 3、4 步**：
        二重根 $-1$ 的两个基础解系向量 $(-1,1,0)\T$、$(-1,0,1)\T$
        ==内积为 $1$，不正交==，必须先施密特正交化再单位化。

        ==这是正交变换法比配方法费事的全部原因==，
        也是最容易在计算上出错的地方。
      `,
      solution: String.raw`
        **① 写矩阵**：
        $$A=\begin{pmatrix}1&2&2\\ 2&1&2\\ 2&2&1\end{pmatrix}.$$

        **② 求特征值**：各行之和为 $5$，
        $$\abs{\lambda E-A}=(\lambda-5)(\lambda+1)^{2},$$
        故 $\lambda_1=5$，$\lambda_2=\lambda_3=-1$。

        **③ 求特征向量**：

        $\lambda_1=5$：解 $(5E-A)x=0$ 得 $\xi_1=(1,1,1)\T$。

        $\lambda_2=-1$：解 $(-E-A)x=0$，即 $x_1+x_2+x_3=0$，取
        $$\alpha_1=(-1,1,0)\T,\qquad \alpha_2=(-1,0,1)\T.$$

        **施密特正交化**（$\alpha_1,\alpha_2$ 不正交）：
        $$\beta_1=\alpha_1=(-1,1,0)\T,$$
        $$\beta_2=\alpha_2-\frac{(\alpha_2,\beta_1)}{(\beta_1,\beta_1)}\beta_1
        =(-1,0,1)\T-\frac{1}{2}(-1,1,0)\T
        =\left(-\tfrac12,-\tfrac12,1\right)\T.$$
        取 $\beta_2$ 的 $2$ 倍以去掉分数：$\beta_2'=(-1,-1,2)\T$。

        **④ 单位化**：
        $$\eta_1=\tfrac{1}{\sqrt3}(1,1,1)\T,\quad
        \eta_2=\tfrac{1}{\sqrt2}(-1,1,0)\T,\quad
        \eta_3=\tfrac{1}{\sqrt6}(-1,-1,2)\T,$$
        $$Q=(\eta_1,\eta_2,\eta_3)=\begin{pmatrix}
        \frac{1}{\sqrt3}&-\frac{1}{\sqrt2}&-\frac{1}{\sqrt6}\\[3pt]
        \frac{1}{\sqrt3}&\frac{1}{\sqrt2}&-\frac{1}{\sqrt6}\\[3pt]
        \frac{1}{\sqrt3}&0&\frac{2}{\sqrt6}
        \end{pmatrix}.$$

        **⑤ 结论**：作正交变换 $x=Qy$，则
        $$f=5y_1^{2}-y_2^{2}-y_3^{2}.$$

        **验算**：$\tr(A)=3=5-1-1\ \checkmark$；
        $\eta_2\cdot\eta_3=\frac{1}{\sqrt{12}}(1-1+0)=0\ \checkmark$；
        三列模长均为 $1\ \checkmark$
      `,
      comment: String.raw`
        **施密特正交化只在重根内部做**。
        $\eta_1$ 与 $\eta_2,\eta_3$ 的正交性是[实对称矩阵自动保证](#/linear-algebra/eigen/symmetric?at=four-properties)的，
        ==不需要也不应该把 $\xi_1$ 也拉进正交化过程==。

        **本题可以取巧**：$\lambda=-1$ 的特征子空间是平面 $x_1+x_2+x_3=0$，
        与其解出基础解系再正交化，==不如直接在这个平面里找两个正交向量==——
        比如 $(-1,1,0)\T$ 和 $(1,1,-2)\T$（内积 $-1+1+0=0$，且都满足方程）。
        ==这样省掉整个施密特过程==，考场上很实用。
        **技巧：先随便取一个 $\alpha_1$，第二个取"与 $\alpha_1$ 和法向量都正交"的方向，
        即 $\alpha_1\times(1,1,1)$（叉积）。**

        **标准形的系数就是特征值**，这是正交变换法的招牌。
        若用[配方法](#/linear-algebra/quadratic/standard?at=lagrange)做同一道题，
        会得到形如 $y_1^{2}-3y_2^{2}+\cdots$ 的另一组系数——
        ==正负号个数（$1$ 正 $2$ 负）必然一致，但具体数值不同==，
        这正是[惯性定理](#/linear-algebra/quadratic/congruence?at=inertia)在说的事。

        **顺带得到的几何信息**：$f=1$ 表示的曲面，
        特征值符号是 $(+,-,-)$，==故为双叶双曲面==，
        见[曲面定型](#/threads/lines/quadratic?at=surface-type)。
      `,
    },

    { t: 'example',
      id: 'ex-lagrange',
      title: '★ 配方法：没有平方项的情形',
      source: '经典例题',
      level: 3,
      problem: String.raw`
        用配方法把二次型
        $$f(x_1,x_2,x_3)=2x_1x_2+2x_1x_3-6x_2x_3$$
        化为标准形，并写出所用的可逆线性变换。
      `,
      idea: String.raw`
        **一眼看出没有平方项**——三项全是交叉项，
        ==配方法的常规套路无从下手==，必须先[造出一个平方项](#/linear-algebra/quadratic/standard?at=lagrange)。

        **造法**：对含交叉项的那一对变量（这里选 $x_1,x_2$）作
        $$x_1=y_1+y_2,\qquad x_2=y_1-y_2,\qquad x_3=y_3,$$
        则 $2x_1x_2=2(y_1^{2}-y_2^{2})$，==平方项就有了==。

        **务必验可逆**：这个变换的矩阵
        $\begin{pmatrix}1&1&0\\ 1&-1&0\\ 0&0&1\end{pmatrix}$，
        行列式 $=-2\ne0$，==合法==。

        **换元之后回到常规情形**：先把所有含 $y_1$ 的项配成完全平方，
        剩下的不含 $y_1$，再看还有没有交叉项。

        **最后要反解出 $x=Cy$**。这里有个陷阱：
        配方过程中得到的是"新变量用旧变量表示"，
        ==而题目要的是 $x$ 用最终变量表示，方向相反==，
        需要把几步换元==复合起来再求逆==，或者从一开始就顺着代。
      `,
      solution: String.raw`
        **第一步（造平方项）**：令
        $$\begin{cases}x_1=y_1+y_2\\ x_2=y_1-y_2\\ x_3=y_3\end{cases}
        \qquad C_1=\begin{pmatrix}1&1&0\\ 1&-1&0\\ 0&0&1\end{pmatrix},\ \abs{C_1}=-2\ne0.$$

        代入：
        $$f=2(y_1+y_2)(y_1-y_2)+2(y_1+y_2)y_3-6(y_1-y_2)y_3$$
        $$=2y_1^{2}-2y_2^{2}+2y_1y_3+2y_2y_3-6y_1y_3+6y_2y_3$$
        $$=2y_1^{2}-2y_2^{2}-4y_1y_3+8y_2y_3.$$

        **第二步（配 $y_1$）**：
        $$2y_1^{2}-4y_1y_3=2\bigl(y_1^{2}-2y_1y_3\bigr)=2\bigl(y_1-y_3\bigr)^{2}-2y_3^{2},$$
        故
        $$f=2(y_1-y_3)^{2}-2y_3^{2}-2y_2^{2}+8y_2y_3.$$

        **第三步（配 $y_2$）**：
        $$-2y_2^{2}+8y_2y_3=-2\bigl(y_2^{2}-4y_2y_3\bigr)=-2\bigl(y_2-2y_3\bigr)^{2}+8y_3^{2},$$
        故
        $$f=2(y_1-y_3)^{2}-2(y_2-2y_3)^{2}+\bigl(-2+8\bigr)y_3^{2}
        =2(y_1-y_3)^{2}-2(y_2-2y_3)^{2}+6y_3^{2}.$$

        **第四步（令新变量）**：
        $$\begin{cases}z_1=y_1-y_3\\ z_2=y_2-2y_3\\ z_3=y_3\end{cases}
        \ \Longrightarrow\
        \begin{cases}y_1=z_1+z_3\\ y_2=z_2+2z_3\\ y_3=z_3\end{cases}
        \qquad C_2=\begin{pmatrix}1&0&1\\ 0&1&2\\ 0&0&1\end{pmatrix}.$$

        标准形为
        $$f=2z_1^{2}-2z_2^{2}+6z_3^{2}.$$

        **复合变换**：$x=C_1y=C_1C_2z$，
        $$C=C_1C_2=\begin{pmatrix}1&1&0\\ 1&-1&0\\ 0&0&1\end{pmatrix}
        \begin{pmatrix}1&0&1\\ 0&1&2\\ 0&0&1\end{pmatrix}
        =\begin{pmatrix}1&1&3\\ 1&-1&-1\\ 0&0&1\end{pmatrix},$$
        $\abs C=\abs{C_1}\abs{C_2}=-2\ne0$，可逆。

        故作可逆线性变换 $x=Cz$ 时 $f=2z_1^{2}-2z_2^{2}+6z_3^{2}$。
      `,
      comment: String.raw`
        **系数 $2,-2,6$ 不是特征值**——这是配方法与正交变换法最大的区别。
        ==可信的只有"两正一负"这个符号分布==：
        正惯性指数 $p=2$，负惯性指数 $q=1$，秩 $r=3$。
        用正交变换做同一题会得到三个特征值，==符号分布必然还是两正一负==。

        **复合变换的方向是这道题最容易错的地方**。
        两步换元都是"旧 $=$ 新"的形式（$x=C_1y$、$y=C_2z$），
        ==所以直接相乘 $x=C_1C_2z$ 即可，不用求逆==。
        若某一步写成了"新 $=$ 旧"（如第四步先写的 $z=\cdots$ 形式），
        ==必须先反解==再参与复合。
        **建议全程坚持"旧用新表示"的写法，就不会乱。**

        **验算办法**（强烈建议做）：取一组具体的 $z$，
        比如 $z=(1,0,0)\T$，则 $x=C z=(1,1,0)\T$，
        代入原式 $f=2\cdot1\cdot1+0-0=2$，
        而标准形给 $2\cdot1^{2}=2\ \checkmark$。
        再取 $z=(0,0,1)\T$：$x=(3,-1,1)\T$，
        $f=2(3)(-1)+2(3)(1)-6(-1)(1)=-6+6+6=6$，
        标准形给 $6\cdot1^{2}=6\ \checkmark$

        **什么时候必须用配方法**：题目说"用可逆线性变换"或只问标准形/规范形时。
        ==它比正交变换法快得多==，因为完全不碰特征值。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '三、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **交叉项系数不对半分**：$4x_1x_2$ 对应 $a_{12}=a_{21}=2$，==不是 $4$==。
      2. **由对称矩阵写二次型时忘了乘 $2$**：非对角元贡献 $2a_{ij}x_ix_j$。
      3. **用 $C\inv AC$ 代替 $C\T AC$**：二次型的变换是==合同==不是相似，
         见[标准形那一节](#/linear-algebra/quadratic/standard?at=standard-form)。
      4. **题目要正交变换却用了配方法**：==配方法给不出正交矩阵==。
      5. **$Q$ 忘了单位化**：只正交不单位化，$Q$ 不是正交矩阵。
      6. **对不同特征值的特征向量做施密特**：==它们已经正交==，
         只需在重根内部做。
      7. **配方法没有平方项时直接配**：==要先作造平方的换元==，
         并验证该换元可逆。
      8. **复合变换方向搞反**：坚持"旧用新表示"，==否则要多求一次逆==。
      9. **把配方法的系数当成特征值**：==只有正交变换法的系数才是特征值==。
      10. **不验算**：正交法查 $\tr$ 与列模长，配方法代两组具体值。
    ` },

  ],
});
