/* ==========================================================================
   线性代数 / 4 线性方程组 / 与向量组语言的互译
   —— 这一页不讲新算法，只做一件事：把"向量组的话"和"方程组的话"对齐。
      判别定理见 linear-systems/solvability；相关性判定见 vectors/independence。
   ========================================================================== */

KM.page({
  path: 'linear-algebra/linear-systems/translation',
  title: '与向量组语言的互译',
  subtitle: '相关性 = 齐次方程组有无非零解，可表示性 = 非齐次方程组有无解。**两边的命题应该能互相翻译**，但课本通常只教一个方向',
  tags: ['概念辨析', '高频', '证明题'],
  updated: '2026-08-27',

  blocks: [

    { t: 'md', c: String.raw`
      向量组这一章和方程组这一章，讲的其实是==同一件事的两种记法==。
      把 $\alpha_1,\dots,\alpha_n$ 按列拼成矩阵 $A=(\alpha_1,\dots,\alpha_n)$ 之后，
      $$x_1\alpha_1+x_2\alpha_2+\cdots+x_n\alpha_n\ =\ Ax,$$
      左边是"向量的线性组合"，右边是"矩阵乘向量"，==它们逐字相等==。
      于是所有关于组合的问题，都变成了关于 $Ax$ 的问题。
    ` },

    { t: 'compare',
      id: 'dictionary',
      title: '★ 词典：同一句话的三种说法',
      cols: ['向量组的说法', '方程组的说法', '矩阵与秩的说法'],
      rows: [
        [String.raw`$\alpha_1,\dots,\alpha_n$ ==线性相关==`,
         String.raw`$Ax=0$ 有==非零解==`,
         String.raw`$\rank(A)<n$`],
        [String.raw`$\alpha_1,\dots,\alpha_n$ ==线性无关==`,
         String.raw`$Ax=0$ ==只有零解==`,
         String.raw`$\rank(A)=n$（==列满秩==）`],
        [String.raw`$\beta$ ==能==由 $\alpha_i$ 表示`,
         String.raw`$Ax=\beta$ ==有解==`,
         String.raw`$\rank(A)=\rank(A\mid\beta)$`],
        [String.raw`$\beta$ ==不能==由 $\alpha_i$ 表示`,
         String.raw`$Ax=\beta$ ==无解==`,
         String.raw`$\rank(A\mid\beta)=\rank(A)+1$`],
        [String.raw`表示法==唯一==`,
         String.raw`$Ax=\beta$ 有==唯一解==`,
         String.raw`$\rank(A)=\rank(A\mid\beta)=n$`],
        [String.raw`表示法有==无穷多种==`,
         String.raw`$Ax=\beta$ 有==无穷多解==`,
         String.raw`$\rank(A)=\rank(A\mid\beta)<n$`],
        [String.raw`$\alpha_i$ 的==极大无关组==`,
         String.raw`阶梯形里的==主元列==对应的未知数`,
         String.raw`挑出 $r$ 个列构成列满秩子块`],
        [String.raw`$\alpha_i$ 张成的空间（==秩 $=r$==）`,
         String.raw`使 $Ax=\beta$ 有解的那些 $\beta$`,
         String.raw`列空间 $\Span\set{\alpha_1,\dots,\alpha_n}$，维数 $r$`],
        [String.raw`两个向量组==等价==`,
         String.raw`两组向量互相"表示得出来"`,
         String.raw`$\rank(S_1)=\rank(S_2)=\rank(S_1\cup S_2)$`],
      ] },

    { t: 'md', c: String.raw`
      ==这张表要能从任意一列读到另外两列。== 考研题的出题方式就是
      "用第一列问，用第三列答"，中间必须经过第二列。
      详细的判别定理在[三种情形那一节](#/linear-algebra/linear-systems/solvability?at=three-cases)，
      相关性的定义与判定在[线性相关性那一页](#/linear-algebra/vectors/independence?at=def)。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'two-views', c: '一、同一个 $Ax=b$ 的两种读法' },

    { t: 'key', id: 'row-vs-col', title: '★ 行观点与列观点', c: String.raw`
      $$\begin{pmatrix}a_{11}&\cdots&a_{1n}\\ \vdots&&\vdots\\ a_{m1}&\cdots&a_{mn}\end{pmatrix}
      \begin{pmatrix}x_1\\ \vdots\\ x_n\end{pmatrix}
      =\begin{pmatrix}b_1\\ \vdots\\ b_m\end{pmatrix}$$

      **行观点（中学的读法）**：横着看，每一行是一个方程
      $$a_{i1}x_1+\cdots+a_{in}x_n=b_i,$$
      它在 $\R^{n}$ 里是==一张超平面==。于是
      $$\boxed{\ \text{解集}=m\ \text{张超平面的**交集**}\ }$$
      "无解"就是这些超平面==交不到一起==，"无穷多解"就是它们交出一条线、一个面。
      这条几何线索单独展开在[第三节](#/linear-algebra/linear-systems/translation?at=hyperplane)。

      **列观点（线代的读法）**：竖着看，方程组就是
      $$x_1\alpha_1+x_2\alpha_2+\cdots+x_n\alpha_n=b,$$
      于是
      $$\boxed{\ \text{有解}\iff b\ \text{能被列向量**拼出来**}\ }$$

      ==向量组那一章讲的全部内容，都是列观点。==
      所谓"把向量组问题翻译成方程组问题"，
      其实只是==把竖着的读法换成横着的读法==，方程组本身一个字没变。

      **两个观点住在不同的空间里，这一点极易混**：

      | | 住在哪 | 维数 |
      |---|---|---|
      | 未知数 $x$、解集 | ==$\R^{n}$==（$n=$ 未知数个数） | 解空间 $n-r$ |
      | 列向量 $\alpha_i$、右端 $b$ | ==$\R^{m}$==（$m=$ 方程个数） | 列空间 $r$ |

      ==所以"基础解系有几个向量"和"极大无关组有几个向量"是两个不同空间里的数==，
      前者 $n-r$、后者 $r$，**加起来才是 $n$**。
    ` },

    { t: 'key', id: 'dimension', title: '维数守恒：$n=r+(n-r)$ 到底在守恒什么', c: String.raw`
      $$\boxed{\ \underbrace{\rank(A)}_{\text{列空间的维数}}
      \ +\ \underbrace{n-\rank(A)}_{\text{解空间的维数}}\ =\ n\ }$$

      把 $A$ 看成一个把 $\R^{n}$ 打到 $\R^{m}$ 的映射 $x\mapsto Ax$：

      - ==打出来的像==有多大？$r$ 维——就是列空间；
      - ==被压没的方向==有多少？$n-r$ 维——就是 $Ax=0$ 的解空间。

      $n$ 个自由度，$r$ 个活了下来，$n-r$ 个被压成了零，==一个也不会丢==。

      **这条守恒是很多结论的共同来源**：

      - $\rank(A)=n$（列无关）$\Rightarrow$ 解空间 $0$ 维 $\Rightarrow$ 只有零解；
      - $n>m$ $\Rightarrow r\le m<n\Rightarrow$ 解空间至少 $n-m$ 维 $\Rightarrow$ ==必有非零解==；
      - $r<m$ $\Rightarrow$ 列空间填不满 $\R^{m}$ $\Rightarrow$ ==存在 $b$ 使方程组无解==。

      ==第三条正是"不是任何 $b$ 都表示得出来"的来源==，
      而它在向量组那边的说法是"这组向量张不成整个空间"。
      基础解系的求法与 $n-r$ 的三重含义见[齐次方程组那一节](#/linear-algebra/linear-systems/solvability?at=homogeneous)。
    ` },

    { t: 'key', id: 'rank-jump', title: '秩的跃升判定：为什么秩最多只涨 $1$', c: String.raw`
      $$\rank(A)\ \le\ \rank(A\mid\beta)\ \le\ \rank(A)+1$$

      **用列空间一句话说清**：$(A\mid\beta)$ 的列空间是
      $\Span\set{\alpha_1,\dots,\alpha_n}$ ==再添一个向量==张成的，
      往一个空间里加一个向量，==维数要么不变，要么加 $1$==，不可能加 $2$。

      于是只有两种情况：

      | 秩 | 含义 | 结论 |
      |---|---|---|
      | ==不涨== | $\beta\in\Span\set{\alpha_i}$ | 有解，$\beta$ ==能被表示== |
      | ==涨 $1$== | $\beta\notin\Span\set{\alpha_i}$ | 无解，$\beta$ ==带来了新方向== |

      ==所以"添一列秩涨没涨"这一个动作，就把可表示性问了个干净。==
      实操上不用真去比两个秩：把 $(A\mid\beta)$ 化阶梯形，
      看有没有 $(0,\dots,0\mid d)$ 且 $d\ne0$ 的行即可，
      ==有就是无解，没有就是有解==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'props', c: '二、两个方向的命题清单' },

    { t: 'key', id: 'to-system', title: String.raw`方向一：向量组 $\Rightarrow$ 方程组（课本主讲的方向）`, c: String.raw`
      **命题一（内部转化）**：$\alpha_1,\dots,\alpha_n\ (n\ge2)$ 线性相关
      $\iff$ ==其中至少有一个==向量能由其余 $n-1$ 个线性表示。

      ==是"至少有一个"，不是"每一个"==——这个坑见[相关性的性质](#/linear-algebra/vectors/independence?at=properties)里的反例。

      **命题二（扩充转化，最高频）**：若 $\alpha_1,\dots,\alpha_n$ ==无关==，
      而 $\alpha_1,\dots,\alpha_n,\beta$ ==相关==，则 $\beta$ 必能由前者表示，==且表示法唯一==。

      证明的踩分点是==先证 $\beta$ 的系数 $k\ne0$==，
      完整两行写法见[线性表示与相关性](#/linear-algebra/vectors/independence?at=representation)。
      用秩来看更直白：无关给出 $\rank(A)=n$，相关给出 $\rank(A\mid\beta)<n+1$，
      两者合起来只能是 $\rank(A\mid\beta)=n=\rank(A)$，==秩没涨，所以有解==。

      **命题三（传递转化）**：若向量组 $S_1$ 能由 $S_2$ 线性表示，则
      $$\rank(S_1)\le\rank(S_2).$$

      ==这里有一处精度必须分清==：

      | 结论 | 需要的条件 |
      |---|---|
      | $\rank(S_1)\le\rank(S_2)$ | ==只需"能被表示"==，不需要无关 |
      | $S_1$ 的==个数== $\le S_2$ 的==个数== | ==还要 $S_1$ 线性无关== |

      因为 $S_1$ 无关时才有"个数 $=\rank(S_1)$"，才能把秩的不等式升级成个数的不等式：
      $$\#S_1=\rank(S_1)\le\rank(S_2)\le\#S_2.$$
      ==把这两句混为一谈是这条命题最常见的用错方式。==
      逆否形式（"多的被少的表示，多的必相关"）见[秩与可表示性](#/linear-algebra/vectors/basis?at=rank-and-representation)。
    ` },

    { t: 'key', id: 'to-vectors', title: String.raw`★ 方向二：方程组 $\Rightarrow$ 向量组（通常被略过的一半）`, c: String.raw`
      下面每一条都是方程组这边的标准结论，==右边是它翻回向量组语言之后的样子==。
      **能把右边说出来，才算真的打通了这两章。**

      **① 解的个数只由 $A$ 决定，$b$ 只决定有没有解。**
      $Ax=b$ 有唯一解 $\iff$ 导出组 $Ax=0$ 只有零解 $\iff\rank(A)=n$。
      <br>==向量组读法==：能不能表示看 $\beta$，表示法唯不唯一==只看 $\alpha$ 自己相不相关==。

      **② 维数公式**：解空间维数 $=n-\rank(A)$。
      <br>==向量组读法==：列向量组里有 $n-r$ 个是"多余的"，
      ==每一个多余向量恰好贡献一个自由未知量==。
      $r=n$（一个都不多余）时解空间为零维，就是只有零解。

      **③ 通解结构**：$Ax=b$ 的通解 $=$ 一个特解 $+$ 导出组的通解。
      <br>==向量组读法==：$\beta$ 的所有表示方式，等于==任取一种表示法，
      再叠加上列向量之间的全部相关关系==。
      $\alpha_i$ 无关时没有相关关系可叠加，所以表示法唯一。
      ==这句话把"表示法为什么不唯一"解释透了==，
      细节见[非齐次解的结构](#/linear-algebra/linear-systems/solvability?at=nonhomogeneous)。

      **④ 系数和判据**：$\eta_i$ 都是 $Ax=b$ 的解时，
      $\sum c_i\eta_i$ 是 $Ax=b$ 的解 $\iff\sum c_i=1$；是 $Ax=0$ 的解 $\iff\sum c_i=0$。
      <br>==向量组读法==：==两种表示法之差，一定是一个相关关系==。

      **⑤ $AB=O$**：则 $B$ 的==每一列都是 $Ax=0$ 的解==，于是
      $$\rank(A)+\rank(B)\le n\qquad(A\ \text{有}\ n\ \text{列}).$$
      <br>==向量组读法==：$B$ 的列全部落在 $A$ 的解空间里，
      而解空间只有 $n-\rank(A)$ 维，==装不下更多无关向量==。
      ==这是"方程组语言反过来解决矩阵问题"的最典型例子==，
      同一条不等式的其他证法见[秩的五条不等式](#/linear-algebra/matrix/rank?at=rank-inequalities)。

      **⑥ 同解判据**：$Ax=0$ 与 $Bx=0$ 同解 $\iff\rank(A)=\rank(B)=\rank\begin{pmatrix}A\\ B\end{pmatrix}$。
      <br>==向量组读法==：$A$ 的行向量组与 $B$ 的行向量组==等价==。
      注意==这里用的是行向量组==，和本页其余地方的列观点不同，
      详见[同解那一节](#/linear-algebra/linear-systems/common-sol?at=same-solution)。

      **⑦ 克拉默法则**：$A$ 为方阵且 $\abs A\ne0$ 时 $x_j=\abs{A_j}/\abs A$。
      <br>==向量组读法==：$n$ 个 $n$ 维向量无关时，
      ==空间里任何向量都能被它们唯一表示==，而且系数有行列式的公式。
      用法与局限见[方阵情形的讨论](#/linear-algebra/linear-systems/parameters?at=when-square)。

      **⑧ 方程少于未知数（$m<n$）必有非零解。**
      <br>==向量组读法==：==个数超过维数的向量组必相关==——
      这两句是同一条命题，见[判定方法](#/linear-algebra/vectors/independence?at=judge)里的那个方框。
    ` },

    { t: 'insight', id: 'my-gap', title: '我的疑问：这套转化只讲了一半', c: String.raw`
      看完那份总结我的感觉是：==向量组这边讲得很细，方程组这边讲得不细，好几个命题根本没提到==。

      想了一下，问题不在于哪一章写得长短，而在于==这套转化几乎总是被单向使用==：
      遇到"相关吗""能表示吗"，就翻成方程组去算；
      可是反过来，方程组自己的那些命题——解的结构、$n-r$、$AB=O$、同解——
      ==很少有人把它们翻回向量组的话再说一遍==。

      于是就出现一种夹生状态：两章的结论我都背得出来，
      但它们在我脑子里是==两套互不相干的口诀==，
      考场上题目一换个说法就要重新想一遍。

      所以这一页我按==两个方向==各列一遍。
      判断自己有没有真懂的标准很简单：
      ==随便挑一条方程组的结论，能不能立刻说出它在向量组那边长什么样。==
      说不出来的那几条，就是还没打通的地方。
    ` },

    { t: 'method', id: 'how-to-translate', title: '实战：看到什么就翻成什么', c: String.raw`
      **拿到题先找触发词，翻译完再动手算。**

      | 题面出现 | 立刻翻成 | 接下来算 |
      |---|---|---|
      | 判断==线性相关性== | $Ax=0$ 有无非零解 | 化 $A$ 的阶梯形，比 $\rank(A)$ 与 $n$ |
      | ==能否线性表示== | $Ax=\beta$ 有无解 | 化==增广矩阵==，比两个秩 |
      | ==表示法唯一吗== | 解唯不唯一 | 只看 $\rank(A)$ 与 $n$，==与 $\beta$ 无关== |
      | ==求出表示式== | 解出方程组 | 化行最简形，读通解 |
      | ==两组向量等价== | 互相表示 | 比 $\rank(S_1),\rank(S_2),\rank(S_1\cup S_2)$ |
      | $AB=O$ | $B$ 的列是 $Ax=0$ 的解 | 用 $\rank(A)+\rank(B)\le n$ |
      | 给了==几个解==求通解 | 用系数和造齐次解 | 先算 $n-r$ 定个数 |
      | ==任意 $b$ 都有解== | 列空间填满 $\R^{m}$ | $\rank(A)=m$（==行满秩==） |

      **一个统一的操作**：不管题目怎么问，
      ==把向量按列拼成矩阵、写出（增广）矩阵、做初等行变换化到阶梯形==，
      然后从阶梯形里读出所有信息：

      - 非零行数 $=\rank$；
      - 主元列 $=$ 极大无关组的位置；
      - 有没有矛盾行 $=$ 有没有解；
      - 非主元列个数 $=$ 自由未知量个数 $=n-r$。

      ==一次行变换回答四个问题，这是本章效率最高的动作。==
      **千万不要为了不同的问题把同一个矩阵化好几遍。**
    ` },

    /* ================================================================== */
    { t: 'h', id: 'geometry', c: '三、几何：$m$ 张超平面怎么交' },

    { t: 'key', id: 'hyperplane', title: '一个方程就是一张超平面', c: String.raw`
      行视角里，第 $i$ 个方程
      $$a_{i1}x_1+a_{i2}x_2+\cdots+a_{in}x_n=b_i$$
      在 $\R^{n}$ 中是一张==超平面==（$n=2$ 是直线，$n=3$ 是平面）。于是
      $$\boxed{\ \text{解集}\ =\ m\ \text{张超平面的交集}\ }$$

      **有解时交集是什么形状**：
      $$\dim(\text{交集})=n-\rank(A)$$
      $r=n$ 时交成==一个点==，$r=n-1$ 时交成==一条直线==，依此类推。
      ==这就是"自由未知量个数"的几何含义==——
      交集里还剩几个方向可以自由走动。

      **两种退化的行别忘了**（它们正是阶梯形里那两种零行的几何意义）：

      | 阶梯形里的行 | 方程长什么样 | 几何 |
      |---|---|---|
      | $(0,\dots,0\mid 0)$ | $0=0$ | ==整个空间==，这个方程==没有约束力==（多余方程） |
      | $(0,\dots,0\mid d),\ d\ne0$ | $0=d$ | ==空集==，它和谁交都是空的 $\Rightarrow$ ==无解== |

      ==所以"出现矛盾行就无解"不是一条要背的规则==，
      它就是"某张超平面根本不存在，交集当然为空"。

      **$m$ 与 $n$ 的几何意义也别搞反**：
      ==$m$ 是超平面的张数，$n$ 是空间的维数==。
      $m<n$（超平面不够多）时交集至少 $n-m$ 维，==不可能只交出一个点==，
      这正是[方程少于未知数必有无穷多解或无解](#/linear-algebra/linear-systems/translation?at=to-vectors)那一条。
    ` },

    { t: 'compare',
      id: 'geometry-table',
      title: '★ 交点状态 · 向量关系 · 秩：一张表对齐三种语言',
      cols: ['几何（行视角）', '向量组（列视角）', '秩'],
      rows: [
        [String.raw`==没有公共交点==<br>（如三线围成三角形、或平行）`,
         String.raw`$\beta$ ==不能==被 $\alpha_1,\dots,\alpha_n$ 表示<br>（$\beta$ 在列空间之外）`,
         String.raw`$\rank(A)<\rank(A\mid\beta)$`],
        [String.raw`==有公共交点==（至少一个）`,
         String.raw`$\beta$ ==能==被表示<br>（$\beta$ 落在列空间里）`,
         String.raw`$\rank(A)=\rank(A\mid\beta)$`],
        [String.raw`==恰好交于一点==`,
         String.raw`表示法==唯一==<br>（列向量组==无关==）`,
         String.raw`$\rank(A)=\rank(A\mid\beta)=n$`],
        [String.raw`==交成一条线 / 一张面==`,
         String.raw`表示法==不唯一==<br>（列向量组==相关==，有冗余）`,
         String.raw`$\rank(A)=\rank(A\mid\beta)<n$<br>交集维数 $n-r$`],
      ] },

    { t: 'md', c: String.raw`
      ==第三行和第四行的差别，全在"列向量组有没有冗余"上==：
      有冗余就意味着可以"多走一步再绕回来"，==抵达 $\beta$ 的路径就不止一条==，
      对应到几何上就是交集不止一个点。
    ` },

    { t: 'key', id: 'axes-in-system', title: '★ 两个轴在方程组这边分别是什么', c: String.raw`
      向量组那边有[两个互相垂直的变形方向](#/linear-algebra/vectors/independence?at=properties)：
      ==动向量的个数==（取子组）和==动向量的维数==（延长 / 截短）。
      翻到方程组这边，它们恰好是==增删方程==与==增删未知数==：

      | 方程组的操作 | 矩阵 | 列向量发生了什么 | 用哪条口诀 |
      |---|---|---|---|
      | ==加一个方程== | 加一==行== | 每个列向量==延长== | 无关的延长仍无关 |
      | ==删一个方程== | 删一==行== | 每个列向量==截短== | 相关的截短仍相关 |
      | ==加一个未知数== | 加一==列== | ==多一个列向量== | 部分无关不保证整体无关 |
      | ==删一个未知数== | 删一==列== | ==少一个列向量== | 整体无关则部分无关 |

      ==最容易错的是第一行==：**加一个方程不是"多一个列向量"，而是"每个列向量长高一截"**。
      列向量的==个数==由未知数个数 $n$ 决定，==维数==由方程个数 $m$ 决定，
      这一点和[行观点/列观点住在哪个空间](#/linear-algebra/linear-systems/translation?at=row-vs-col)是同一件事。

      **由此可以直接读出两条结论**：

      **① 加方程：唯一性丢不掉，但可能变无解。**
      原来解唯一意味着列向量组无关；加一行后列向量延长，
      ==由"无关的延长仍无关"，它们仍然无关==，故 $\rank(A)$ 还是 $n$。
      于是新方程组==要么还是那个唯一解，要么无解==，
      ==绝不可能变成无穷多解==。
      至于会不会无解，取决于 $\beta$ 新长出来的那个分量配不配得上——
      配不上，等式就在新维度上崩了。

      **② 删方程：解只会更多，不会更少。**
      删掉一个约束，原来的解显然还是解，==解集只增不减==。
      所以==无解可能变成有解，唯一解可能变成无穷多解==。

      ==这两条几乎不用算，是选择题里判断"某个操作之后解会怎样"的直接依据。==

      **一个几何画面帮你记 ②**：三条直线围成三角形时无公共交点，
      ==随便去掉一条，剩下两条必定交于一点==——
      约束一松，解就出来了。完整算法见[下面那道例题](#/linear-algebra/linear-systems/translation?at=ex-three-lines)。
    ` },

    { t: 'key', id: 'homogeneous-geo', title: '齐次：所有超平面都被钉在原点上', c: String.raw`
      $b=0$ 时每个方程变成 $a_{i1}x_1+\cdots+a_{in}x_n=0$，
      ==所有超平面都强制过原点==。于是

      $$\boxed{\ \text{原点是天然的公共交点}\ \Longrightarrow\ \text{齐次方程组**永远有解**}\ }$$

      ==这就是"齐次方程组说无解"为什么一定错==的几何理由。
      在列视角里它同样显然：$\beta=0$ ==总能用全零系数拼出来==。

      **于是齐次情形的问题从"有没有解"降级成"解多不多"**：

      | 几何 | 向量组 | 秩 |
      |---|---|---|
      | 只交于==原点一点== | $\alpha_i$ ==线性无关== | $\rank(A)=n$ |
      | 交出==过原点的直线 / 平面== | $\alpha_i$ ==线性相关== | $\rank(A)<n$，交集维数 $n-r$ |

      ==所以"齐次方程组有没有非零解"和"列向量组相不相关"是同一个问题的两种问法==，
      而[非齐次那边还要多管一件事](#/linear-algebra/linear-systems/translation?at=dictionary)：$\beta$ 够不够得着。

      **基础解系的几何**：$Ax=0$ 的基础解系，
      就是==那条（那张）过原点的交集的一组方向向量==，
      个数 $n-r$ 正是交集的维数，见[齐次方程组那一节](#/linear-algebra/linear-systems/solvability?at=homogeneous)。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '四、例题' },

    { t: 'example',
      id: 'ex-three-lines',
      title: '★ 三条直线：无解、删一条、以及共点条件',
      source: '经典几何—代数互译题',
      level: 2,
      problem: String.raw`
        平面上给定三条直线
        $$l_1:\ x+y=1,\qquad l_2:\ x-y=1,\qquad l_3:\ x+2y=0.$$

        1. 三条直线是否有公共交点？用秩说明。
        2. 把这件事翻译成"某个向量能否被线性表示"。
        3. 删去 $l_3$ 之后结论怎么变？
        4. 把 $l_3$ 的右端 $0$ 改成什么，三线才共点？
      `,
      idea: String.raw`
        **第 1 问**：三条直线共点 $\iff$ 方程组有解 $\iff\rank(A)=\rank(\bar A)$。
        $n=2$ 个未知数、$m=3$ 个方程，==方程比未知数多，大概率无解==，
        除非第三条恰好穿过前两条的交点。
        ==最省事的做法是先求 $l_1\cap l_2$，再把这个点代进 $l_3$ 试一下==，
        比化矩阵还快；但答题要用秩说话，所以两条都走一遍。

        **第 2 问**：把系数竖着提，
        $$x\begin{pmatrix}1\\1\\1\end{pmatrix}+y\begin{pmatrix}1\\-1\\2\end{pmatrix}
        =\begin{pmatrix}1\\1\\0\end{pmatrix},$$
        ==三个分量分别来自三条直线==。于是问题变成
        "$\beta$ 能不能由 $\alpha_1,\alpha_2$ 表示"。
        ==注意这两个列向量是 $3$ 维的，$3$ 来自方程个数，不是来自未知数个数==。

        **第 3 问**：删一个方程 $=$ [每个列向量截短一格](#/linear-algebra/linear-systems/translation?at=axes-in-system)。
        约束变松，==解只会更多==。几何上就是"三角形去掉一条边，剩下两条必然相交"。

        **第 4 问**：三线共点要求 $\rank(\bar A)=2$，
        而 $\bar A$ 是 $3\times3$ 的，==等价于 $\abs{\bar A}=0$==。
        ==这就是"三线共点"的行列式判据==，选择题里经常直接考。
      `,
      solution: String.raw`
        **1. 判有无公共交点。** 对增广矩阵作行变换：
        $$\bar A=\left(\begin{array}{cc|c}
        1&1&1\\ 1&-1&1\\ 1&2&0\end{array}\right)
        \xrightarrow{r_2-r_1,\ r_3-r_1}
        \left(\begin{array}{cc|c}
        1&1&1\\ 0&-2&0\\ 0&1&-1\end{array}\right)
        \xrightarrow{r_2\div(-2),\ r_3-r_2}
        \left(\begin{array}{cc|c}
        1&1&1\\ 0&1&0\\ 0&0&-1\end{array}\right).$$

        末行是 $(0,0\mid-1)$，即 $0=-1$，==矛盾行==。故
        $$\rank(A)=2<3=\rank(\bar A),$$
        方程组==无解==，三条直线==没有公共交点==。

        （两两求交点可知它们分别交于 $(1,0)$、$(2,-1)$、$\bigl(\tfrac23,-\tfrac13\bigr)$，
        ==三个不同的点，正是围成一个三角形==。）

        **2. 列视角的说法。** 记
        $$\alpha_1=\begin{pmatrix}1\\1\\1\end{pmatrix},\quad
        \alpha_2=\begin{pmatrix}1\\-1\\2\end{pmatrix},\quad
        \beta=\begin{pmatrix}1\\1\\0\end{pmatrix},$$
        则原问题即"$\beta$ 能否由 $\alpha_1,\alpha_2$ 线性表示"。
        由第 1 问 $\rank(\alpha_1,\alpha_2)=2$ 而 $\rank(\alpha_1,\alpha_2,\beta)=3$，
        ==添 $\beta$ 后秩涨了 $1$，故 $\beta$ 不能被表示==，
        即 $\beta$ ==落在 $\alpha_1,\alpha_2$ 张成的平面之外==。

        **3. 删去 $l_3$。** 此时三个向量各自==截短掉第三个分量==：
        $$\tilde\alpha_1=\begin{pmatrix}1\\1\end{pmatrix},\quad
        \tilde\alpha_2=\begin{pmatrix}1\\-1\end{pmatrix},\quad
        \tilde\beta=\begin{pmatrix}1\\1\end{pmatrix}.$$
        由 $\begin{vmatrix}1&1\\1&-1\end{vmatrix}=-2\ne0$ 知 $\rank=2=n$，
        故==有唯一解==
        $$x=1,\quad y=0,$$
        即 $l_1$ 与 $l_2$ 交于唯一点 $(1,0)$，且 $\tilde\beta=1\cdot\tilde\alpha_1+0\cdot\tilde\alpha_2$，
        ==截短之后就能表示了==。

        **4. 共点条件。** 设 $l_3:\ x+2y=c$，三线共点要求
        $$\abs{\bar A}=\begin{vmatrix}1&1&1\\ 1&-1&1\\ 1&2&c\end{vmatrix}=0.$$
        按第一行展开：
        $$=1\cdot(-c-2)-1\cdot(c-1)+1\cdot(2+1)=-2c+2.$$
        令其为零得 $\boxed{c=1}$。

        **验证**：$l_1\cap l_2=(1,0)$，代入 $x+2y=1$ 得 $1+0=1\ \checkmark$。
      `,
      comment: String.raw`
        **这道题把三种语言在一道小题里跑了一遍**：

        | 问 | 几何 | 向量组 | 秩 |
        |---|---|---|---|
        | 1 | 围成三角形 | $\beta$ 表示不了 | $2=\rank(A)<\rank(\bar A)=3$ |
        | 3 | 两线交于一点 | 截短后能唯一表示 | $\rank=\rank=2=n$ |
        | 4 | 三线共点 | $\beta$ 回到平面内 | $\abs{\bar A}=0$ 且 $\rank(A)=2$ |

        **第 3 问是"截短"最干净的一次亮相**：
        ==同一组向量，$3$ 维时表示不了，$2$ 维时就表示得了==。
        这正是[「无关的截短未必无关」「表示不了的截短之后可能表示得了」](#/linear-algebra/vectors/independence?at=properties)
        ——==截短会丢信息，而丢掉的信息恰恰是那个"不匹配"的分量==。

        **第 4 问的判据要写完整**：$\abs{\bar A}=0$ ==只保证 $\rank(\bar A)\le2$==，
        还需要 $\rank(A)=2$（即前两条直线==不平行==）才能断定"恰好共于一点"。
        ==若 $\rank(A)=1$（三条直线互相平行或重合），$\abs{\bar A}=0$ 也可能成立==，
        那时要么无解要么整条直线都是解。
        **考试里直接写"行列式为零即共点"是会被扣分的**，
        要补一句"且系数矩阵秩为 $2$"。

        **一个值得记住的推广**：$n$ 个 $n$ 元方程时，
        $$\text{有唯一解}\iff\abs A\ne0,$$
        本题因为 $m=3>n=2$ 不是方阵，==才要用增广矩阵的行列式说事==。
        ==方程比未知数多的时候，"多出来的方程是不是冗余的"就是全部问题==，
        $\abs{\bar A}=0$ 说的正是"第三个方程能被前两个线性表示，没带来新约束"。
      `,
    },

    { t: 'example',
      id: 'ex-three-languages',
      title: String.raw`★ 一个条件，四种追问：$4\times3$ 矩阵，$\rank(A)=2$`,
      source: '概念题（自编，覆盖全表）',
      level: 2,
      problem: String.raw`
        设 $A$ 是 $4\times3$ 矩阵，$\rank(A)=2$，记 $A$ 的列向量组为 $\alpha_1,\alpha_2,\alpha_3$。回答：

        1. $Ax=0$ 的基础解系含几个向量？
        2. $\alpha_1,\alpha_2,\alpha_3$ 相关还是无关？极大无关组含几个向量？
        3. 是否任意 $b\in\R^{4}$ 都能由 $\alpha_1,\alpha_2,\alpha_3$ 线性表示？
        4. 若某个 $b$ 能被表示，表示法唯一吗？
      `,
      idea: String.raw`
        **先把两个数字摆清楚**：==$m=4$（行数，向量的维数），$n=3$（列数，向量的个数）==，
        $r=2$。
        这四问分别落在[词典](#/linear-algebra/linear-systems/translation?at=dictionary)的不同行上，
        ==全部只需要 $m,n,r$ 三个数==，一个具体元素都不用知道。

        **最容易错的是第 1 问和第 2 问的数字反过来。**
        提醒自己[两个观点住在不同空间](#/linear-algebra/linear-systems/translation?at=row-vs-col)：
        解向量住在 $\R^{3}$，个数 $n-r$；列向量住在 $\R^{4}$，无关的有 $r$ 个。
        ==$3-2=1$ 和 $2$ 是两个不同的答案。==

        **第 3 问的想法**：列向量只有 $3$ 个、秩只有 $2$，
        它们张成的空间最多 $2$ 维，==而 $\R^{4}$ 是 $4$ 维==，肯定填不满。

        **第 4 问的想法**：唯一性只看 $\rank(A)$ 与 $n$ 比较，
        ==$2<3$，所以只要有解就是无穷多解==，与是哪个 $b$ 无关。
      `,
      solution: String.raw`
        **1.** 未知数个数 $n=3$，故基础解系含
        $$n-\rank(A)=3-2=1$$
        个向量。

        **2.** 由 $\rank(A)=2<3=n$ 知 $Ax=0$ 有非零解，故 $\alpha_1,\alpha_2,\alpha_3$ ==线性相关==；
        列向量组的秩等于 $\rank(A)=2$，故==极大无关组含 $2$ 个向量==，
        即三个向量中有一个是多余的。

        **3.** ==不是==。$\alpha_1,\alpha_2,\alpha_3$ 张成的空间维数为 $\rank(A)=2$，
        而 $\R^{4}$ 的维数是 $4$，$2<4$，故存在 $b\in\R^{4}$ 落在这个空间之外，
        对这样的 $b$，$Ax=b$ ==无解==，$b$ 表示不出来。

        （要"任意 $b$ 都能表示"，需要==列空间填满 $\R^{4}$==，即 $\rank(A)=4$，
        但 $\rank(A)\le\min\set{4,3}=3$，==本题根本不可能==。）

        **4.** ==不唯一==。若 $Ax=b$ 有解，则由 $\rank(A)=\rank(A\mid b)=2<3=n$
        知有==无穷多解==，且解集为
        $$x=\eta^{*}+k\xi,\qquad k\ \text{任意},$$
        其中 $\xi$ 是第 1 问那个基础解系向量。故 $b$ 有无穷多种表示法。
      `,
      comment: String.raw`
        **把四个答案排在一起看**：
        $$n-r=1\ (\text{基础解系}),\qquad r=2\ (\text{极大无关组}),\qquad
        1+2=3=n\ \checkmark$$
        ==两个数加起来等于 $n$==，这是[维数守恒](#/linear-algebra/linear-systems/translation?at=dimension)的直接体现，
        **也是检查自己有没有把两个数搞反的最快办法**。

        **第 3 问的一般结论值得单独记**：
        $$\text{任意}\ b\in\R^{m}\ \text{都能被列向量表示}\iff\rank(A)=m\ (\text{行满秩})$$
        $$\text{表示法唯一}\iff\rank(A)=n\ (\text{列满秩})$$
        ==一个管"存在"，一个管"唯一"，一个比 $m$、一个比 $n$，绝不能混。==
        两者同时成立就是 $m=n=r$，即 $A$ ==可逆==——
        这正是[可逆的等价条件](#/linear-algebra/matrix/operations?at=invertible-equiv)在说的事。

        **常见变体**：把条件换成"$A$ 是 $3\times4$ 矩阵，$\rank(A)=3$"，
        四问的答案全变：基础解系 $4-3=1$ 个，列向量组 $4$ 个 $3$ 维向量==必相关==
        （个数 $>$ 维数），==任意 $b\in\R^{3}$ 都能表示==（行满秩），但表示法不唯一。
        ==$m$ 和 $n$ 换个位置，结论就是另一套==，做题时第一件事永远是先认清哪个是哪个。
      `,
    },

    { t: 'example',
      id: 'ex-abo',
      title: '★ $AB=O$：把矩阵条件读成"$B$ 的列都是解"',
      source: '经典题型（高频）',
      level: 3,
      problem: String.raw`
        设 $A$ 为 $3$ 阶==非零==矩阵，$B$ 为 $3\times2$ 矩阵且 $\rank(B)=2$，
        已知 $AB=O$，求 $\rank(A)$。
      `,
      idea: String.raw`
        **看到 $AB=O$ 只做一个动作**：把 $B$ 按列拆开，
        $B=(\beta_1,\beta_2)$，则
        $$AB=(A\beta_1,\ A\beta_2)=O\ \Longrightarrow\ A\beta_1=0,\ A\beta_2=0.$$
        ==$B$ 的两列都是 $Ax=0$ 的解==。矩阵条件就此变成了方程组条件。

        **接下来是数数**：$\rank(B)=2$ 说明 $\beta_1,\beta_2$ ==线性无关==，
        也就是 $Ax=0$ 有==两个线性无关的解==。
        而解空间的维数是 $3-\rank(A)$，==它至少要装得下这两个无关的向量==：
        $$3-\rank(A)\ge2.$$

        **最后卡另一头**：上式给出 $\rank(A)\le1$，
        而 ==$A\ne O$ 保证 $\rank(A)\ge1$==，两边一夹就定死了。

        =="非零矩阵"这个条件专门用来卡住下界==，题目给了就一定要用上。
      `,
      solution: String.raw`
        记 $B=(\beta_1,\beta_2)$。由
        $$AB=(A\beta_1,\ A\beta_2)=O$$
        得 $A\beta_1=0,\ A\beta_2=0$，即 ==$\beta_1,\beta_2$ 都是 $Ax=0$ 的解==。

        由 $\rank(B)=2$ 知 $\beta_1,\beta_2$ 线性无关，
        故 $Ax=0$ 的解空间中含有 $2$ 个线性无关的向量，于是
        $$3-\rank(A)=\dim(\text{解空间})\ \ge\ 2,$$
        即 $\rank(A)\le1$。

        又 $A\ne O$，故 $\rank(A)\ge1$。

        综上 $\boxed{\rank(A)=1}$。
      `,
      comment: String.raw`
        **这道题的全部难度在第一步的翻译**。
        $AB=O$ 本身是个矩阵等式，看不出能干什么；
        一旦读成"==$B$ 的列都是 $Ax=0$ 的解=="，
        题目立刻变成一道"解空间够不够大"的送分题。

        **同一条思路的公式版**：$AB=O$ 且 $A$ 有 $n$ 列时
        $$\rank(A)+\rank(B)\le n.$$
        本题即 $\rank(A)+2\le3$。
        ==公式和上面的推理是一回事==，
        但考场上==写出推理过程更稳==：万一题目变形（比如给的是 $ABC=O$），
        公式未必套得上，而"列都是解"这个读法永远管用。

        **两个常见变体**：

        1. 只给 $B\ne O$（不给 $\rank(B)$）：则只知道 $Ax=0$ 有非零解，
           得 ==$\rank(A)\le2$==，无法定死。
           ==条件强弱直接决定结论能精确到什么程度==，别多答。
        2. 反过来给 $\rank(A)=1$，问 $\rank(B)$ 最大是多少：
           解空间是 $3-1=2$ 维，$B$ 的列全在里面，故 ==$\rank(B)\le2$==。
           ==同一个不等式，换个未知量问==。

        **顺带一提**：本题若去掉"$A\ne O$"，答案就只能写 $\rank(A)\le1$，
        ==即 $\rank(A)=0$ 或 $1$==。
        **凡是给了"非零"两个字的题，都要回头找它卡的是哪一头。**
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '五、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '互译时的固定失分点', c: String.raw`
      1. **$m$ 与 $n$ 搞反**：==$n$ 是未知数个数 $=$ 向量个数 $=$ 列数==；
         $m$ 是方程个数 $=$ 向量维数 $=$ 行数。
         ==这一条错了，后面全错。==
      2. **基础解系个数与极大无关组个数混淆**：==前者 $n-r$，后者 $r$==，
         它们在不同的空间里，加起来才是 $n$。
      3. **判可表示时只看 $\rank(A)$**：==必须比较 $\rank(A)$ 与 $\rank(A\mid\beta)$==。
      4. **判唯一性时去看 $\beta$**：==唯一性只由 $\rank(A)$ 与 $n$ 决定==，与 $\beta$ 无关。
      5. **"任意 $b$ 都有解"写成列满秩**：那是==行满秩 $\rank(A)=m$==；
         列满秩 $\rank(A)=n$ 管的是唯一性。
      6. **命题三用错**：==$\rank(S_1)\le\rank(S_2)$ 不需要无关==，
         要推"个数 $\le$ 个数"才必须加上 $S_1$ 无关。
      7. **命题一读成"每一个都能被其余表示"**：==只能保证至少有一个==。
      8. **$AB=O$ 只会背公式不会推**：==记住"$B$ 的列都是 $Ax=0$ 的解"==，
         变形题只有这个读法管用。
      9. **同解判据里用了列向量组**：==同解对应的是行向量组等价==。
      10. **为不同的问题把同一个矩阵化好几遍**：==一次行变换能同时回答秩、
          极大无关组、有无解、自由未知量个数==。
      11. **把"加一个方程"当成"多一个列向量"**：==它是每个列向量**延长**一格==；
          多一个列向量对应的是==加一个未知数==。
      12. **齐次方程组说"无解"**：==所有超平面都过原点，必有零解==。
      13. **由 $\abs{\bar A}=0$ 直接断言"三线共点"**：
          ==还要 $\rank(A)=2$==，否则可能是平行或重合的退化情形。
      14. **忘了退化的行**：$(0,\dots,0\mid0)$ 是==多余方程==，
          $(0,\dots,0\mid d\ne0)$ 是==空集==，两者天差地别。
    ` },

  ],
});
