/* ==========================================================================
   线性代数 / 4 线性方程组 / 解的存在性与结构
   —— 全书的汇合点：行列式、秩、向量组在这里变成"有没有解、有几个解"。
      含参数的讨论见 linear-systems/parameters；公共解见 common-sol。
   ========================================================================== */

KM.page({
  path: 'linear-algebra/linear-systems/solvability',
  title: '解的存在性与结构',
  subtitle: '一切都归到两个秩的比较：$\\rank(A)$ 与 $\\rank(A\\mid b)$ 比出**有没有解**，再与 $n$ 比出**有几个解**',
  tags: ['大题', '计算题', '高频'],
  updated: '2026-08-27',

  blocks: [

    { t: 'key', id: 'three-cases', title: '★ 判别定理：两次比较定生死', c: String.raw`
      设 $A$ 为 $m\times n$ 矩阵，考虑 $Ax=b$（$n$ 为未知数个数），记 $\bar A=(A\mid b)$。

      $$\boxed{\begin{aligned}
      \rank(A)<\rank(\bar A)\quad&\Longrightarrow\quad \text{无解}\\
      \rank(A)=\rank(\bar A)=n\quad&\Longrightarrow\quad \text{唯一解}\\
      \rank(A)=\rank(\bar A)=r<n\quad&\Longrightarrow\quad \text{无穷多解}（自由未知量\ n-r\ 个）
      \end{aligned}}$$

      **两次比较，各管一件事**：

      | 比较 | 回答 |
      |---|---|
      | $\rank(A)$ vs $\rank(\bar A)$ | ==有没有解== |
      | $\rank(A)$ vs $n$ | ==有几个解== |

      ==注意 $\rank(A)$ 与 $\rank(\bar A)$ 只差 $0$ 或 $1$==
      （加一列，秩最多涨一），所以第一次比较只有"相等"和"差 $1$"两种结果。

      **齐次方程组 $Ax=0$ 永远有解**（零解），所以只需第二次比较：
      $$\boxed{\ Ax=0\ \text{有非零解}\iff\rank(A)<n\ }$$
      特别地 $A$ 为方阵时 $\iff\abs A=0$，
      这就是[可逆等价条件](#/linear-algebra/matrix/operations?at=invertible-equiv)里的第 $4$ 条。

      **化成阶梯形后怎么看**：出现 $(0,0,\dots,0\mid d)$ 且 $d\ne0$ 的行 $\Rightarrow$ ==无解==；
      没有这种行时，==非零行数就是 $r$，自由未知量个数是 $n-r$==。

      **这三条同时也是向量组的三条结论**（$b$ 能不能被 $A$ 的列表示、表示法唯不唯一），
      对照表见[互译词典](#/linear-algebra/linear-systems/translation?at=dictionary)。
    ` },

    { t: 'key', id: 'homogeneous', title: '齐次方程组：基础解系', c: String.raw`
      $Ax=0$ 的全体解构成一个==线性空间==（解空间），因为
      ==解的线性组合仍是解==：$A(k_1\xi_1+k_2\xi_2)=0$。

      $$\boxed{\ \dim(\text{解空间})=n-\rank(A)\ }$$

      **基础解系** $=$ 解空间的一组基，即 $n-r$ 个==线性无关的解==，
      且每个解都能由它们线性表示。通解为
      $$x=k_1\xi_1+k_2\xi_2+\cdots+k_{n-r}\xi_{n-r},\qquad k_i\ \text{任意}.$$

      **求法（标准四步）**：

      1. 化 $A$ 为==行最简形==；
      2. 找出主元列与非主元列，==非主元对应的未知数就是自由未知量==（共 $n-r$ 个）；
      3. 依次令自由未知量取 $(1,0,\dots,0),(0,1,\dots,0),\dots$，
         ==每次回代求出其余分量==，得到 $n-r$ 个解向量；
      4. 它们就是基础解系。

      ==第 3 步取单位向量能保证所得的解线性无关==
      （它们在自由分量上构成单位阵，[延长仍无关](#/linear-algebra/vectors/independence?at=properties)），
      **这是为什么要这样取的理由**，答题时值得写一句。

      **$n-r$ 这个数的三重含义**（打通了才算懂）：
      $$\underbrace{n-r}_{\text{自由未知量个数}}
      =\underbrace{n-r}_{\text{基础解系向量个数}}
      =\underbrace{n-\rank(A)}_{\text{被 }A\text{ 压掉的维数}}$$
      ==$A$ 把 $n$ 维空间压成 $r$ 维，损失的 $n-r$ 个维度全部塌进了解空间。==
    ` },

    { t: 'key', id: 'nonhomogeneous', title: '非齐次方程组：特解 + 齐次通解', c: String.raw`
      $$\boxed{\ Ax=b\ \text{的通解}\ =\ \underbrace{\eta^{*}}_{\text{一个特解}}
      \ +\ \underbrace{k_1\xi_1+\cdots+k_{n-r}\xi_{n-r}}_{\text{导出组}\ Ax=0\ \text{的通解}}\ }$$

      **两条支撑性质**（证明题常直接考）：

      1. ==两个非齐次解之差是齐次解==：$A(\eta_1-\eta_2)=b-b=0$；
      2. ==非齐次解加齐次解仍是非齐次解==：$A(\eta+\xi)=b+0=b$。

      **由性质 1 立刻得到一个高频结论**：
      若 $\eta_1,\dots,\eta_s$ 是 $Ax=b$ 的解，则
      $$\sum_{i=1}^{s}c_i\eta_i\ \text{是}\ Ax=b\ \text{的解}\iff\sum c_i=1;$$
      $$\sum_{i=1}^{s}c_i\eta_i\ \text{是}\ Ax=0\ \text{的解}\iff\sum c_i=0.$$
      ==系数和为 $1$ 还是 $0$，决定了它落在哪一边==，这是选择题的常客。

      **求法**：把增广矩阵 $\bar A$ 化行最简形后，

      - **特解**：==令所有自由未知量为 $0$==，回代求出主元未知量；
      - **齐次通解**：把常数列换成 $0$（即只看 $A$ 部分），按上一节的四步做。

      ==两部分共用同一次行变换，不要化两遍。==
    ` },

    { t: 'key', id: 'affine', title: '解集的几何：一个「平移过的子空间」', c: String.raw`
      $$\text{齐次解集}=\text{过原点的 }(n-r)\text{ 维子空间}$$
      $$\text{非齐次解集}=\text{把它整体平移 }\eta^{*}\text{ 后得到的集合}$$

      ==所以非齐次解集**不是**线性空间==（不含零向量、对加法不封闭），
      它是一个**仿射子空间**。
      这解释了为什么通解必须写成"特解 $+$ 齐次通解"，
      ==而不能写成若干解的任意线性组合==。

      **这个结构在别处见过**：
      $$\underbrace{y=y^{*}+C_1y_1+C_2y_2}_{\text{二阶线性微分方程}}
      \qquad\longleftrightarrow\qquad
      \underbrace{x=\eta^{*}+k_1\xi_1+k_2\xi_2}_{\text{线性方程组}}$$
      ==完全同构==：都是"一个特解 $+$ 齐次解空间"，
      而齐次解空间的维数由"损失的自由度"决定。
      详见[解的结构那一页](#/calculus/ode/solution-structure?at=affine-rule)，
      它把这个共同骨架讲透了。

      **一条由此立得的判断**：$Ax=b$ 有唯一解 $\iff$ 导出组只有零解 $\iff\rank(A)=n$。
      =="解的个数"取决于齐次部分，与 $b$ 无关==；$b$ 只影响"有没有解"。
    ` },

    { t: 'method', id: 'steps', title: '求通解：五步走', c: String.raw`
      1. **写增广矩阵** $\bar A=(A\mid b)$；
      2. **只用初等行变换**化成行最简形（==不能用列变换==，会打乱未知数）；
      3. **判断**：有没有 $(0,\dots,0\mid d\ne0)$ 的行？有则无解，结束；
      4. **定自由未知量**：非主元列对应的那些，共 $n-r$ 个；
      5. **写通解**：特解（自由量全取 $0$）$+$ 基础解系（自由量依次取单位向量）。

      **格式上的三个得分点**：

      - ==必须写出 $\rank(A)$ 与 $\rank(\bar A)$ 并作比较==，这是判别定理的引用；
      - ==通解要带任意常数 $k_i$，并注明"$k_i$ 为任意常数"==；
      - ==基础解系要说明个数是 $n-r$==。

      **算完的两个自检**：

      1. ==把特解代回原方程验算==（十秒钟，能抓住绝大多数计算错误）；
      2. ==把基础解系代入 $Ax=0$ 验算==；
      3. 数一数基础解系的个数是不是 $n-\rank(A)$。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '一、例题' },

    { t: 'example',
      id: 'ex-general-solution',
      title: '求非齐次方程组的通解：完整流程',
      source: '标准例题',
      level: 2,
      problem: String.raw`
        求解方程组
        $$\begin{cases}
        x_1+x_2-x_3-x_4=1\\
        2x_1+x_2-3x_3+x_4=3\\
        3x_1+2x_2-4x_3+0\cdot x_4=4
        \end{cases}$$
      `,
      idea: String.raw`
        $n=4$ 个未知数、$3$ 个方程，==方程比未知数少，几乎肯定有无穷多解==
        （只要不出现矛盾行）。

        **动作**：写增广矩阵，行变换化行最简形，==一次到底==。
        化完之后：

        - 看有没有矛盾行（判有无解）；
        - 数非零行得 $r$，自由未知量 $4-r$ 个；
        - 特解、基础解系直接读。

        **化简时的小技巧**：第一列已经有 $1$，==直接用它消掉下面两行==，
        不用换行也不用凑分数。
      `,
      solution: String.raw`
        $$\bar A=\left(\begin{array}{cccc|c}
        1&1&-1&-1&1\\
        2&1&-3&1&3\\
        3&2&-4&0&4
        \end{array}\right)
        \xrightarrow{r_2-2r_1,\ r_3-3r_1}
        \left(\begin{array}{cccc|c}
        1&1&-1&-1&1\\
        0&-1&-1&3&1\\
        0&-1&-1&3&1
        \end{array}\right)$$
        $$\xrightarrow{r_3-r_2}
        \left(\begin{array}{cccc|c}
        1&1&-1&-1&1\\
        0&-1&-1&3&1\\
        0&0&0&0&0
        \end{array}\right)
        \xrightarrow{r_2\times(-1),\ r_1-r_2}
        \left(\begin{array}{cccc|c}
        1&0&-2&2&2\\
        0&1&1&-3&-1\\
        0&0&0&0&0
        \end{array}\right).$$

        **判别**：$\rank(A)=\rank(\bar A)=2<4=n$，故==有无穷多解==，
        自由未知量有 $4-2=2$ 个。

        主元在第 $1,2$ 列，故 $x_3,x_4$ 为自由未知量。方程组等价于
        $$\begin{cases}x_1=2+2x_3-2x_4\\ x_2=-1-x_3+3x_4\end{cases}$$

        **特解**（取 $x_3=x_4=0$）：$\eta^{*}=(2,-1,0,0)\T$。

        **基础解系**（对导出组，即上式去掉常数项）：

        - 取 $(x_3,x_4)=(1,0)$：$\xi_1=(2,-1,1,0)\T$；
        - 取 $(x_3,x_4)=(0,1)$：$\xi_2=(-2,3,0,1)\T$。

        **通解**：
        $$x=\begin{pmatrix}2\\-1\\0\\0\end{pmatrix}
        +k_1\begin{pmatrix}2\\-1\\1\\0\end{pmatrix}
        +k_2\begin{pmatrix}-2\\3\\0\\1\end{pmatrix},
        \qquad k_1,k_2\ \text{为任意常数}.$$

        **验算**（代入第一个方程）：
        特解 $2+(-1)-0-0=1\ \checkmark$；
        $\xi_1$：$2+(-1)-1-0=0\ \checkmark$；
        $\xi_2$：$-2+3-0-1=0\ \checkmark$
      `,
      comment: String.raw`
        **第三行整行为零不是巧合**：原方程组的第三式 $=$ 第一式 $+$ 第二式
        （$1+2=3$，$1+1=2$，$-1-3=-4$，$-1+1=0$，$1+3=4$），
        ==所以它是多余的方程==。
        $\rank=2$ 正说明"三个方程里只有两个是独立的"。

        **自由未知量的选择不唯一**：本题取 $x_3,x_4$ 是因为主元在第 $1,2$ 列。
        若化简路径不同，也可能取 $x_1,x_4$ 之类，
        ==得到的基础解系形式不同，但都对==，
        因为它们张成同一个解空间。
        ==考试中按主元列自然地选即可，不必纠结。==

        **验算为什么只代第一个方程**：因为第三个方程是前两个的组合，
        代前两个就够了。==稳妥起见把两个方程都代一遍==，仍然只要二十秒。

        **常见追问**："该方程组的解集是几维的？"
        ==注意区分==：解空间（齐次）是 $2$ 维；
        非齐次解集是[平移过的 $2$ 维仿射子空间](#/linear-algebra/linear-systems/solvability?at=affine)，
        ==本身不是线性空间，说"维数"时要讲清是哪个==。
      `,
    },

    { t: 'example',
      id: 'ex-structure',
      title: '★ 由已知的几个解反求通解',
      source: '经典题型（高频）',
      level: 4,
      problem: String.raw`
        设 $A$ 是 $4\times4$ 矩阵，$\rank(A)=3$，
        $\eta_1,\eta_2,\eta_3$ 是非齐次方程组 $Ax=b$ 的三个解，且
        $$\eta_1=\begin{pmatrix}1\\2\\3\\4\end{pmatrix},\qquad
        \eta_2+\eta_3=\begin{pmatrix}0\\1\\2\\3\end{pmatrix}.$$
        求 $Ax=b$ 的通解。
      `,
      idea: String.raw`
        **题目没给 $A$，所以不能化矩阵**——
        ==只能用[解的结构性质](#/linear-algebra/linear-systems/solvability?at=nonhomogeneous)==。

        **第一步：确定基础解系需要几个向量。**
        $n=4$，$\rank(A)=3$，故 $n-r=1$，
        ==基础解系只需一个非零解向量==。这是本题能做出来的前提。

        **第二步：造一个齐次解。**
        由"两个非齐次解之差是齐次解"，
        更一般地==系数和为 $0$ 的组合是齐次解==。
        手上有 $\eta_1$ 和 $\eta_2+\eta_3$，
        ==凑一个系数和为 $0$ 的组合==：
        $$(\eta_2+\eta_3)-2\eta_1\quad\text{系数和}=1+1-2=0\ \checkmark$$
        所以它是 $Ax=0$ 的解。

        **第三步：特解现成的**，$\eta_1$ 本身就是。

        ==关键就是"系数和为 $0$ 造齐次解、系数和为 $1$ 造特解"这一对判据。==
      `,
      solution: String.raw`
        **基础解系的个数**：$n-\rank(A)=4-3=1$，故基础解系由一个非零向量构成。

        **造齐次解**：由 $A\eta_i=b$（$i=1,2,3$），
        $$A\bigl[(\eta_2+\eta_3)-2\eta_1\bigr]=b+b-2b=0,$$
        故
        $$\xi=(\eta_2+\eta_3)-2\eta_1
        =\begin{pmatrix}0\\1\\2\\3\end{pmatrix}-2\begin{pmatrix}1\\2\\3\\4\end{pmatrix}
        =\begin{pmatrix}-2\\-3\\-4\\-5\end{pmatrix}$$
        是 $Ax=0$ 的解。由 $\xi\ne0$ 且基础解系只需一个向量，
        ==$\xi$ 就是一个基础解系==。

        **特解**：$\eta_1$ 是 $Ax=b$ 的解。

        **通解**：
        $$x=\begin{pmatrix}1\\2\\3\\4\end{pmatrix}
        +k\begin{pmatrix}-2\\-3\\-4\\-5\end{pmatrix},\qquad k\ \text{为任意常数}.$$

        （也可以把 $\xi$ 取成 $(2,3,4,5)\T$，==差一个非零倍数不影响通解==。）
      `,
      comment: String.raw`
        **这类题的三个固定动作**：

        1. ==先算 $n-\rank(A)$==，确定需要几个齐次解；
        2. ==用系数和为 $0$ 的组合造齐次解==，个数够了就停；
        3. ==随便挑一个已知解作特解==。

        **必须验证的一件事**：造出来的齐次解要==线性无关且个数恰好是 $n-r$==。
        本题只需一个，验证 $\xi\ne0$ 即可；
        ==若 $n-r=2$，就要造两个并说明它们无关==，这是难度陡增的地方。

        **系数和判据的完整版**（值得单独记）：设 $\eta_i$ 都是 $Ax=b$ 的解，则
        $$A\left(\sum c_i\eta_i\right)=\left(\sum c_i\right)b,$$
        于是
        $$\sum c_i=1\Rightarrow\text{是 }Ax=b\text{ 的解};\qquad
        \sum c_i=0\Rightarrow\text{是 }Ax=0\text{ 的解}.$$
        ==一个等式统管两种情形==，比分开记省事。

        **常见变体**：给 $\eta_1,\eta_2$ 两个解、$\rank(A)=n-2$，
        问"能否求出通解"——==不能==，因为只造得出一个齐次解，
        而基础解系需要两个。
        ==这种"条件不够"的选项经常出现在选择题里。==
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '二、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **解方程组用了列变换**：==只能用行变换==，列变换会打乱未知数顺序。
      2. **忘了比较 $\rank(A)$ 与 $\rank(\bar A)$**：这是判有无解的唯一依据。
      3. **$n$ 取成方程个数**：==$n$ 是未知数个数==，不是行数。
      4. **基础解系个数写成 $\rank(A)$**：应当是 ==$n-\rank(A)$==。
      5. **通解漏写任意常数**：必须写 $k_i$ 并注明任意。
      6. **把非齐次解集当线性空间**：==它是仿射的==，不含零向量。
      7. **系数和判据记反**：==和为 $1$ 是非齐次解，和为 $0$ 是齐次解==。
      8. **造出的齐次解个数不够就写通解**：
         ==必须凑够 $n-r$ 个且线性无关==。
      9. **不验算**：特解和基础解系各代一次，==成本极低==。
      10. **齐次方程组说"无解"**：==它永远有零解==。
    ` },

  ],
});
