/* ==========================================================================
   高等数学 / 7 常微分方程 / 欧拉方程与可降阶方程
   —— 两类"能化归"的高阶方程。常系数见 ode/linear-const。
   ========================================================================== */

KM.page({
  path: 'calculus/ode/euler',
  title: '欧拉方程与可降阶方程',
  subtitle: '两类都靠**换元化归**：欧拉方程换掉自变量变成常系数，可降阶方程换掉未知函数降一阶',
  tags: ['小题', '计算题'],
  updated: '2026-08-21',

  blocks: [

    { t: 'h', id: 'reducible-sec', c: '一、可降阶的高阶方程' },

    { t: 'compare',
      id: 'three-types',
      title: '★ 三种可降阶型：看方程里"缺"了谁',
      cols: ['类型', '特征', '换元', '降成'],
      rows: [
        ['$y^{(n)}=f(x)$', '==只有最高阶导和 $x$==', '无需换元', '连续积分 $n$ 次'],
        ['$y\'\'=f(x,y\')$', '==缺 $y$==', '$p=y\'$，$y\'\'=p\'$', '关于 $p(x)$ 的一阶方程'],
        ['$y\'\'=f(y,y\')$', '==缺 $x$==', '$p=y\'$，==$y\'\'=p\\dfrac{\\mathrm{d}p}{\\mathrm{d}y}$==', '关于 $p(y)$ 的一阶方程'],
      ] },

    { t: 'key', id: 'missing-x', title: '★ 缺 $x$ 型：那个链式法则是关键', c: String.raw`
      $y''=f(y,y')$ 中==方程不显含 $x$==，令 $p=y'$，则
      $$\boxed{\ y''=\deriv px=\deriv py\cdot\deriv yx=p\deriv py\ }$$

      ==把 $p$ 看成 $y$ 的函数==（而不是 $x$ 的函数），这是全部技巧所在。
      代入后得到关于 $p(y)$ 的一阶方程，解出 $p=p(y)$ 后，
      ==再由 $\deriv yx=p(y)$ 分离变量求出 $y$==。

      **所以要积分两次、出现两个常数**：
      第一次解 $p$ 时出 $C_1$，第二次解 $y$ 时出 $C_2$。
      ==二阶方程的通解必须含两个独立常数==，
      **只有一个常数就说明漏了一步。**

      **对照"缺 $y$ 型"**：$y''=f(x,y')$ 中令 $p=y'$，
      ==直接 $y''=\deriv px$==（$p$ 仍是 $x$ 的函数），
      ==不需要那个链式法则==。

      $$\boxed{\ \text{缺 }y\Rightarrow y''=p';\qquad \text{缺 }x\Rightarrow y''=p\dfrac{\d p}{\dy}\ }$$
      ==把这两个搞混是本节头号错误==。
      **判据：解出的 $p$ 是谁的函数？缺 $y$ 时是 $x$ 的，缺 $x$ 时是 $y$ 的。**

      **两者都缺时（$y''=f(y')$）**：两条路都可以，==选好算的那条==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'euler-sec', c: '二、欧拉方程' },

    { t: 'key', id: 'euler-form', title: '欧拉方程：变系数化常系数', c: String.raw`
      $$x^{n}y^{(n)}+a_1x^{n-1}y^{(n-1)}+\cdots+a_{n-1}xy'+a_ny=f(x)$$
      ==特征：$x$ 的次数与求导的阶数**逐项相等**==（$x^{k}y^{(k)}$）。

      **换元** $x=e^{t}$（即 $t=\ln x$，设 $x>0$），则
      $$xy'=\deriv yt,\qquad x^{2}y''=\deriv{^{2}y}{t^{2}}-\deriv yt,$$
      $$x^{3}y'''=\deriv{^{3}y}{t^{3}}-3\deriv{^{2}y}{t^{2}}+2\deriv yt.$$

      **用算子记号更好记**：记 $D=\deriv{}{t}$，则
      $$\boxed{\ x^{k}y^{(k)}=D(D-1)(D-2)\cdots(D-k+1)\,y\ }$$
      ==是一个"下降阶乘"的形状==：
      $k=1$ 给 $D$；$k=2$ 给 $D(D-1)=D^{2}-D$；
      $k=3$ 给 $D(D-1)(D-2)=D^{3}-3D^{2}+2D$ $\checkmark$

      ==换元后就变成关于 $t$ 的[常系数线性方程](#/calculus/ode/linear-const?at=char-roots)==，
      按那一套解完再把 $t=\ln x$ 代回。

      **$x<0$ 时**取 $x=-e^{t}$，==结果形式相同==，
      通常把答案写成含 $\abs x$ 的形式。

      **一个更快的办法（试解法）**：齐次欧拉方程可以直接试 $y=x^{r}$，
      $$x^{k}\left(x^{r}\right)^{(k)}=r(r-1)\cdots(r-k+1)x^{r},$$
      ==代入后 $x^{r}$ 整体约掉，得到关于 $r$ 的代数方程==（就是特征方程）。

      | 特征根 | 对应解 |
      |---|---|
      | 单根 $r$ | $x^{r}$ |
      | $k$ 重根 $r$ | $x^{r},\ x^{r}\ln x,\ \dots,\ x^{r}\ln^{k-1}x$ |
      | 共轭复根 $\alpha\pm\beta i$ | $x^{\alpha}\cos(\beta\ln x)$，$x^{\alpha}\sin(\beta\ln x)$ |

      ==注意重根配的是 $\ln x$ 而不是 $x$==——
      **因为换元后 $t=\ln x$，常系数方程里重根配的 $t$ 就变成了 $\ln x$。**
      这个对应关系理解了就不用背。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '三、例题' },

    { t: 'example',
      id: 'ex-missing-x',
      title: '★ 缺 $x$ 型：$y\'\'=p\\frac{\\mathrm{d}p}{\\mathrm{d}y}$',
      source: '标准例题（高频）',
      level: 3,
      problem: String.raw`
        求解 $yy''+\left(y'\right)^{2}=0$，满足 $y(0)=1$、$y'(0)=2$。
      `,
      idea: String.raw`
        **方程里没有 $x$**（只有 $y,y',y''$）$\Rightarrow$ ==缺 $x$ 型==。

        令 $p=y'$，==把 $p$ 看成 $y$ 的函数==，则
        $$y''=p\deriv py.$$
        代入：
        $$yp\deriv py+p^{2}=0\ \Longrightarrow\ p\left(y\deriv py+p\right)=0.$$

        **分两种情况**：
        - $p=0$：即 $y'=0$，$y$ 为常数。==由初值 $y'(0)=2\ne0$ 排除==；
        - $y\deriv py+p=0$：==可分离变量==，
          $$\frac{\d p}{p}=-\frac{\dy}{y}\ \Rightarrow\ \ln\abs p=-\ln\abs y+C_1'
          \ \Rightarrow\ p=\frac{C_1}{y}.$$

        **用初值定 $C_1$**：$y=1$ 时 $p=y'=2$，==故 $C_1=2$==。
        ==在这里就代初值，比留到最后省事。==

        **第二次积分**：$\deriv yx=\frac2y$，分离得 $y\dy=2\dx$，
        $$\frac{y^{2}}{2}=2x+C_2.$$
        再用 $y(0)=1$ 定 $C_2$。

        **一个更快的看法**：原方程左端恰好是 $(yy')'$：
        $$(yy')'=y'y'+yy''=yy''+(y')^{2}\ \checkmark$$
        ==所以方程就是 $(yy')'=0$，即 $yy'=C_1$==，一步到位。
        **看出"恰好是某个式子的导数"能省掉整个换元过程。**
      `,
      solution: String.raw`
        **解法一（凑全导数，最快）**：注意到
        $$\deriv{}{x}\left(yy'\right)=\left(y'\right)^{2}+yy'',$$
        故原方程即
        $$\deriv{}{x}\left(yy'\right)=0\ \Longrightarrow\ yy'=C_1.$$

        代入初值 $y(0)=1$、$y'(0)=2$：$C_1=1\times2=2$，故
        $$yy'=2\ \Longrightarrow\ y\dy=2\dx.$$

        积分：
        $$\frac{y^{2}}{2}=2x+C_2.$$
        代入 $x=0,y=1$：$\dfrac12=C_2$。故
        $$\frac{y^{2}}{2}=2x+\frac12\ \Longrightarrow\ y^{2}=4x+1,$$
        $$y=\sqrt{4x+1}\qquad(\text{由 }y(0)=1>0\ \text{取正根}).$$

        **解法二（标准换元）**：令 $p=y'$，视 $p$ 为 $y$ 的函数，$y''=p\deriv py$，
        $$yp\deriv py+p^{2}=0.$$
        由 $y'(0)=2\ne0$ 知 $p\not\equiv0$，约去 $p$：
        $$y\deriv py=-p\ \Longrightarrow\ \frac{\d p}{p}=-\frac{\dy}{y}
        \ \Longrightarrow\ p=\frac{C_1}{y}.$$
        由 $y=1$ 时 $p=2$ 得 $C_1=2$，即 $\deriv yx=\dfrac2y$，
        以下同解法一。

        **验证**：$y=\sqrt{4x+1}$，$y'=\dfrac{2}{\sqrt{4x+1}}$，
        $y''=-\dfrac{4}{(4x+1)^{3/2}}$，
        $$yy''+\left(y'\right)^{2}
        =\sqrt{4x+1}\cdot\left(-\frac{4}{(4x+1)^{3/2}}\right)+\frac{4}{4x+1}
        =-\frac{4}{4x+1}+\frac{4}{4x+1}=0\ \checkmark$$
        且 $y(0)=1$、$y'(0)=2$ $\checkmark$
      `,
      comment: String.raw`
        **解法一的"凑全导数"值得专门练**。
        本章有一批方程的左端恰好是某个乘积的导数：

        | 方程左端 | 等于 |
        |---|---|
        | $yy''+(y')^{2}$ | $(yy')'$ |
        | $y''y'$ | $\frac12\left[(y')^{2}\right]'$ |
        | $xy''+y'$ | $(xy')'$ |
        | $y''+2xy'+2y$…… | 看情况 |

        ==第三行很常见==：$xy''+y'=(xy')'$，
        所以 $xy''+y'=f(x)$ 可以直接积分一次。

        **两次积分、两个常数**：本题 $C_1$ 由 $y'(0)$ 定、$C_2$ 由 $y(0)$ 定。
        ==初值问题中"边解边代初值"比"求出通解再代"省很多力气==，
        **尤其是像本题这样第一次积分后就能定出 $C_1$ 的情形。**

        **约去 $p$ 时的讨论不能省**：$p=0$ 对应 $y\equiv$ 常数，
        ==它确实是原方程的解==（代入验证：$0+0=0$），
        只是不满足本题的初值。
        ==若题目只问通解，就必须把 $y=C$ 也写出来==
        （它对应 $C_1=0$，==其实已包含在 $yy'=C_1$ 里==）。

        **取正根的理由要写**：$y^{2}=4x+1$ 给出 $y=\pm\sqrt{4x+1}$，
        ==由 $y(0)=1>0$ 及解的连续性，只能取正根==。
        漏掉这句话会扣分。
      `,
    },

    { t: 'example',
      id: 'ex-euler',
      title: '欧拉方程：两种解法',
      source: '标准例题',
      level: 3,
      problem: String.raw`
        求解 $x^{2}y''-3xy'+4y=0$（$x>0$）。
      `,
      idea: String.raw`
        **认出是欧拉方程**：$x^{2}y''$、$xy'$、$y$——
        ==$x$ 的次数与导数阶数逐项相等== $\checkmark$

        **解法一（试解 $y=x^{r}$，最快）**：
        $$y=x^{r},\quad y'=rx^{r-1},\quad y''=r(r-1)x^{r-2},$$
        代入：
        $$r(r-1)x^{r}-3rx^{r}+4x^{r}=0,$$
        ==$x^{r}$ 整体约掉==（$x>0$ 时非零），得特征方程
        $$r^{2}-r-3r+4=r^{2}-4r+4=(r-2)^{2}=0.$$
        ==$r=2$ 是二重根==。

        **重根配 $\ln x$**（不是配 $x$）：
        $$y=C_1x^{2}+C_2x^{2}\ln x.$$

        **解法二（换元 $x=e^{t}$）**：用算子公式
        $$x y'=Dy,\qquad x^{2}y''=D(D-1)y,$$
        代入得
        $$D(D-1)y-3Dy+4y=0\ \Longrightarrow\ \left(D^{2}-4D+4\right)y=0,$$
        ==这是关于 $t$ 的常系数方程==，特征方程 $\lambda^{2}-4\lambda+4=0$，
        $\lambda=2$ 二重根，故
        $$y=\left(C_1+C_2t\right)e^{2t}\ \xrightarrow{\ t=\ln x\ }\
        \left(C_1+C_2\ln x\right)x^{2}.$$
        ==两法结果相同==。

        =="$t$ 换回 $\ln x$"正好解释了为什么重根配的是 $\ln x$。==
      `,
      solution: String.raw`
        这是二阶欧拉方程（$x$ 的次数与求导阶数逐项相等）。

        **试解 $y=x^{r}$**（$x>0$）：
        $$y'=rx^{r-1},\qquad y''=r(r-1)x^{r-2},$$
        代入原方程：
        $$x^{2}\cdot r(r-1)x^{r-2}-3x\cdot rx^{r-1}+4x^{r}=0,$$
        $$\left[r(r-1)-3r+4\right]x^{r}=0.$$

        由 $x^{r}\ne0$ 得特征方程
        $$r^{2}-4r+4=0\ \Longrightarrow\ (r-2)^{2}=0,$$
        即 $r=2$ 为==二重根==。

        由重根对应 $x^{r},\ x^{r}\ln x$，通解为
        $$y=C_1x^{2}+C_2x^{2}\ln x=x^{2}\left(C_1+C_2\ln x\right).$$

        **验证**（取 $y=x^{2}\ln x$）：
        $$y'=2x\ln x+x,\qquad y''=2\ln x+2+1=2\ln x+3,$$
        $$x^{2}y''-3xy'+4y=x^{2}(2\ln x+3)-3x(2x\ln x+x)+4x^{2}\ln x$$
        $$=2x^{2}\ln x+3x^{2}-6x^{2}\ln x-3x^{2}+4x^{2}\ln x=0\ \checkmark$$
      `,
      comment: String.raw`
        **两种解法的分工**：

        | | 试解法 | 换元法 |
        |---|---|---|
        | 齐次方程 | ==快，推荐== | 也行 |
        | ==非齐次方程== | ==用不了== | ==必须用== |

        ==试解 $y=x^{r}$ 只对齐次方程有效==。
        遇到 $x^{2}y''-3xy'+4y=x^{3}$ 这类，
        ==必须换元 $x=e^{t}$ 化成常系数，再用[待定系数法](#/calculus/ode/linear-const?at=undetermined)求特解==。
        ==注意右端 $x^{3}$ 换元后变成 $e^{3t}$==，正好是标准的指数型。

        **特征根三种情形的对照记忆**：

        | 常系数（自变量 $t$） | 欧拉（自变量 $x$） |
        |---|---|
        | $e^{\lambda t}$ | ==$x^{\lambda}$== |
        | $te^{\lambda t}$（重根） | ==$x^{\lambda}\ln x$== |
        | $e^{\alpha t}\cos\beta t$ | $x^{\alpha}\cos(\beta\ln x)$ |

        ==只需把 $t$ 换成 $\ln x$、把 $e^{t}$ 换成 $x$==，
        **一张表就能从常系数的结论推出欧拉的结论。**

        **算子公式的自检**：
        $$x^{2}y''=D(D-1)y=D^{2}y-Dy,$$
        代入本题：$D^{2}-D-3D+4=D^{2}-4D+4$ $\checkmark$
        ==与试解法得到的 $r^{2}-4r+4$ 完全一致==——
        **这不是巧合：试解 $x^{r}$ 与试解 $e^{rt}$ 在换元下是同一件事。**

        **$x<0$ 的情形**：通解写成
        $$y=x^{2}\left(C_1+C_2\ln\abs x\right),$$
        ==加绝对值即可==。考研题通常会限定 $x>0$。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '四、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **缺 $x$ 与缺 $y$ 的换元搞混**：
         ==缺 $y$ 时 $y''=p'$，缺 $x$ 时 $y''=p\frac{\d p}{\dy}$==。
      2. **缺 $x$ 型把 $p$ 当成 $x$ 的函数**：==它是 $y$ 的函数==。
      3. **只积分一次就写通解**：==二阶方程要两个常数==。
      4. **约去 $p$ 不讨论 $p=0$**：可能丢解。
      5. **欧拉方程的重根配 $x$ 而不是 $\ln x$**：==是 $x^{r}\ln x$==。
      6. **对非齐次欧拉方程用试解法**：==试解只对齐次有效==，
         非齐次必须换元。
      7. **换元后忘了把 $t$ 换回 $\ln x$**。
      8. **不验证**：==代回原方程==，欧拉方程尤其容易在系数上出错。
      9. **初值问题留到最后才代**：==边解边代更省事==。
      10. **开方后不讨论符号**：由初值确定取正根还是负根。
    ` },

  ],
});
