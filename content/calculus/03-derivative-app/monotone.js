/* ==========================================================================
   高等数学 / 3 微分中值定理与导数应用 / 单调性 / 极值 / 最值
   —— 一阶导管单调与极值，二阶导管判别。
      凹凸见 derivative-app/convexity；不等式见 derivative-app/inequality。
   ========================================================================== */

KM.page({
  path: 'calculus/derivative-app/monotone',
  title: '单调性 / 极值 / 最值',
  subtitle: '**极值是局部的、最值是整体的**。两者的求法只差一步：最值还要比端点',
  tags: ['小题', '计算题', '高频'],
  updated: '2026-08-21',

  blocks: [

    { t: 'key', id: 'monotone', title: '单调性判别', c: String.raw`
      $f$ 在区间 $I$ 上可导，则
      $$f'(x)>0\ \text{在}\ I\ \text{上}\ \Longrightarrow\ f\ \text{在}\ I\ \text{上严格递增}$$

      **严格递增只需要 $f'\ge0$ 且 $f'$ 不在任何子区间上恒为零**——
      ==$f'$ 在个别点为零不影响严格单调==。
      例：$f=x^{3}$ 的 $f'(0)=0$，==但它仍严格递增==。

      $$\boxed{\ f\ \text{在}\ I\ \text{上单调不减}\iff f'\ge0\ \text{在}\ I\ \text{上}\ }$$
      ==这一条是充要的==（注意是"不减"不是"递增"）。

      **求单调区间的四步**：

      1. ==定定义域==（这一步最常被跳过）；
      2. 求 $f'$，找出==驻点==（$f'=0$）与==不可导点==；
      3. 用这些点把定义域分成若干区间；
      4. 在每个区间上判 $f'$ 的符号。

      **第 2 步的"不可导点"不能漏**：
      $f=\abs{x}$、$f=x^{2/3}$ 的单调性在 $x=0$ 处改变，
      ==而那里 $f'$ 不存在==，不是驻点。

      **写答案的格式**：单调区间要写成==区间==，
      ==不能写成"$x>0$ 时递增"这种不清晰的表述==；
      多个单调递增区间之间==用逗号隔开，不能用并集符号==
      （$(-\infty,0)$ 与 $(1,+\infty)$ 各自递增，但并起来不递增）。
    ` },

    { t: 'key', id: 'extremum-def', title: '极值：局部的概念', c: String.raw`
      $f(x_0)$ 是**极大值**，若存在 $x_0$ 的某邻域，使该邻域内恒有 $f(x)\le f(x_0)$。

      ==极值是"和邻居比"，最值是"和全体比"==。所以：

      | | 极值 | 最值 |
      |---|---|---|
      | 范围 | ==局部== | ==整体== |
      | 可以有几个 | 多个 | 各一个（值唯一） |
      | 能否在端点取 | ==不能==（要有邻域） | ==可以== |
      | 大小关系 | ==极大值可能小于极小值== | 最大值 $\ge$ 最小值 |

      ==第三行是最常考的区别==：
      端点处==没有双侧邻域，所以不是极值点==，但可以是最值点。

      ==第四行反直觉但确实成立==：
      $f$ 在 $[0,3]$ 上有极大值 $f(1)=1$、极小值 $f(2)=5$ 是可能的（不同的"山头"）。

      **费马定理（极值的必要条件）**：
      $$f\ \text{在}\ x_0\ \text{可导且取极值}\ \Longrightarrow\ f'(x_0)=0$$
      ==反过来不成立==：$f=x^{3}$ 在 $0$ 处 $f'=0$ 但无极值。

      $$\boxed{\ \text{极值点} \subset \set{\text{驻点}}\cup\set{\text{不可导点}}\ }$$
      ==这两类点统称"可疑点"，是搜索的全部范围==。
    ` },

    { t: 'compare',
      id: 'judge',
      title: '★ 极值的三个判别法',
      cols: ['判别法', '用什么', '适用', '结论'],
      rows: [
        ['**第一充分条件**', '$f\'$ 在 $x_0$ ==两侧变号==', '==最通用==，含不可导点', '正变负 $\\to$ 极大'],
        ['**第二充分条件**', '$f\'(x_0)=0$ 且 $f\'\'(x_0)\\ne0$', '$f$ 二阶可导', '$f\'\'<0\\to$ 极大'],
        ['**第 $n$ 阶条件**', '首个非零的 $f^{(n)}(x_0)$', '高阶可导', '$n$ ==偶==才是极值'],
      ] },

    { t: 'md', c: String.raw`
      **第二充分条件失效时（$f''(x_0)=0$）退回第一条**，
      或者用第 $n$ 阶条件：设 $f'(x_0)=\cdots=f^{(n-1)}(x_0)=0$、$f^{(n)}(x_0)\ne0$，则

      - ==$n$ 为偶数==：是极值（$f^{(n)}>0$ 极小，$<0$ 极大）；
      - ==$n$ 为奇数==：==不是极值==（是[拐点](#/calculus/derivative-app/convexity?at=inflection)）。

      $$f=x^{3}:\ n=3\ \text{（奇）}\ \Rightarrow\ \text{非极值};\qquad
      f=x^{4}:\ n=4\ \text{（偶）}\ \Rightarrow\ \text{极小}.$$

      ==这条规律和[泰勒展开](#/threads/lines/taylor?at=read-coefficient)一致==：
      $f(x)-f(x_0)\approx\frac{f^{(n)}(x_0)}{n!}(x-x_0)^{n}$，
      ==偶次幂两侧同号（有极值），奇次幂两侧异号（穿过去）==。
    ` },

    { t: 'method', id: 'maxmin', title: '求最值：闭区间与开区间', c: String.raw`
      **闭区间 $[a,b]$ 上连续函数求最值（三步）**：

      1. 求出所有==驻点与不可导点==；
      2. 算出这些点处的函数值，==以及两个端点 $f(a),f(b)$==；
      3. ==比大小==，最大的就是最大值。

      ==不需要判断哪个是极大哪个是极小==——直接比数值最快。
      **这是闭区间最值题的标准做法，"求极值再比较"是绕远路。**

      **开区间或无穷区间上求最值**：==没有端点可比，要看趋势==。

      1. 求驻点、不可导点；
      2. ==求区间端点处的极限==（可能是 $\pm\infty$）；
      3. 若极限值都小于某个内部的极大值，==则该极大值是最大值==；
         ==若极限是 $+\infty$，则无最大值==。

      **一个常用的简化**：
      $$\boxed{\ \text{区间内只有**一个**极值点}\ \Rightarrow\ \text{它就是最值点}\ }$$
      ==极大值即最大值、极小值即最小值==。
      **应用题（求最省材料、最大利润）几乎都属于这种情形**，
      ==算出唯一驻点后可以直接下结论，不必再讨论==，
      ==但要写一句"由实际意义知最值存在且在内部取到"==。

      **实际应用题的完整流程**：
      建立目标函数 $\to$ 写出定义域 $\to$ 求导找驻点 $\to$
      ==说明这就是最值==（唯一驻点 + 实际意义）$\to$ 回答问题。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '一、例题' },

    { t: 'example',
      id: 'ex-monotone',
      title: '含不可导点的单调性与极值',
      source: '标准例题',
      level: 3,
      problem: String.raw`
        求 $f(x)=x^{2/3}(x-5)$ 的单调区间与极值。
      `,
      idea: String.raw`
        **定义域是 $\R$**（$x^{2/3}=\sqrt[3]{x^{2}}$ 对负数也有定义）。

        **先化简再求导**：
        $$f(x)=x^{5/3}-5x^{2/3},$$
        $$f'(x)=\frac53x^{2/3}-\frac{10}{3}x^{-1/3}
        =\frac53x^{-1/3}\left(x-2\right).$$

        ==提出 $x^{-1/3}$ 是关键的一步==，它让符号分析变得清楚：
        $$f'(x)=\frac{5(x-2)}{3\sqrt[3]{x}}.$$

        **可疑点有两个**：
        - $x=2$：驻点（$f'=0$）；
        - ==$x=0$：不可导点==（$f'$ 的分母为零）。

        ==$x=0$ 极容易漏==——它不是驻点，但单调性在那里会变。

        **符号分析**（分子 $x-2$、分母 $\sqrt[3]x$）：

        | 区间 | $x-2$ | $\sqrt[3]x$ | $f'$ |
        |---|---|---|---|
        | $(-\infty,0)$ | $-$ | $-$ | ==$+$== |
        | $(0,2)$ | $-$ | $+$ | $-$ |
        | $(2,+\infty)$ | $+$ | $+$ | $+$ |

        ==所以 $x=0$ 是极大值点、$x=2$ 是极小值点==。
      `,
      solution: String.raw`
        定义域为 $(-\infty,+\infty)$。将 $f$ 写成
        $$f(x)=x^{5/3}-5x^{2/3},$$
        求导（$x\ne0$）：
        $$f'(x)=\frac53x^{2/3}-\frac{10}{3}x^{-1/3}
        =\frac53x^{-1/3}\left(x-2\right)=\frac{5(x-2)}{3\sqrt[3]{x}}.$$

        **可疑点**：$x=2$（驻点），$x=0$（$f'$ 不存在，但 $f$ 连续）。

        **符号讨论**：

        - $x<0$：$x-2<0$，$\sqrt[3]x<0$，故 $f'>0$，==递增==；
        - $0<x<2$：$x-2<0$，$\sqrt[3]x>0$，故 $f'<0$，==递减==；
        - $x>2$：$f'>0$，==递增==。

        **单调区间**：递增区间 $(-\infty,0)$ 与 $(2,+\infty)$；
        递减区间 $(0,2)$。

        **极值**（第一充分条件）：

        - $x=0$：$f'$ 由正变负，==取极大值== $f(0)=0$；
        - $x=2$：$f'$ 由负变正，==取极小值==
          $$f(2)=2^{2/3}(2-5)=-3\sqrt[3]{4}\approx-4.762.$$
      `,
      comment: String.raw`
        **$x=0$ 是本题的题眼**。它是不可导点而非驻点，
        ==只用"令 $f'=0$"去找极值点会完全漏掉它==。

        $$\boxed{\ \text{可疑点}=\text{驻点}\cup\text{不可导点}\ }$$
        ==含 $\abs\cdot$、分数次幂、根号的函数一定要查不可导点==。

        **注意 $f$ 在 $x=0$ 处连续**（$f(0)=0$），
        ==只是不可导（切线竖直）==：
        $$\lim_{x\to0}f'(x)=\lim_{x\to0}\frac{5(x-2)}{3\sqrt[3]x}=\infty.$$
        ==所以 $(0,0)$ 是一个"尖点"，图像在那里有竖直切线==。

        **不能用第二充分条件**：$f''$ 在 $x=0$ 处不存在，
        ==只能用第一充分条件（导数变号）==。
        这也说明[第一条最通用](#/calculus/derivative-app/monotone?at=judge)。

        **一个有趣的对照**：极大值 $f(0)=0$ 竟然==大于==极小值 $f(2)\approx-4.76$，
        这符合直觉；==但如果曲线有多个"山头"，极大值小于极小值是可能的==，
        见[极值与最值的对比表](#/calculus/derivative-app/monotone?at=extremum-def)。

        **有没有最值**：$x\to+\infty$ 时 $f\to+\infty$、$x\to-\infty$ 时 $f\to-\infty$，
        ==所以在 $\R$ 上既无最大值也无最小值==。
        极值和最值在这里彻底分开了。
      `,
    },

    { t: 'example',
      id: 'ex-application',
      title: '★ 应用题：唯一驻点即最值',
      source: '标准例题（高频）',
      level: 3,
      problem: String.raw`
        要做一个容积为 $V$ 的无盖圆柱形容器，
        问底面半径 $r$ 与高 $h$ 取何值时用料最省？
      `,
      idea: String.raw`
        **建立目标函数**：用料 $=$ 表面积 $=$ 底面 $+$ 侧面
        $$S=\pi r^{2}+2\pi rh.$$
        ==无盖，所以只有一个底面==。

        **消元**：由约束 $V=\pi r^{2}h$ 解出 $h=\dfrac{V}{\pi r^{2}}$，代入：
        $$S(r)=\pi r^{2}+2\pi r\cdot\frac{V}{\pi r^{2}}=\pi r^{2}+\frac{2V}{r}.$$

        **定义域**：$r>0$（==这一步要写==，它是开区间，没有端点可比）。

        **求导找驻点**：
        $$S'(r)=2\pi r-\frac{2V}{r^{2}}=0\ \Longrightarrow\ r^{3}=\frac{V}{\pi}.$$

        **说明是最小值**：$r\to0^{+}$ 与 $r\to+\infty$ 时 $S\to+\infty$，
        ==而区间内只有一个驻点==，
        由[唯一驻点原则](#/calculus/derivative-app/monotone?at=maxmin)，==它就是最小值点==。

        ==这句话必须写==，否则只算出驻点是不完整的。

        **预判结果**：这类题的答案通常很对称，
        ==猜 $h=r$（高等于半径）==。算完验证。
      `,
      solution: String.raw`
        设底面半径为 $r$、高为 $h$，则约束条件为
        $$V=\pi r^{2}h\ \Longrightarrow\ h=\frac{V}{\pi r^{2}}.$$

        无盖容器的表面积（用料）为
        $$S=\pi r^{2}+2\pi rh=\pi r^{2}+\frac{2V}{r},\qquad r\in(0,+\infty).$$

        **求导**：
        $$S'(r)=2\pi r-\frac{2V}{r^{2}}=\frac{2\left(\pi r^{3}-V\right)}{r^{2}}.$$

        令 $S'(r)=0$ 得唯一驻点
        $$r_0=\sqrt[3]{\frac{V}{\pi}}.$$

        **判断最值**：
        当 $0<r<r_0$ 时 $\pi r^{3}<V$，故 $S'<0$，$S$ 递减；
        当 $r>r_0$ 时 $S'>0$，$S$ 递增。
        故 $S$ 在 $r_0$ 处取==唯一的极小值，也是最小值==。

        **相应的高**：
        $$h=\frac{V}{\pi r_0^{2}}=\frac{\pi r_0^{3}}{\pi r_0^{2}}=r_0$$
        （用了 $V=\pi r_0^{3}$）。

        **结论**：当
        $$r=h=\sqrt[3]{\frac{V}{\pi}}$$
        时用料最省，此时 $S_{\min}=\pi r_0^{2}+2\pi r_0^{2}=3\pi\left(\dfrac{V}{\pi}\right)^{2/3}$。
      `,
      comment: String.raw`
        **结论 $h=r$ 很漂亮**：==无盖圆柱最省料时，高等于底面半径==。
        （==有盖==的情形答案是 $h=2r$，即"高等于直径"，
        因为多了一个底面，会把最优解推向"更矮更胖"。
        ==这两个结论都值得记，考试两种都考过。==）

        **数值验证**（取 $V=\pi$，则 $r_0=1$，$h=1$，$S=3\pi\approx9.4248$）：

        | $r$ | $S=\pi r^{2}+\frac{2\pi}{r}$ |
        |---|---|
        | $0.8$ | $9.862$ |
        | $0.9$ | $9.526$ |
        | ==$1.0$== | ==$9.4248$== |
        | $1.1$ | $9.514$ |
        | $1.2$ | $9.760$ |

        ==$r=1$ 处确实取到最小== $\checkmark$

        **应用题的四个得分点**（缺一扣分）：

        1. ==设变量并写出约束==；
        2. ==写出目标函数及其定义域==；
        3. 求导、解驻点；
        4. ==说明该驻点给出最值==（唯一驻点 + 单调性，或实际意义）。

        ==第 4 步最常被省略==。写法可以很短：
        "由 $S'$ 在 $r_0$ 两侧变号且 $r_0$ 是唯一驻点知 $S(r_0)$ 为最小值。"

        **消元这一步的选择**：本题消去 $h$ 保留 $r$。
        ==若消去 $r$ 保留 $h$，会得到 $S=\frac{V}{h}+2\sqrt{\pi Vh}$，含根号，更难求导==。
        ==消元时选"能让目标函数最简单"的那个变量==，
        这与[条件极值](#/calculus/multi-derivative/extremum?at=why-elimination-hard)那里的考量一致。
        实在不好消就用[拉格朗日乘数法](#/calculus/multi-derivative/extremum?at=lagrange-steps)。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '二、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **漏掉不可导点**：==可疑点 $=$ 驻点 $\cup$ 不可导点==。
      2. **不先定定义域**：$\ln$、根号、分母会限制范围。
      3. **把驻点直接当极值点**：==$f=x^{3}$ 是反例==，要验变号。
      4. **单调区间用并集连接**：==要写成两个区间，用逗号隔开==。
      5. **端点当极值点**：==端点不是极值点==（无双侧邻域），但可以是最值点。
      6. **闭区间求最值忘了比端点**：==三步法的第 2 步==。
      7. **应用题不说明"是最值"**：==唯一驻点 + 单调性/实际意义==，必须写一句。
      8. **$f''(x_0)=0$ 就断言无极值**：==要退回第一充分条件或看更高阶导数==。
      9. **认为极大值一定大于极小值**：==不同"山头"时可能反过来==。
      10. **消元选错变量**：选能让目标函数最简单的那个。
    ` },

  ],
});
