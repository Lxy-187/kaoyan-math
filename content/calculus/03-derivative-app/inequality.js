/* ==========================================================================
   高等数学 / 3 微分中值定理与导数应用 / 不等式的证明
   —— 五条路线。总纲见 derivative-app/proof-overview；
      积分不等式见 definite/integral-proof。
   ========================================================================== */

KM.page({
  path: 'calculus/derivative-app/inequality',
  title: '不等式的证明',
  subtitle: '把 $A>B$ 改写成 $F=A-B>0$，然后**用导数研究 $F$**。全部技巧都在"怎么构造 $F$"和"从哪个端点出发"',
  tags: ['大题', '证明题', '高频'],
  updated: '2026-08-21',

  blocks: [

    { t: 'compare',
      id: 'route-table',
      title: '★ 五条路线：按条件与结论的形状选',
      cols: ['特征', '路线', '关键'],
      rows: [
        ['一般的 $f(x)>g(x)$', '==[单调性](#/calculus/derivative-app/inequality?at=monotone-route)==', '造 $F=f-g$，证 $F$ 单调 + 端点值'],
        ['含"和的函数 vs 函数的和"', '==[凹凸性](#/calculus/derivative-app/inequality?at=convexity-route)==', '琴生 / 切线在下方'],
        ['条件给 $f\'$ 或 $f\'\'$ 的信息', '==[中值定理](#/calculus/derivative-app/proof-overview?at=mvt-group)==', '拉格朗日 / 泰勒'],
        ['两端形状差很远，含高阶导', '==[泰勒展开](#/calculus/derivative-app/taylor-proof?at=where)==', '在合适的点展开'],
        ['求"最好的常数"', '==最值==', '化成求 $F$ 的最值'],
      ] },

    { t: 'key', id: 'monotone-route', title: '路线一：单调性（主力）', c: String.raw`
      **要证 $f(x)>g(x)$ 在 $(a,b)$ 上成立**：

      1. 令 $F(x)=f(x)-g(x)$；
      2. 算 $F'$，==判断符号==；
      3. 算一个==端点的值==（通常是 $F(a)$ 或 $\lim\limits_{x\to a^{+}}F$）；
      4. 由"端点值 $\ge0$ $+$ 递增"得结论。

      $$\boxed{\ \text{端点值}\ +\ \text{单调方向}\ =\ \text{结论}\ }$$

      **从哪个端点出发要选对**：
      ==要证 $F>0$ 且 $F$ 递增，就从**左端点**出发（$F(a)\ge0$）==；
      ==$F$ 递减则从**右端点**出发==。
      **选反了什么也证不出来。**

      **$F'$ 符号不好判时，再求一次导**：
      $$F''>0\ \text{且}\ F'(a)\ge0\ \Longrightarrow\ F'>0\ \Longrightarrow\ F\ \text{递增},$$
      ==这叫"两层单调"，是本节最常用的加深==。
      有时要求到三阶。

      **每加深一层，就要多验一个端点值**：
      $$F(a)\ge0,\quad F'(a)\ge0,\quad F''(a)\ge0,\ \dots$$
      ==漏验中间层的端点值是最常见的逻辑漏洞==。

      **技巧：先化简再构造。**
      要证 $\frac{\ln x}{x-1}<\frac{1}{\sqrt x}$（$x>1$），
      ==直接作差会很难求导==；
      ==先去分母==化成 $\sqrt x\ln x<x-1$ 再构造 $F$，==导数干净得多==。
      $$\boxed{\ \text{能去分母、能取对数就先做，再构造 }F\ }$$
    ` },

    { t: 'key', id: 'convexity-route', title: '路线二：凹凸性', c: String.raw`
      **两个可用的形式**：

      **① 切线在下方**（$f$ 凹，即 $f''\ge0$）：
      $$f(x)\ \ge\ f(x_0)+f'(x_0)(x-x_0)$$
      ==用来证"函数 $\ge$ 一次式"型的不等式==。

      典型：证 $e^{x}\ge1+x$——
      $f=e^{x}$ 凹，在 $x_0=0$ 处的切线是 $y=1+x$，==直接得证==。

      **② [琴生不等式](#/calculus/derivative-app/convexity?at=jensen)**：
      $$f\left(\frac{\sum x_i}{n}\right)\le\frac{\sum f(x_i)}{n}\qquad(f\ \text{凹})$$
      ==用来证含多个变量的对称不等式==。

      **识别信号**：

      | 看到 | 想到 |
      |---|---|
      | $f(x)\ge$ 一次式，且在某点相切 | ==切线在下方== |
      | $\frac{f(a)+f(b)}{2}$ 与 $f\left(\frac{a+b}{2}\right)$ 比 | 琴生 |
      | $\sqrt{ab}$、$\frac{a+b}{2}$、$\frac{2}{\frac1a+\frac1b}$ | ==均值不等式==（取 $f=-\ln$） |

      ==凹凸路线的优势是"一步到位"==，
      不像单调路线要反复求导。==代价是要认出结构。==
    ` },

    { t: 'key', id: 'mvt-route', title: '路线三：中值定理（条件给导数时）', c: String.raw`
      **识别信号**：条件里出现 ==$f'$ 有界==、==$f'$ 单调==、
      ==$f(a)=f(b)$== 之类，而结论只含 $f$。

      **拉格朗日的标准用法**：
      $$f(b)-f(a)=f'(\xi)(b-a),$$
      ==把"函数值之差"换成"导数值 × 区间长"==，
      再用 $f'$ 的界去估计。

      **例**：$f'$ 满足 $\abs{f'}\le M$，证 $\abs{f(b)-f(a)}\le M(b-a)$——
      ==一行==（这就是[利普希茨条件](#/calculus/derivative-app/derivative-bound?at=lipschitz)）。

      **柯西中值定理**用于"两个函数之差的比"：
      $$\frac{f(b)-f(a)}{g(b)-g(a)}=\frac{f'(\xi)}{g'(\xi)}.$$

      **泰勒公式**用于条件给 $f''$ 时：
      $$f(x)=f(x_0)+f'(x_0)(x-x_0)+\frac{f''(\xi)}{2}(x-x_0)^{2},$$
      ==用 $f''$ 的符号或界来放缩最后一项==。

      **展开点怎么选**：见[泰勒证明题](#/calculus/derivative-app/taylor-proof?at=where)。
      ==常见的选择是"区间中点"（利用对称）或"已知信息最多的点"==。
    ` },

    { t: 'method', id: 'best-constant', title: '路线五：求最好的常数', c: String.raw`
      **题型**："求最大的 $k$，使 $f(x)\ge kg(x)$ 在 $I$ 上恒成立"。

      **转化**：
      $$f(x)\ge kg(x)\ \forall x\in I
      \iff k\le\min_{x\in I}\frac{f(x)}{g(x)}\qquad(g>0)$$
      ==所以最大的 $k$ 就是 $\frac fg$ 的最小值==。

      $$\boxed{\ \text{"恒成立求参数"} \Longrightarrow \text{"分离参数求最值"}\ }$$

      **三步**：

      1. ==分离参数==：把 $k$ 单独放在一边；
      2. 求另一边的最值（用[单调性/极值](#/calculus/derivative-app/monotone?at=maxmin)）；
      3. ==注意端点与开闭==：最值取不到时 $k$ 的范围是开的。

      **注意 $g$ 的符号**：$g<0$ 时除过去==不等号要反向==，
      ==$g$ 变号时必须分区间讨论==。
      **这是分离参数法唯一的坑。**

      **另一条路（不分离）**：令 $F(x)=f(x)-kg(x)$，
      ==要求 $\min F\ge0$==，对 $k$ 讨论。
      ==当 $g$ 会变号、或分离后的式子难求最值时用这条==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '一、例题' },

    { t: 'example',
      id: 'ex-monotone-proof',
      title: '★ 两层单调：每层都要验端点',
      source: '经典例题（高频）',
      level: 3,
      problem: String.raw`
        证明：当 $x>0$ 时，
        $$\ln(1+x)>\frac{x}{1+\frac x2}=\frac{2x}{2+x}.$$
      `,
      idea: String.raw`
        **构造** $F(x)=\ln(1+x)-\dfrac{2x}{2+x}$，==要证 $x>0$ 时 $F>0$==。

        **端点**：$F(0)=\ln1-0=0$ $\checkmark$
        ==所以只需证 $F$ 在 $(0,+\infty)$ 上递增==。

        **求导**：
        $$F'(x)=\frac{1}{1+x}-\frac{2(2+x)-2x}{(2+x)^{2}}
        =\frac{1}{1+x}-\frac{4}{(2+x)^{2}}.$$

        **通分判号**：
        $$F'=\frac{(2+x)^{2}-4(1+x)}{(1+x)(2+x)^{2}}
        =\frac{4+4x+x^{2}-4-4x}{(1+x)(2+x)^{2}}
        =\frac{x^{2}}{(1+x)(2+x)^{2}}.$$

        ==分子分母在 $x>0$ 时都为正，故 $F'>0$==！
        **这道题只需一层，第二次求导都不用。**

        ==关键的化简是"分子恰好化成 $x^{2}$"==——
        这类题的分子往往能整体化简成一个完全平方或单项式，
        ==算出来乱七八糟就要检查代数==。

        **结论**：$F(0)=0$ 且 $F$ 在 $(0,+\infty)$ 递增 $\Rightarrow$ $F(x)>0$。
      `,
      solution: String.raw`
        令
        $$F(x)=\ln(1+x)-\frac{2x}{2+x},\qquad x\ge0.$$

        **端点值**：$F(0)=\ln1-0=0$。

        **求导**：
        $$F'(x)=\frac{1}{1+x}-\frac{2(2+x)-2x\cdot1}{(2+x)^{2}}
        =\frac{1}{1+x}-\frac{4}{(2+x)^{2}}.$$

        通分：
        $$F'(x)=\frac{(2+x)^{2}-4(1+x)}{(1+x)(2+x)^{2}}
        =\frac{x^{2}}{(1+x)(2+x)^{2}}.$$

        当 $x>0$ 时分子 $x^{2}>0$、分母 $(1+x)(2+x)^{2}>0$，故
        $$F'(x)>0,$$
        即 $F$ 在 $[0,+\infty)$ 上==严格递增==。

        于是当 $x>0$ 时
        $$F(x)>F(0)=0,$$
        即
        $$\ln(1+x)>\frac{2x}{2+x}.\qquad\blacksquare$$
      `,
      comment: String.raw`
        **数值验证**：

        | $x$ | $\ln(1+x)$ | $\frac{2x}{2+x}$ |
        |---|---|---|
        | $0.5$ | $0.4055$ | $0.4000$ |
        | $1$ | $0.6931$ | $0.6667$ |
        | $5$ | $1.7918$ | $1.4286$ |

        ==左边始终更大，且差距随 $x$ 增大== $\checkmark$

        **这个不等式的来历**：它是 $\ln(1+x)$ 的一个==帕德逼近==，
        比常见的 $\ln(1+x)<x$ 精确得多：

        | 近似 | $x=1$ 时的值 | 误差 |
        |---|---|---|
        | $x$（上界） | $1$ | $+0.307$ |
        | $\frac{x}{1+x}$（下界） | $0.5$ | $-0.193$ |
        | ==$\frac{2x}{2+x}$（下界）== | $0.667$ | ==$-0.026$== |

        ==它是比 $\frac{x}{1+x}$ 好得多的下界==。

        **本题为什么只需一层**：因为 $F'$ 通分后分子恰好是 $x^{2}$，
        ==符号一目了然==。
        ==若分子是个符号不定的二次式，就要对 $F'$ 再求一次导==，
        并补验 $F'(0)$——那才是"两层单调"。

        **两层单调的完整模板**（供对照）：
        $$F(0)=0,\quad F'(0)=0,\quad F''>0
        \ \Longrightarrow\ F'>0\ \Longrightarrow\ F>0.$$
        ==三个条件缺一不可==，
        ==尤其 $F'(0)=0$ 这一步最容易漏==——
        没有它，"$F''>0$" 只能推出 $F'$ 递增，==推不出 $F'>0$==。
      `,
    },

    { t: 'example',
      id: 'ex-best-constant',
      title: '分离参数求最好的常数',
      source: '标准例题',
      level: 4,
      problem: String.raw`
        求最大的常数 $k$，使得
        $$\ln(1+x)\ \ge\ \frac{kx}{1+x}$$
        对一切 $x\ge0$ 成立。
      `,
      idea: String.raw`
        **分离参数**：$x>0$ 时 $\frac{x}{1+x}>0$，可以除过去：
        $$k\le\frac{(1+x)\ln(1+x)}{x}\qquad\forall x>0.$$
        ==所以 $k_{\max}=\inf\limits_{x>0}g(x)$，其中
        $g(x)=\frac{(1+x)\ln(1+x)}{x}$==。

        **$x=0$ 处要单独看**：原不等式在 $x=0$ 时两边都是 $0$，==恒成立==，
        不构成限制。

        **求 $g$ 的下确界**：先看两个端点的极限。
        - $x\to0^{+}$：$(1+x)\ln(1+x)\sim x$，故 ==$g\to1$==；
        - $x\to+\infty$：$g\sim\ln x\to+\infty$。

        ==所以下确界很可能是 $1$（在 $x\to0^{+}$ 处取到，但取不到）==。
        要确认 $g$ 在 $(0,+\infty)$ 上==递增==。

        **证 $g$ 递增**：求导会比较繁，
        ==换个思路：证 $g(x)>1$ 即可==，
        即 $(1+x)\ln(1+x)>x$——==这是一个更简单的不等式==，
        用[单调性路线](#/calculus/derivative-app/inequality?at=monotone-route)一步就出。

        ==所以 $k\le1$，且 $k=1$ 时不等式成立==（严格来说是 $\ge$）。
      `,
      solution: String.raw`
        **$x=0$ 时**两边均为 $0$，不等式成立，对 $k$ 无限制。

        **$x>0$ 时**，$\dfrac{x}{1+x}>0$，原不等式等价于
        $$k\ \le\ g(x):=\frac{(1+x)\ln(1+x)}{x}.$$
        故所求的最大 $k$ 为 $\displaystyle\inf_{x>0}g(x)$。

        **证 $g(x)>1$**：等价于
        $$(1+x)\ln(1+x)>x\qquad(x>0).$$
        令 $h(x)=(1+x)\ln(1+x)-x$，则 $h(0)=0$，且
        $$h'(x)=\ln(1+x)+1-1=\ln(1+x)>0\quad(x>0),$$
        故 $h$ 严格递增，$h(x)>h(0)=0$，即 $g(x)>1$。

        **证下确界就是 $1$**：由
        $$\lim_{x\to0^{+}}g(x)=\lim_{x\to0^{+}}\frac{(1+x)\ln(1+x)}{x}
        =\lim_{x\to0^{+}}(1+x)\cdot\frac{\ln(1+x)}{x}=1\cdot1=1,$$
        故 $\inf\limits_{x>0}g(x)=1$（==不能取到，但可以无限接近==）。

        因此 $k\le1$；而 $k=1$ 时由上面已证 $g(x)>1\ge k$，不等式成立。

        **结论**：最大的常数为 $\boxed{k=1}$，即
        $$\ln(1+x)\ge\frac{x}{1+x}\qquad(x\ge0).$$
      `,
      comment: String.raw`
        **这道题的结构值得拆开看**：

        1. ==分离参数==把"求 $k$"变成"求 $g$ 的下确界"；
        2. 求下确界时==没有直接对 $g$ 求导==（会很繁），
           而是==猜出下确界是 $1$（由 $x\to0$ 的极限），再证 $g>1$==；
        3. 证 $g>1$ 又化归成一个更简单的不等式。

        =="先由端点极限猜出答案，再验证"是这类题的高效路线==。
        ==直接对 $g$ 求导也能做，但分子会出现 $x-\ln(1+x)$ 之类，还要再讨论一层。==

        **注意"下确界取不到"这个细节**：
        $g(x)>1$ 严格成立，$\inf g=1$ ==但不是最小值==。
        ==这不影响答案==：$k=1$ 仍然满足"$k\le g(x)$ 对一切 $x>0$"。
        ==若下确界能取到且我们要严格不等号，答案才会变成开区间。==

        **结果的意义**：
        $$\frac{x}{1+x}\le\ln(1+x)\le x\qquad(x\ge0)$$
        ==这对经典的双边估计，两端都是"最好的"==：
        $x$ 是最好的上界（在 $0$ 处相切），$\frac{x}{1+x}$ 是最好的一次分式下界。
        ==它们在[积分不等式的放缩](#/calculus/definite/integral-proof?at=scaling)里天天用到。==

        **同型题的变体**：
        "求最小的 $k$ 使 $\ln(1+x)\le kx$ 对 $x\ge0$ 成立"，
        ==分离得 $k\ge\frac{\ln(1+x)}{x}$，而右边在 $x\to0^{+}$ 时趋于 $1$ 且递减==，
        故 $k_{\min}=1$。
        ==注意这次是"上确界"，方向和本题相反==。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '二、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **端点选错方向**：==要证 $F>0$ 且 $F$ 递增，从左端点出发==。
      2. **多层单调漏验中间端点**：$F(a)\ge0$、$F'(a)\ge0$ ==每层都要验==。
      3. **不先化简就构造 $F$**：==能去分母、取对数就先做==。
      4. **分离参数时不看 $g$ 的符号**：==$g<0$ 要反号，$g$ 变号要分段==。
      5. **端点不在定义域内还代值**：用 ==$\lim\limits_{x\to a^{+}}F$== 代替 $F(a)$。
      6. **把 $\inf$ 当成 $\min$**：==取不到不影响"$\le$"型的答案==，但要说清。
      7. **凹凸路线不确认 $f''$ 符号**：==它决定不等号方向==。
      8. **$F'$ 通分后不化简**：==分子往往能化成完全平方或单项式==，
         化不出来多半是代数算错了。
      9. **严格不等号与非严格混用**：$F'>0$ 给严格、$F'\ge0$ 只给非严格。
    ` },

  ],
});
