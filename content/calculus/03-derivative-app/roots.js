/* ==========================================================================
   高等数学 / 3 微分中值定理与导数应用 / 零点个数与方程的根
   ========================================================================== */

KM.page({
  path: 'calculus/derivative-app/roots',
  title: '零点个数与方程的根',
  subtitle: '证"恰有 n 个根"的统一打法：导数切出单调段，每段各用一次零点定理',
  tags: ['大题', '证明题', '高频'],
  updated: '2026-08-10',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'framework', c: '一、统一框架：存在性 + 唯一性' },

    { t: 'key', id: 'two-steps', title: '一个根的标准两步', c: String.raw`
      证明"方程在 $(a,b)$ 内==有且仅有==一个根"，永远是两步，缺一不可：

      | 步骤 | 要证什么 | 工具 |
      |---|---|---|
      | **存在性** | 至少一个 | [零点定理](#/calculus/limit/closed-interval?at=use-zero)：找出 $F(a)F(b)<0$ |
      | **唯一性** | 至多一个 | 严格单调：证 $F'(x)>0$ 或 $F'(x)<0$ 在整个区间上恒成立 |

      ==只写一半是最常见的丢分点。== 很多人证完存在性就收笔，唯一性一分不得。
    ` },

    { t: 'method', id: 'monotone-split', title: '多个根：单调区间划分法', c: String.raw`
      要证"恰有 $n$ 个根"，把上面的两步在每个单调段上各做一遍：

      1. 求 $F'(x)$，解出全部驻点，把区间切成若干==严格单调==的小段；
      2. 每段内 $F$ 严格单调 $\Rightarrow$ 每段==至多一个根==，于是先得到"根数 $\le$ 段数"；
      3. 算出所有分界点（端点 + 驻点）处 $F$ 的==符号==；
      4. 相邻两点异号的那些段，各用一次零点定理，凑够 $n$ 个。

      **关键认识**：==单调段的数目从一开始就锁死了根数的上界==。
      所以看到题目说"恰有两个根"，第一反应应该是
      "$F$ 大概只有一个驻点、只有两段单调"，而不是先去算函数值。
    ` },

    { t: 'compare',
      id: 'variants',
      title: '问法 → 该做什么',
      cols: ['题目怎么问', '要证的两件事', '主要工具'],
      rows: [
        ['至少有一个根', '只证存在', '零点定理'],
        ['有且仅有一个根', '存在 + 至多一个', '零点定理 + 严格单调'],
        ['恰有 $n$ 个根', '每段至多一个 + 凑够 $n$ 个', '驻点分段 + 逐段零点定理'],
        ['至多有一个根', '只证至多', '严格单调，或[罗尔反证](#/calculus/derivative-app/mvt-proof?at=four-theorems)'],
        ['讨论参数使根数变化', '把参数分离成 $\\lambda=g(x)$', '画 $g$ 的单调性与极值，看水平线交点'],
      ] },

    { t: 'key', id: 'convexity-bound', title: '二阶导数定号 = 根数上界', c: String.raw`
      若 $F''(x)>0$ 在区间上恒成立，则：

      - $F'$ ==严格单调递增== $\Rightarrow$ $F'(x)=0$ ==至多一个解== $\Rightarrow$ $F$ 至多两段单调；
      - 因此 $F(x)=0$ ==至多两个根==。

      同理 $F'''$ 定号 $\Rightarrow$ 至多三个根，依此类推。
      ==这是"恰有两个根"这类题的题眼==：题目敢说"两个"，
      往往就是因为二阶导数恒正（或恒负）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'abs-integral', c: '二、被积函数带绝对值的变限积分' },

    { t: 'method', id: 'split-first', title: '必须先按分界点拆区间，再求导', c: String.raw`
      形如
      $$F(x)=\int_{a}^{b}\left|x-t\right|g(t)\dt$$
      的函数（$x$ 是参数、$t$ 是积分变量），处理顺序是死的：

      1. **以 $t=x$ 为界把积分拆成两段**，让绝对值在每段内变成确定的符号：
         $$\int_a^{x}(x-t)g(t)\dt+\int_x^{b}(t-x)g(t)\dt ;$$
      2. **把与 $t$ 无关的 $x$ 提到积分号外**，化成"$x\cdot$变限积分"加"变限积分"的形式；
      3. **再用变限积分求导法则**求 $F'$，注意 $x$ 同时出现在==上下限和被积表达式外==，
         要用乘积法则。

      拆完之后 $F$ 就是初等函数与变限积分的组合，一切照常。
    ` },

    { t: 'warn', id: 'no-direct-diff', title: '为什么不能直接对 $\\left|x-t\\right|$ 求导', c: String.raw`
      $\left|x-t\right|$ 在 $t=x$ 处==不可导==，直接对积分号内求导（把导数搬进积分号）
      在这一点上没有依据，是无效步骤。

      ==拆区间不是"为了好算"，是为了合法。==
      改卷时直接对绝对值求导的过程，即使结果碰巧对了也拿不到步骤分。
    ` },

    { t: 'method', id: 'estimate-sign', title: '端点符号算不出来时：把积分放缩成可算的', c: String.raw`
      像 $\displaystyle\int_0^1 e^{-t^2}\dt$ 这种==没有初等原函数==的积分，
      判断符号时不要硬算，找一个可积的函数把它夹住：

      在 $(0,1)$ 上 $t^2<t$，故 $-t^2>-t$，故
      $$e^{-t^2}>e^{-t}\;\Longrightarrow\;
      \int_0^1 e^{-t^2}\dt>\int_0^1 e^{-t}\dt=1-e^{-1}.$$

      常用的放缩来源：

      | 场合 | 常用不等式 |
      |---|---|
      | $t\in(0,1)$ | $t^{2}<t$，故 $e^{-t^{2}}>e^{-t}$、$\sqrt t>t$ |
      | 全局 | $e^{x}\ge 1+x$，$\ln(1+x)\le x$ |
      | $t\in(0,\frac\pi2)$ | $\frac{2}{\pi}t<\sin t<t$ |

      ==放缩方向必须和要证的不等号一致==：要证 $F(1)>0$ 就把 $F(1)$ 往小了放。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '三、例题' },

    { t: 'example',
      id: 'ex-two-roots',
      title: '带绝对值的变限积分：证明恰有两个实根',
      source: '综合大题',
      level: 4,
      problem: String.raw`
        设
        $$F(x)=-\frac{1}{2}\left(1+e^{-1}\right)+\int_{-1}^{1}\left|x-t\right|e^{-t^{2}}\dt ,$$
        证明 $F(x)=0$ 在 $(-1,1)$ 内有两个实根。
      `,
      idea: String.raw`
        **第一眼：为什么是"两个"。** 根数的上界由单调段数决定。
        猜测 $F$ 是凸的（二阶导数恒正）—— 那就只有一个驻点、两段单调、
        ==至多两个根==。所以任务变成：证明它确实凸，再凑出两次变号。

        **第二眼：绝对值挡路。** $\left|x-t\right|$ 在 $t=x$ 处不可导，不能直接求导，
        必须先以 $t=x$ 为界拆成两段，把绝对值去掉。

        **拆完之后有个惊喜。** 老老实实求出
        $$F'(x)=\int_{-1}^{x}e^{-t^{2}}\dt-\int_{x}^{1}e^{-t^{2}}\dt$$
        之后，别急着去求 $F''$ —— 利用 $e^{-t^2}$ 是==偶函数==把两个积分合并：
        $$F'(x)=\underbrace{\left(\int_{-1}^{0}-\int_{0}^{1}\right)}_{=0}+2\int_{0}^{x}e^{-t^{2}}\dt
        =2\int_{0}^{x}e^{-t^{2}}\dt .$$
        ==这一步把单调性直接摆在脸上==：被积函数恒正，所以 $x<0$ 时 $F'<0$、
        $x>0$ 时 $F'>0$、$F'(0)=0$。驻点唯一、两段单调，一步到位。

        **第三眼：三个点定号。** $F$ 是偶函数（$\left|-x-t\right|$ 换元 $t\to-t$ 即知），
        所以只需算 $F(0)$ 和 $F(1)$ 两个值。
        $F(0)$ 能直接算出 $\frac{e-3}{2e}<0$；
        $F(1)$ 里剩下 $\int_0^1e^{-t^2}\dt$ ==算不出来==，用 $e^{-t^2}>e^{-t}$ 放缩即可。
      `,
      solution: String.raw`
        **第一步：去绝对值，化简 $F$。**

        对 $x\in(-1,1)$，以 $t=x$ 为界拆开：
        $$\int_{-1}^{1}\left|x-t\right|e^{-t^{2}}\dt
        =\int_{-1}^{x}(x-t)e^{-t^{2}}\dt+\int_{x}^{1}(t-x)e^{-t^{2}}\dt .$$
        把与 $t$ 无关的 $x$ 提出：
        $$=x\int_{-1}^{x}e^{-t^{2}}\dt-\int_{-1}^{x}te^{-t^{2}}\dt
        +\int_{x}^{1}te^{-t^{2}}\dt-x\int_{x}^{1}e^{-t^{2}}\dt .$$

        由 $\displaystyle\int te^{-t^{2}}\dt=-\frac12e^{-t^{2}}+C$：
        $$-\int_{-1}^{x}te^{-t^{2}}\dt=\frac12e^{-x^{2}}-\frac12e^{-1},\qquad
        \int_{x}^{1}te^{-t^{2}}\dt=\frac12e^{-x^{2}}-\frac12e^{-1},$$
        两者之和为 $e^{-x^{2}}-e^{-1}$。故
        $$F(x)=-\frac12\left(1+e^{-1}\right)
        +x\left(\int_{-1}^{x}e^{-t^{2}}\dt-\int_{x}^{1}e^{-t^{2}}\dt\right)
        +e^{-x^{2}}-e^{-1}.$$

        ---

        **第二步：求导，定出单调性。**

        记 $A(x)=\displaystyle\int_{-1}^{x}e^{-t^{2}}\dt$，$B(x)=\displaystyle\int_{x}^{1}e^{-t^{2}}\dt$，
        则 $A'=e^{-x^{2}}$，$B'=-e^{-x^{2}}$。于是
        $$F'(x)=\left(A-B\right)+x\left(A'-B'\right)-2xe^{-x^{2}}
        =\left(A-B\right)+2xe^{-x^{2}}-2xe^{-x^{2}}=A-B .$$
        即
        $$F'(x)=\int_{-1}^{x}e^{-t^{2}}\dt-\int_{x}^{1}e^{-t^{2}}\dt .$$

        利用 $e^{-t^{2}}$ 为偶函数，$\displaystyle\int_{-1}^{0}e^{-t^{2}}\dt=\int_{0}^{1}e^{-t^{2}}\dt$，
        把上式合并：
        $$F'(x)=\left(\int_{-1}^{0}-\int_{0}^{1}\right)e^{-t^{2}}\dt+2\int_{0}^{x}e^{-t^{2}}\dt
        =2\int_{0}^{x}e^{-t^{2}}\dt .$$

        因 $e^{-t^{2}}>0$，故

        - $x\in(-1,0)$ 时 $F'(x)<0$，$F$ ==严格单调递减==；
        - $x=0$ 时 $F'(0)=0$；
        - $x\in(0,1)$ 时 $F'(x)>0$，$F$ ==严格单调递增==。

        （亦可由 $F''(x)=2e^{-x^{2}}>0$ 知 $F'$ 严格递增，驻点唯一。）
        所以 $x=0$ 是 $F$ 在 $[-1,1]$ 上的唯一极小值点，$F$ 至多两段单调，
        从而 ==$F(x)=0$ 至多两个根==。

        ---

        **第三步：定出三个点的符号。**

        *偶性*：在 $\displaystyle\int_{-1}^{1}\left|-x-t\right|e^{-t^{2}}\dt$ 中令 $t=-s$，
        得 $\displaystyle\int_{-1}^{1}\left|x-s\right|e^{-s^{2}}\ds$，故 $F(-x)=F(x)$，
        $F$ 为偶函数，只需算 $F(0)$ 与 $F(1)$。

        *$F(0)$*：代入 $x=0$，中间一项为 $0$，
        $$F(0)=-\frac12\left(1+e^{-1}\right)+e^{0}-e^{-1}
        =\frac12-\frac32e^{-1}=\frac{e-3}{2e}.$$
        因 $e<3$，故 ==$F(0)<0$==。

        *$F(1)$*：此时对一切 $t\in[-1,1]$ 有 $\left|1-t\right|=1-t$，
        $$F(1)=-\frac12\left(1+e^{-1}\right)+\int_{-1}^{1}(1-t)e^{-t^{2}}\dt .$$
        $te^{-t^{2}}$ 是奇函数，在对称区间上积分为 $0$；$e^{-t^{2}}$ 是偶函数，故
        $$F(1)=-\frac12\left(1+e^{-1}\right)+2\int_{0}^{1}e^{-t^{2}}\dt .$$
        在 $(0,1)$ 上 $t^{2}<t$，故 $e^{-t^{2}}>e^{-t}$，于是
        $$\int_{0}^{1}e^{-t^{2}}\dt>\int_{0}^{1}e^{-t}\dt=1-e^{-1}.$$
        代回得
        $$F(1)>-\frac12-\frac12e^{-1}+2\left(1-e^{-1}\right)
        =\frac32-\frac52e^{-1}=\frac{3e-5}{2e}.$$
        因 $3e>5$，故 ==$F(1)>0$==，由偶性 $F(-1)=F(1)>0$。

        ---

        **第四步：逐段用零点定理。**

        $F$ 在 $[-1,1]$ 上连续。

        - 在 $[-1,0]$ 上 $F$ 严格递减，且 $F(-1)>0$、$F(0)<0$，
          由零点定理存在唯一 $x_{1}\in(-1,0)$ 使 $F(x_{1})=0$；
        - 在 $[0,1]$ 上 $F$ 严格递增，且 $F(0)<0$、$F(1)>0$，
          由零点定理存在唯一 $x_{2}\in(0,1)$ 使 $F(x_{2})=0$。

        又已证根数至多为二，故 $F(x)=0$ 在 $(-1,1)$ 内==恰有两个实根==。$\blacksquare$
      `,
      comment: String.raw`
        **三个可以省力的地方**（考场上差好几分钟）：

        1. ==把 $F'$ 合并成 $2\int_0^x e^{-t^2}\dt$==，单调性直接可见，
           不必再求 $F''$ 再讨论。合并靠的是 $e^{-t^2}$ 的偶性。
        2. ==先说明 $F$ 是偶函数==，端点只用算一个。
        3. $F(1)$ 里的 $\int_{-1}^1 te^{-t^2}\dt$ 由奇函数直接得 $0$，不用算。

        **为什么必须放缩而不能硬算**：$\int_0^1e^{-t^2}\dt$ 没有初等原函数
        （它是误差函数 $\mathrm{erf}$），考场上任何"算出来"的写法都是错的。
        ==看到 $e^{-t^2}$、$\frac{\sin t}{t}$、$\frac{1}{\ln t}$ 这类被积函数，就该切换到放缩模式==。

        **同族变式**：把 $e^{-t^{2}}$ 换成任何在 $[-1,1]$ 上恒正的偶函数，
        整套做法一字不变；换成非偶函数，则驻点不再是 $x=0$，
        需要另行论证驻点位置（或只说"存在唯一驻点"即可，不必求出）。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '四、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这类题的固定失分点', c: String.raw`
      1. **只证存在、不证唯一**：题目问"恰有 / 有且仅有"时，必须同时给出根数上界。
      2. **直接对 $\left|x-t\right|$ 求导**：$t=x$ 处不可导，==必须先拆区间==。
      3. **单调性只说"由图可知"**：要写出 $F'$ 的符号判断，不能靠直觉。
      4. **放缩方向反了**：证 $F(1)>0$ 要把 $F(1)$ 往==小==了估；反之亦然。
      5. **忘记验证连续性**：零点定理要求闭区间上连续，这一句要写。
      6. **驻点处的值忘了算**：分界点的符号是把"至多两个"落实成"恰好两个"的关键。
      7. **把极值点当成根**：$F(0)<0$ 说明极小值点在 $x$ 轴下方，
         这恰恰是有两个根的原因；若 $F(0)>0$ 则一个根都没有。
    ` },

  ],
});
