/* ==========================================================================
   高等数学 / 3 微分中值定理与导数应用 / 导数有界与函数的控制
   —— 2020 真题（|f'| ≤ M ⟹ M = 0）的专题页：包络 + 取等刚性
   ========================================================================== */

KM.page({
  path: 'calculus/derivative-app/derivative-bound',
  title: '导数有界与函数的控制',
  subtitle: String.raw`条件里出现 $\abs{f'(x)}\le M$ 时该想什么 —— 锥形包络、取等刚性，与 2020 真题压轴`,
  tags: ['大题', '证明题', '真题', '高频'],
  updated: '2026-08-13',

  blocks: [

    { t: 'md', c: String.raw`
      这一页只处理一件事：**题目给了导数的界（或让你去估计导数的界）时，函数被限制成了什么样子。**

      整页的骨架就两句话：

      1. 导数的界把函数图像==关进一个锥形区域==（这是不等式的一半）；
      2. 一旦函数真的==碰到==锥的边界，它就被锁死成一条直线 —— 这叫**取等刚性**（这是被大多数人漏掉的另一半）。

      考场上第一句几乎人人会用，卡死人的永远是第二句。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'cone', c: '一、导数的界 = 图像被关进锥形区域' },

    { t: 'key', id: 'lipschitz', title: '拉格朗日的"估值"身份', c: String.raw`
      设 $f$ 在区间上可导且 $\abs{f'(x)}\le M$，则对任意两点 $x,y$，
      $$\abs{f(x)-f(y)}=\abs{f'(\xi)}\cdot\abs{x-y}\le M\abs{x-y}.$$

      ==拉格朗日中值定理最常用的身份，不是"存在一点"，而是"把函数增量换算成导数乘长度"。==
      上式称为 **Lipschitz（利普希茨）条件**，几何意义是：
      以图像上任意一点为顶点、张开斜率为 $\pm M$ 的双锥，==整条曲线都逃不出这个锥==。

      反过来读同样重要：
      $$\abs{f(x)-f(y)}\ \text{已知很大}\quad\Longrightarrow\quad
        \exists\,\xi,\ \abs{f'(\xi)}\ge\frac{\abs{f(x)-f(y)}}{\abs{x-y}} .$$
      **函数在短距离内变化大 $\Rightarrow$ 导数必然有大值。** 这是所有"证明存在 $\xi$ 使 $\abs{f'(\xi)}$ 不小于某个数"的引擎。
    ` },

    { t: 'key', id: 'envelope', title: '两端为零时的包络：一个"屋顶"', c: String.raw`
      设 $f(0)=f(2)=0$ 且 $\abs{f'(x)}\le M$。分别以 $0$ 和 $2$ 为基点用上面的估计：

      $$\abs{f(x)}=\abs{f(x)-f(0)}\le Mx,\qquad
        \abs{f(x)}=\abs{f(x)-f(2)}\le M(2-x).$$

      两条一起：
      $$\boxed{\ \abs{f(x)}\le M\cdot\min\{x,\ 2-x\}\ \le M\ }$$

      图像被两条直线 $y=Mx$ 与 $y=M(2-x)$ 夹成一个**屋顶形**，
      顶点唯一地落在 $\left(1,M\right)$。

      ==注意这里还没有任何矛盾==：屋顶的高度恰好是 $M$，函数完全"够得着"。
      所以第二问的矛盾不可能从这个不等式本身挤出来，
      必须去追问==够得着要付出什么代价==（见下一节）。
    ` },

    { t: 'method', id: 'reverse', title: '反向用法：从最值点反推导数的下界', c: String.raw`
      设 $M=\max\limits_{[0,2]}\abs{f(x)}>0$。因为 $f(0)=f(2)=0$，最大值点必在内部：
      存在 $x_0\in(0,2)$ 使 $\abs{f(x_0)}=M$。

      **关键动作：把 $x_0$ 当分点，把区间劈成两段。**
      这样每一段都恰好有一个端点函数值为 $0$，差商立刻算得出来：

      | 区间 | 拉格朗日给出 | 绝对值 |
      |---|---|---|
      | $[0,x_0]$ | $f'(\xi_1)=\dfrac{f(x_0)-f(0)}{x_0}$ | $\abs{f'(\xi_1)}=\dfrac{M}{x_0}$ |
      | $[x_0,2]$ | $f'(\xi_2)=\dfrac{f(2)-f(x_0)}{2-x_0}$ | $\abs{f'(\xi_2)}=\dfrac{M}{2-x_0}$ |

      因为两段长度之和恒等于 $2$，==必有一段长度 $\le 1$==，那一段的差商就 $\ge M$。
      一句话就覆盖了 $x_0<1$、$x_0=1$、$x_0>1$ 三种情况。

      **通用形式**（区间换成 $[a,b]$，$f(a)=f(b)=0$）：
      $$\exists\,\xi\in(a,b),\qquad \abs{f'(\xi)}\ \ge\ \frac{2M}{b-a}.$$
      $b-a=2$ 时右端正好是 $M$ —— 真题里的那个 $2$ 不是巧合，是被算出来的。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'rigidity', c: '二、取等刚性：等号成立要付出什么代价' },

    { t: 'md', c: String.raw`
      一个不等式取到等号，通常意味着==所有的放缩都必须同时取等==，
      而这会把函数的形状彻底锁死。这类论证在考研里出现的形式高度固定，只有下面两种写法。
    ` },

    { t: 'key', id: 'zero-integral', title: '写法一｜积分版（题目给"连续导数"时首选）', c: String.raw`
      已知 $f(0)=0,\ f(1)=M,\ f'(x)\le M$，且 $f'$ 连续。由牛顿–莱布尼茨公式
      $$M=f(1)-f(0)=\int_0^1 f'(x)\dx
        \quad\Longrightarrow\quad \int_0^1\left[\,M-f'(x)\,\right]\dx=0 .$$
      而 $M-f'(x)$ ==连续且非负==，积分为零，故
      $$f'(x)\equiv M,\qquad x\in[0,1].$$

      依据的一般原理值得单独记住：
      $$g\in C[a,b],\quad g\ge 0,\quad \int_a^b g\dx=0\ \Longrightarrow\ g\equiv 0 .$$
      （理由：若某点 $g(c)>0$，由连续性在 $c$ 的一个小邻域内 $g>\frac{g(c)}{2}$，
      该段的积分已经为正，与总积分为零矛盾。）
    ` },

    { t: 'key', id: 'monotone-trap', title: '写法二｜单调版（只要求可导，不需要导数连续）', c: String.raw`
      同样的条件下，令
      $$g(x)=Mx-f(x),\qquad x\in[0,1].$$
      则 $g'(x)=M-f'(x)\ge 0$，故 $g$ ==单调不减==；又
      $$g(0)=0,\qquad g(1)=M-f(1)=0 .$$
      对任意 $x\in[0,1]$ 有 $0=g(0)\le g(x)\le g(1)=0$，夹逼得 $g\equiv 0$，即
      $$f(x)=Mx,\qquad x\in[0,1].$$

      **这就是"辅助函数"的来历**：$g$ 不是凭空造的，
      它就是==包络线 $y=Mx$ 与真实曲线之间的高度差==。
      证明 $g\equiv 0$，等于证明曲线全程贴死在包络线上。
    ` },

    { t: 'warn', id: 'equality-detail', title: '这两步都必须写理由', c: String.raw`
      - "非负连续函数积分为零 $\Rightarrow$ 恒为零"：至少补一句反证，==不能直接写"显然"==。
      - "单调不减 + 两端点值相等 $\Rightarrow$ 恒为常数"：要写出夹逼那一行 $g(0)\le g(x)\le g(1)$。
      - 用到 $f'$ 连续时（写法一），要在开头点明"$f$ 具有连续导数，故 $f'$ 在 $[0,2]$ 上连续"，
        这是题目白送的条件，==不用等于浪费==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'exam', c: '三、2020 真题' },

    { t: 'example',
      id: 'ex-2020',
      title: '两端为零 + 导数被最值压制 $\\Rightarrow$ 函数恒为零',
      source: '2020 数一 真题',
      level: 4,
      problem: String.raw`
        设函数 $f(x)$ 在区间 $[0,2]$ 上具有连续导数，$f(0)=f(2)=0$，
        $$M=\max_{x\in[0,2]}\abs{f(x)} .$$
        证明：

        **(I)** 存在 $\xi\in(0,2)$，使得 $\abs{f'(\xi)}\ge M$；
        **(II)** 若对任意的 $x\in(0,2)$ 都有 $\abs{f'(x)}\le M$，则 $M=0$。
      `,
      idea: String.raw`
        **(I) 为什么想到劈区间。**
        结论要的是 $\abs{f'(\xi)}\ge M$，而 $M$ 是==函数值==的量级 ——
        把函数值换成导数，工具只有拉格朗日。
        但拉格朗日需要两个端点值，手上只有 $f(0)=f(2)=0$，
        而 $M$ 出现在中间某个未知点 $x_0$。

        ==所以第一步只能是：把 $x_0$ 拽出来当分点。==
        劈开之后，每段都恰好有一个端点值为 $0$，差商立刻等于 $\pm M/\text{段长}$，
        剩下的只是比较段长与 $1$ 的大小。

        **(II) 卡住的地方在哪。**
        第二问的条件恰好是第一问结论的**反向压制**：
        第一问说"必有一点导数 $\ge M/\text{段长}$"，第二问说"所有点导数 $\le M$"。
        两句一夹，段长只能等于 $1$，最值点被逼到 $x_0=1$。

        ==被逼到唯一位置，就是"刚性"的信号。==
        这时候该问的不再是"还能往哪走"，而是
        **"等号成立要付出什么代价"** —— 代价就是 $f$ 必须在两段上都以最大速度直线前进，
        于是 $x=1$ 变成一个尖点，与"$f$ 有连续导数"直接冲突。
      `,
      solution: String.raw`
        **(I)**

        若 $M=0$，则 $f\equiv 0$，任取 $\xi\in(0,2)$ 均有 $\abs{f'(\xi)}=0\ge M$，结论成立。

        以下设 $M>0$。$f$ 在 $[0,2]$ 上连续，由最值定理存在 $x_0\in[0,2]$ 使 $\abs{f(x_0)}=M$。
        又 $f(0)=f(2)=0<M$，故 $x_0\in(0,2)$。

        在 $[0,x_0]$ 与 $[x_0,2]$ 上分别用拉格朗日中值定理：存在
        $\xi_1\in(0,x_0)$、$\xi_2\in(x_0,2)$，使得
        $$f'(\xi_1)=\frac{f(x_0)-f(0)}{x_0-0}=\frac{f(x_0)}{x_0},\qquad
          f'(\xi_2)=\frac{f(2)-f(x_0)}{2-x_0}=\frac{-f(x_0)}{2-x_0},$$
        取绝对值得
        $$\abs{f'(\xi_1)}=\frac{M}{x_0},\qquad \abs{f'(\xi_2)}=\frac{M}{2-x_0}.$$

        因 $x_0+(2-x_0)=2$，故 $x_0\le 1$ 与 $2-x_0\le 1$ 至少成立一个。

        - 若 $x_0\le 1$，取 $\xi=\xi_1$，则 $\abs{f'(\xi)}=\dfrac{M}{x_0}\ge M$；
        - 若 $x_0>1$，则 $2-x_0<1$，取 $\xi=\xi_2$，则 $\abs{f'(\xi)}=\dfrac{M}{2-x_0}>M$。

        两种情形均有 $\xi\in(0,2)$ 且 $\abs{f'(\xi)}\ge M$。

        ---

        **(II)** 反证法。设 $M>0$。

        **第 1 步：把最值点逼到 $x=1$。**
        沿用 (I) 的记号（注意这里用的是 ==(I) 的证明过程==，不是 (I) 的结论）。
        由已知 $\abs{f'(x)}\le M$ 对一切 $x\in(0,2)$ 成立，而 $\xi_1,\xi_2\in(0,2)$，故
        $$\frac{M}{x_0}=\abs{f'(\xi_1)}\le M\ \Longrightarrow\ x_0\ge 1,$$
        $$\frac{M}{2-x_0}=\abs{f'(\xi_2)}\le M\ \Longrightarrow\ 2-x_0\ge 1\ \Longrightarrow\ x_0\le 1 .$$
        两式合起来得 $x_0=1$，即 $\abs{f(1)}=M$。

        不妨设 $f(1)=M>0$（若 $f(1)=-M$，用 $-f$ 代替 $f$，题设条件与所证结论都不变）。

        **第 2 步：等号成立迫使 $f$ 在两段上都是直线。**
        $f$ 具有连续导数，由牛顿–莱布尼茨公式，在 $[0,1]$ 上
        $$M=f(1)-f(0)=\int_0^1 f'(x)\dx
          \ \Longrightarrow\ \int_0^1\left[\,M-f'(x)\,\right]\dx=0 .$$
        被积函数 $M-f'(x)$ 连续且非负，若它在某点为正，则由连续性在该点的一个邻域上的积分为正，
        与上式矛盾。故
        $$f'(x)\equiv M,\qquad x\in[0,1].$$

        同理在 $[1,2]$ 上
        $$-M=f(2)-f(1)=\int_1^2 f'(x)\dx
          \ \Longrightarrow\ \int_1^2\left[\,M+f'(x)\,\right]\dx=0
          \ \Longrightarrow\ f'(x)\equiv -M,\qquad x\in[1,2].$$

        **第 3 步：在 $x=1$ 处撞车。**
        由第一式取 $x=1$ 得 $f'(1)=M$；由第二式取 $x=1$ 得 $f'(1)=-M$。
        故 $M=-M$，即 $M=0$，与假设 $M>0$ 矛盾。

        综上 $M=0$（从而 $f\equiv 0$）。
      `,
      comment: String.raw`
        **(II) 的第二种写法**（不使用积分，只用可导性）：
        在 $[0,1]$ 上令 $g(x)=Mx-f(x)$，则 $g'\ge 0$ 且 $g(0)=g(1)=0$，
        由[单调 + 夹逼](#/calculus/derivative-app/derivative-bound?at=monotone-trap)得 $f(x)=Mx$；
        于是左导数 $f'_-(1)=M>0$。但 $f$ 在内点 $x=1$ 取得最大值且可导，
        由[费马引理](#/calculus/limit/closed-interval?at=use-extremum)必有 $f'(1)=0$，矛盾。
        ==这条路只用到 $f$ 可导，连"导数连续"都不需要。==

        **"连续导数"这个条件到底在拦谁。**
        把可导性去掉，取
        $$f(x)=M\cdot\min\{x,\ 2-x\}\quad(\text{三角折线}),$$
        它连续、$f(0)=f(2)=0$、除 $x=1$ 外处处有 $\abs{f'(x)}=M$、且最大值 $M>0$。
        ==(II) 的结论对它就是假的。==
        所以整道题的全部张力都集中在 $x=1$ 这一个点上：
        题目条件不允许尖点，而不等式的等号又必须造出尖点。

        **一般化**：区间换成 $[a,b]$、$f(a)=f(b)=0$ 时，
        (I) 变成 $\abs{f'(\xi)}\ge\dfrac{2M}{b-a}$，(II) 中的压制条件相应变成
        $\abs{f'(x)}\le\dfrac{2M}{b-a}$，结论仍是 $M=0$，证法一字不改。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'notes', c: '四、我的思路记录' },

    { t: 'insight', id: 'my-route', title: '第一问我是怎么想出来的', c: String.raw`
      我是这样想的：**分类讨论。**
      先假设这个 $M$ 不在端点取得，设它在 $x_0$ 点取得，
      然后对闭区间 $[0,x_0]$ 和 $[x_0,2]$ 都做一次拉格朗日中值定理，
      再分别讨论 $x_0>1$、$x_0<1$、$x_0=1$ 三种情况 ——
      会发现三种情况下都存在 $\ge M$ 的结论。

      **第一次做就把第一问做出来了。**

      > 复盘：这个思路是完全正确的，卷面照写不丢分。
      > 唯一可以再压缩的地方是最后一步 —— 两段长度加起来是 $2$，
      > 所以==必有一段 $\le 1$==，一句话就替掉了三分支讨论。
      > 但先能想到"劈区间"才是这道题真正的门槛，
      > 三分支只是把同一件事说慢了一点。
    ` },

    { t: 'insight', id: 'my-block', title: '第二问卡在哪：推到 $x_0=1$ 之后就断片了', c: String.raw`
      由第一问的结论，如果 $M>0$，而 $\abs{f'}$ 又恒 $\le M$，
      那就只能是最值点在 $x_0=1$ 处取得 —— **说到这里我就卡住了，不知道后面该怎么想。**

      > 想通之后：卡住是因为我一直在问"接下来还能推出什么新东西"，
      > 而正确的问题是=="把 $x_0$ 逼到唯一位置，是不是已经把 $f$ 也逼死了"==。
      >
      > $x_0=1$ 不是一个中间结论，它是**所有不等式同时取等**的证据：
      > $\frac{M}{x_0}\le M$ 和 $\frac{M}{2-x_0}\le M$ 同时成立且 $x_0+(2-x_0)=2$，
      > 说明两段的平均速度都==顶到了限速==。
      > 一旦"平均速度 = 限速上限"，全程就不许有任何一刻低于上限 ——
      > 这就把 $f$ 锁成了两条直线，尖点自动出现。
      >
      > **口诀：看到"只能取某个值"，下一步就去写取等条件，而不是继续找新不等式。**
    ` },

    { t: 'insight', id: 'my-misread', title: '我当时的疑问：$f(x)=Mx$ 代进 $x=2$ 不就是 $2M$ 吗', c: String.raw`
      推到 $f(x)=Mx$ 的时候我懵了：题目明明说 $f(2)=0$，
      可这个式子在 $x=2$ 处给出 $2M$，那 $M$ 不是当场就只能是 $0$ 了吗？

      > 澄清：$f(x)=Mx$ ==只在 $[0,1]$ 上成立==，它是在区间 $[0,1]$ 上、
      > 用端点条件 $f(0)=0$ 与 $f(1)=M$ 推出来的，没有资格代入 $x=2$。
      >
      > $[1,2]$ 上要重新推一次，得到的是 $f(x)=M(2-x)$，代入 $x=2$ 恰好给出 $f(2)=0$，
      > ==完全不矛盾==。所以 $f$ 是一条三角折线，而不是一条直线。
      >
      > 真正的矛盾不在端点值上，而在拼接点 $x=1$：
      > 左边斜率是 $M$，右边斜率是 $-M$，导数在这里跳变。
      > 题目给的"连续导数"不允许跳变，于是 $M=-M$。
      >
      > **教训：辅助函数推出来的等式，有效范围就是构造它的那个区间，一步都不能外推。**
    ` },

    /* ================================================================== */
    { t: 'h', id: 'view', c: '五、高于考研的看法：这道题在说什么' },

    { t: 'method', id: 'kinematics', title: '运动学模型：限速 + 平滑 = 跑不到边界', c: String.raw`
      把 $x$ 读成时间（共 $2$ 秒），$f(x)$ 读成位移。

      | 数学条件 | 物理读法 |
      |---|---|
      | $f(0)=f(2)=0$ | 出发和返回都在原点 |
      | $M=\max\abs{f}$ | 全程跑到过的最远距离 |
      | $\abs{f'(x)}\le M$ | 全程限速为 $M$ |
      | $f'$ 连续 | 速度不许突变，掉头必须减速 |

      **第一问**说的是：$2$ 秒内跑出去 $M$ 再回来，==瞬时速度必然在某一刻达到 $M$==。

      **第二问**说的是：如果限速恰好也是 $M$，那么唯一能碰到距离 $M$ 的跑法是
      "以最高速冲刺 $1$ 秒、瞬间掉头、再以最高速冲回来" ——
      而瞬间掉头意味着速度从 $+M$ 跳到 $-M$。
      题目又要求速度连续（必须有减速过程），==减速就够不着 $M$==。
      两个要求互斥，唯一的出路是这个质点根本没动，即 $M=0$。

      记住这张图之后，那些"辅助函数"就不再是灵感：
      $g(x)=Mx-f(x)$ 就是==实际位置与"全程满速"理想位置之间的差距==，
      证明 $g\equiv 0$ 就是证明它一刻都没有偷懒。
    ` },

    { t: 'method', id: 'rigidity-family', title: '刚性论证：一整个家族，不止这一道题', c: String.raw`
      "不等式取到等号 $\Rightarrow$ 对象被锁死成某个极端形状"是一条贯穿整个数学分析的模式，
      考研范围内你已经见过好几个成员：

      | 不等式 | 取等条件（被锁死成什么） |
      |---|---|
      | $\abs{f(x)-f(y)}\le M\abs{x-y}$ | 取等 $\Rightarrow$ 这一段是==斜率 $\pm M$ 的直线== |
      | [柯西–施瓦茨](#/threads/patterns/cauchy-schwarz?at=one-proof) | 取等 $\Rightarrow$ 两个向量/函数==成比例== |
      | 均值不等式 $\frac{a+b}{2}\ge\sqrt{ab}$ | 取等 $\Rightarrow$ ==所有量相等== |
      | $\int_a^b g\dx=0,\ g\ge0,\ g$ 连续 | $\Rightarrow$ $g\equiv 0$ |

      **共同的动作是同一个**：先证不等式，再假设取等，然后把"取等"当成新的已知条件往回推形状。
      本题的 (II) 就是这条模式的完整演出。

      再往上一层，"==导数被函数自身控制，就把函数逼死==" 是常微分方程解的唯一性定理
      （Gronwall 型论证）的雏形：若 $\abs{f'(x)}\le L\abs{f(x)}$ 且 $f(a)=0$，则 $f\equiv 0$。
      本题把 $\abs{f(x)}$ 换成了全局最大值 $M$，形式不同，但"函数没有自我放大的余地"这层意思是一样的。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '六、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这几处最容易丢分', c: String.raw`
      1. **忘记讨论 $M=0$**。第一问必须先把 $M=0$ 的平凡情形单独交代一句，
         否则"最值点在内部"这个论断就没有依据（$M=0$ 时最值点可以就在端点）。
      2. **没说清最值点为什么在内部**。要写"由最值定理存在 $x_0$ 使 $\abs{f(x_0)}=M$，
         又 $f(0)=f(2)=0<M$，故 $x_0\in(0,2)$"，==三句话缺一不可==。
      3. **第二问引用第一问时张冠李戴**。第二问要用的是第一问==证明过程中的两个中值点==
         $\xi_1,\xi_2$，不是第一问的结论（结论只给了一个 $\xi$）。
         卷面上要写"由 (I) 的证明过程"或直接重新做一遍那两次拉格朗日。
      4. **"不妨设 $f(1)=M$"不加说明**。必须补一句"若 $f(1)=-M$，用 $-f$ 代替 $f$，
         条件与结论均不变"，否则等于只证了一半。
      5. **把 $f(x)=Mx$ 用到 $[0,1]$ 之外**。它只在构造它的那个区间上成立。
      6. **矛盾找错地方**。矛盾在 $x=1$ 处导数跳变（或费马引理给出 $f'(1)=0$），
         ==不在端点函数值上==。
    ` },

    { t: 'md', c: String.raw`
      相关页面：
      [中值定理证明题 · 四大定理的条件](#/calculus/derivative-app/mvt-proof?at=four-theorems)、
      [证明题总纲 · 微分中值定理组](#/calculus/derivative-app/proof-overview?at=mvt-group)、
      [闭区间连续函数四大定理 · 激活费马引理](#/calculus/limit/closed-interval?at=use-extremum)。
    ` },

  ],
});
