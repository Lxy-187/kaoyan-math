/* ==========================================================================
   高等数学 / 3 微分中值定理与导数应用 / 泰勒公式类证明
   ========================================================================== */

KM.page({
  path: 'calculus/derivative-app/taylor-proof',
  title: '泰勒公式类证明',
  subtitle: '在哪展开、展开到几阶、代入哪些点 —— 三个决策定死整道题',
  tags: ['大题', '证明题', '高频'],
  updated: '2026-08-10',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'remainders', c: '一、两种余项，什么时候用哪个' },

    { t: 'key', id: 'remainder', title: '皮亚诺 vs 拉格朗日', c: String.raw`
      **皮亚诺余项**（$x\to x_0$ 的局部信息）
      $$f(x)=\sum_{k=0}^{n}\frac{f^{(k)}(x_0)}{k!}(x-x_0)^{k}+o\!\left((x-x_0)^{n}\right)$$

      **拉格朗日余项**（整个区间上的全局信息，$\xi$ 介于 $x$ 与 $x_0$ 之间）
      $$f(x)=\sum_{k=0}^{n}\frac{f^{(k)}(x_0)}{k!}(x-x_0)^{k}
      +\frac{f^{(n+1)}(\xi)}{(n+1)!}(x-x_0)^{n+1}$$

      | 题目在问什么 | 用哪个 |
      |---|---|
      | 求极限、判断极值/拐点 | **皮亚诺**（只需要 $x_0$ 附近） |
      | 证明不等式、估计最值、证明存在某点 | **拉格朗日**（余项里的 $f^{(n+1)}(\xi)$ 可以放缩） |

      判据很硬：==只要题目给了 $|f^{(n+1)}(x)|\le M$ 这类整个区间上的界，
      或者结论里要出现一个 $\xi$，就必须用拉格朗日余项==。
      皮亚诺余项的 $o(\cdot)$ 是没法放缩的。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'decisions', c: '二、三个决策' },

    { t: 'method', id: 'where', title: '决策 1：在哪个点展开？', c: String.raw`
      **在"要出现在结论里的那个点"展开。**

      - 结论里是 $f'(c)$ → 在 $x=c$ 处展开，这样 $f'(c)$ 就成了一次项系数；
      - 结论是"存在 $\eta$ 使 $f''(\eta)=\cdots$" → 在已知条件最多的点展开；
      - 条件对称（如已知 $f(a),f(b)$）→ 在==中点== $\frac{a+b}{2}$ 展开，
        相加时奇次项自动消掉。
    ` },

    { t: 'method', id: 'order', title: '决策 2：展开到几阶？', c: String.raw`
      **展开到"比已知的最高阶导数低一阶"，让余项恰好装下已知的界。**

      已知 $|f''(x)|\le b$ → 展开到==一阶==，余项是 $\frac{f''(\xi)}{2!}(x-c)^2$，正好能用 $b$ 放缩。
      展开到二阶就会冒出 $f'''$，题目没给界，做不下去。
    ` },

    { t: 'method', id: 'which-points', title: '决策 3：代入哪些点？', c: String.raw`
      **代入能让未知量消掉的点 —— 通常是区间端点。**

      展开式里含 $f(c)$ 和 $f'(c)$ 两个未知量，而结论只要 $f'(c)$。
      代入两个点得到两个等式，==相减消 $f(c)$、相加消 $f'(c)$==：

      | 想消掉谁 | 怎么做 |
      |---|---|
      | 消 $f(c)$，留 $f'(c)$ | 两式相**减** |
      | 消 $f'(c)$，留 $f(c)$ 或 $f''(\xi)$ | 两式相**加** |

      端点是首选，因为 $|x-c|$ 最大时余项估计最松，同时端点函数值往往有已知界。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '三、例题' },

    { t: 'example',
      id: 'ex-estimate',
      title: '端点展开相减：导数的整体估计',
      source: '经典题型',
      level: 3,
      problem: String.raw`
        设 $f(x)$ 在 $[0,1]$ 上具有二阶导数，且 $|f(x)|\le a$，$|f''(x)|\le b$，
        其中 $a,b$ 都是非负常数，$c$ 是 $(0,1)$ 内任一点。

        **(I)** 写出 $f(x)$ 在 $x=c$ 处带拉格朗日余项的一阶泰勒公式；
        **(II)** 证明：$\displaystyle |f'(c)|\le 2a+\frac{b}{2}$。
      `,
      idea: String.raw`
        三个决策一次走完：

        - **在哪展开**：结论要 $f'(c)$，就在 $x=c$ 展开，$f'(c)$ 变成一次项系数；
        - **到几阶**：只给了 $|f''|\le b$，所以展到一阶，余项恰是 $\frac{f''(\xi)}{2}(x-c)^2$；
        - **代哪些点**：展开式里多出一个碍事的 $f(c)$，要消掉它就==两式相减==。
          代 $x=1$ 和 $x=0$，一次项系数变成 $(1-c)-(0-c)=1$ ——
          ==$f'(c)$ 正好被干净地留下，系数是 $1$==，这就是代端点的理由。

        剩下就是取绝对值放缩：$|f(1)|,|f(0)|\le a$ 贡献 $2a$，
        两个余项贡献 $\frac{b}{2}\left[(1-c)^2+c^2\right]$，
        而 $(1-c)^2+c^2\le 1$，于是得到 $2a+\frac b2$。
      `,
      solution: String.raw`
        **(I)** 对任意 $x\in[0,1]$，存在 $\xi$ 介于 $x$ 与 $c$ 之间，使
        $$f(x)=f(c)+f'(c)(x-c)+\frac{f''(\xi)}{2!}(x-c)^{2}.$$

        ---

        **(II)** 分别令 $x=1$ 与 $x=0$：

        存在 $\xi_1\in(c,1)$，使
        $$f(1)=f(c)+f'(c)(1-c)+\frac{f''(\xi_1)}{2}(1-c)^{2};$$
        存在 $\xi_2\in(0,c)$，使
        $$f(0)=f(c)+f'(c)(0-c)+\frac{f''(\xi_2)}{2}c^{2}.$$

        两式相减，$f(c)$ 消去，一次项系数为 $(1-c)-(-c)=1$：
        $$f(1)-f(0)=f'(c)+\frac{f''(\xi_1)}{2}(1-c)^{2}-\frac{f''(\xi_2)}{2}c^{2}.$$

        移项孤立 $f'(c)$：
        $$f'(c)=f(1)-f(0)-\frac{f''(\xi_1)}{2}(1-c)^{2}+\frac{f''(\xi_2)}{2}c^{2}.$$

        两边取绝对值，用三角不等式与已知界 $|f|\le a$、$|f''|\le b$：
        $$|f'(c)|\le |f(1)|+|f(0)|+\frac{|f''(\xi_1)|}{2}(1-c)^{2}+\frac{|f''(\xi_2)|}{2}c^{2}
        \le 2a+\frac{b}{2}\left[(1-c)^{2}+c^{2}\right].$$

        记 $g(c)=(1-c)^{2}+c^{2}=2\left(c-\tfrac12\right)^{2}+\tfrac12$，
        在 $[0,1]$ 上最大值为 $g(0)=g(1)=1$，故 $(1-c)^{2}+c^{2}\le 1$。因此
        $$|f'(c)|\le 2a+\frac{b}{2}. \qquad\blacksquare$$
      `,
      comment: String.raw`
        **为什么系数恰好是 $2a$ 和 $\frac b2$**：$2a$ 来自两个端点函数值各贡献 $a$；
        $\frac b2$ 来自二阶余项的 $\frac{1}{2!}$ 乘上 $(1-c)^2+c^2$ 的最大值 $1$。
        ==结论里的系数结构本身就在提示解法==：看到 $2a$ 就该想到"两个点的函数值相减"。

        **同族变式**：

        - 区间换成 $[0,2]$、$[-1,1]$，做法不变，只是 $g(c)$ 的最大值变了；
        - 若结论改成估计 $|f(c)|$，就把两式相==加==消掉 $f'(c)$；
        - 若已知 $|f'''|\le b$，就展开到二阶，代三个点解三元一次组合。

        **注意 $c\in(0,1)$ 是开区间**，所以严格地说 $(1-c)^2+c^2<1$，
        结论 $\le 2a+\frac b2$ 依然成立（而且不取等）。写 $\le$ 是安全的。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '四、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '泰勒题最容易丢分的地方', c: String.raw`
      1. **余项类型选错**：要放缩却用了皮亚诺余项，$o(\cdot)$ 没法估计，整题作废
         （[判据](#/calculus/derivative-app/taylor-proof?at=remainder)）。
      2. **不同点展开出来的 $\xi$ 混为一谈**：代 $x=1$ 和 $x=0$ 得到的是
         ==两个不同的 $\xi_1,\xi_2$==，必须分别记号，不能都写成 $\xi$。
      3. **忘记写 $\xi$ 的位置**："$\xi$ 介于 $x$ 与 $c$ 之间"这句是踩分点。
      4. **展开阶数贪多**：多展一阶就多出一个没有界的高阶导数，卡死在放缩那一步。
      5. **二次函数最值搞反**：$(1-c)^2+c^2$ 在中点取==最小==、在端点取==最大==，
         放缩要的是最大值。
    ` },

  ],
});
