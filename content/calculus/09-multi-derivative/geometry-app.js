/* ==========================================================================
   高等数学 / 9 多元函数微分学 / 几何应用：切平面与切线
   ========================================================================== */

KM.page({
  path: 'calculus/multi-derivative/geometry-app',
  title: '几何应用：切平面与切线',
  subtitle: '曲面求法向量、曲线求切向量 —— 两个对称的模块，剩下的全是点法式与对称式',
  tags: ['小题', '计算题', '数一', '高频'],
  updated: '2026-08-10',

  blocks: [

    { t: 'md', c: String.raw`
      这一章的几何应用只有两件事：**曲面找法向量 $\vec n$、曲线找切向量 $\vec\tau$**。
      找到之后，切平面 / 法平面用==点法式==，法线 / 切线用==对称式==，全是套公式。
      难点从来不在写方程，而在==题目不给切点==，要你从几何条件反推。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'surface', c: '一、曲面：切平面与法线' },

    { t: 'key', id: 'normal-implicit', title: '隐函数形式 $F(x,y,z)=0$（最通用）', c: String.raw`
      设 $F$ 在 $M(x_0,y_0,z_0)$ 邻域内有连续偏导数，且三个偏导数不同时为零，则

      $$\vec n=\left(F_x(M),\;F_y(M),\;F_z(M)\right)$$

      **切平面**（点法式）
      $$F_x(M)(x-x_0)+F_y(M)(y-y_0)+F_z(M)(z-z_0)=0$$

      **法线**（对称式）
      $$\frac{x-x_0}{F_x(M)}=\frac{y-y_0}{F_y(M)}=\frac{z-z_0}{F_z(M)}$$

      ==$\vec n$ 就是 $F$ 在该点的梯度 $\nabla F$== ——
      这不是巧合，见 [梯度与法向量的关系](#/calculus/multi-derivative/gradient?at=level-surface)。
    ` },

    { t: 'key', id: 'normal-explicit', title: '显函数形式 $z=f(x,y)$', c: String.raw`
      先移项化成隐函数：$F(x,y,z)=f(x,y)-z=0$。于是

      $$\vec n=\left(f_x(x_0,y_0),\;f_y(x_0,y_0),\;-1\right)$$

      ==那个 $-1$ 是 $F_z=\partial(f-z)/\partial z$ 来的，符号别丢==。
      写成 $(f_x,f_y,-1)$ 或 $(-f_x,-f_y,1)$ 都对（差一个负号，方向相反，同一条法线）。
    ` },

    { t: 'key', id: 'normal-param', title: '参数方程形式（数一，考频低）', c: String.raw`
      曲面 $x=x(u,v),\;y=y(u,v),\;z=z(u,v)$，记 $\vec r=(x,y,z)$，则

      $$\vec n=\frac{\partial\vec r}{\partial u}\times\frac{\partial\vec r}{\partial v}$$

      思路：固定 $v$ 变 $u$ 得一条曲线、固定 $u$ 变 $v$ 得另一条，
      两条切线都躺在切平面里，==它们的叉乘自然垂直于切平面==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'why-partial', c: '一·补、为什么是「对 $F$ 求偏导」' },

    { t: 'insight', id: 'why-not-zero', title: '我的错误想法：$F=0$ 两边求偏导，不该都是 0 吗？', c: String.raw`
      看到"把曲面写成 $F(x,y,z)=x^{2}+y^{2}-z=0$，然后 $F_x=2x,\;F_y=2y,\;F_z=-1$"时我懵了：

      > ==它不是整个式子等于 0 吗？两边同时求偏导，那偏导不就始终为零？==

      **错在哪**：我把 $F(x,y,z)=0$ 当成了**恒等式**。
      它不是 —— 它是一个**方程**，只在曲面那一层点上成立，
      空间里绝大多数点根本不满足它（比如 $(1,1,-1)$ 代进去得 $3$，不是 $0$）。

      而==两边求导这个操作，要求等式在一个邻域内处处成立==。
      曲面是二维的、在 $\mathbb R^{3}$ 里没有内部，所以对它"两边求偏导"这一步
      从一开始就不合法，得不出 $F_x=0$。

      **但我的直觉不是废的** —— 它只差一步就是定理本身，见下面那块。
    ` },

    { t: 'key', id: 'identity-vs-equation', title: '恒等式 vs 方程：一字之差', c: String.raw`
      | | 含义 | 能不能两边求导 |
      |---|---|---|
      | **恒等式** $F(x,y,z)\equiv 0$ | 对空间中==所有点==都成立 | 能，且确实推出 $F_x=F_y=F_z=0$ |
      | **方程** $F(x,y,z)=0$ | 只对==满足它的那些点==成立，这些点拼成曲面 | ==不能==，因为它在任何邻域内都不恒成立 |

      $F(x,y,z)=x^{2}+y^{2}-z$ 是一个==三元函数==，它在整个空间里有高低起伏，
      因此各方向的变化率（偏导数）客观存在，$F_x=2x$ 就是其中之一。

      $F=0$ 只是从这个函数里==挑出函数值为 0 的那一层点==（称为**等值面**）。
      我们求偏导的对象==始终是函数 $F$ 本身==，
      跟后面那个"$=0$"没有关系 —— ==写下 $F$ 之后就把 "$=0$" 忘掉==，
      把 $x,y,z$ 当成三个地位平等、互不相干的独立变量去求导。
    ` },

    { t: 'key', id: 'intuition-becomes-theorem', title: '你的直觉差一步就是定理：沿曲面求导确实为零', c: String.raw`
      "在曲面上 $F$ 一直等于 $0$，所以它的变化率是 $0$" —— ==这句话限制在"沿着曲面走"时是对的==，
      而且正是"梯度就是法向量"的证明。

      **证明**：任取曲面上一条过该点的曲线 $\vec r(t)=(x(t),y(t),z(t))$。
      因为曲线整条都在曲面上，所以
      $$F\!\left(x(t),y(t),z(t)\right)\equiv 0$$
      ==这才是真正的恒等式==（对一元变量 $t$ 在一个区间上处处成立），可以两边求导：
      $$F_x\,x'(t)+F_y\,y'(t)+F_z\,z'(t)=0
      \quad\Longleftrightarrow\quad \nabla F\cdot\vec\tau=0 .$$
      $\vec\tau$ 是曲面上任意方向的切向量，故 ==$\nabla F$ 垂直于所有切向量==，
      即垂直于切平面 —— 它就是法向量。$\blacksquare$

      所以正确的说法是：**沿曲面方向的方向导数为零**（这给出法向量），
      而不是"三个偏导数为零"（这是把方程误当恒等式）。
      同一件事的另一面见
      [梯度 = 等值面法向量](#/calculus/multi-derivative/gradient?at=level-surface)。
    ` },

    { t: 'compare',
      id: 'two-paths',
      title: '两条合法路径，结论相同',
      cols: ['', '路径 A：梯度法', '路径 B：隐函数法'],
      rows: [
        ['怎么看 $z$', '$x,y,z$ ==三个独立变量==', '$z=z(x,y)$，==$z$ 是因变量=='],
        ['做什么', '构造 $F(x,y,z)$，求 $\\nabla F$', '对 $F=0$ 两边关于 $x$ 求导（隐函数求导）'],
        ['本题得到', '$\\nabla F=(2x,\\,2y,\\,-1)$', '$2x-\\dfrac{\\partial z}{\\partial x}=0\\Rightarrow z_x=2x$，同理 $z_y=2y$'],
        ['法向量', '$(F_x,F_y,F_z)$', '$(z_x,\\,z_y,\\,-1)$'],
        ['本题结果', '$(2x,2y,-1)$', '$(2x,2y,-1)$ ==完全一样=='],
      ] },

    { t: 'md', c: String.raw`
      两条路径永远给出==同一个方向==，因为由隐函数求导公式
      $z_x=-\dfrac{F_x}{F_z}$、$z_y=-\dfrac{F_y}{F_z}$，有
      $$\left(z_x,\;z_y,\;-1\right)
      =-\frac{1}{F_z}\left(F_x,\;F_y,\;F_z\right)
      =-\frac{1}{F_z}\,\nabla F .$$
      差的只是一个非零倍数 $-\frac1{F_z}$，==法向量本来就允许差倍数==。
      这也顺带解释了[显函数公式里那个 $-1$](#/calculus/multi-derivative/geometry-app?at=normal-explicit)
      是从哪来的。

      **实战建议**：优先走路径 A。构造 $F$ 求梯度不需要判断谁是自变量、
      也不会出现分母 $F_z=0$ 的麻烦（例如球面在赤道处 $F_z=0$，路径 B 直接失效，路径 A 照常）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'curve', c: '二、曲线：切线与法平面' },

    { t: 'key', id: 'tangent-param', title: '参数方程形式（首选）', c: String.raw`
      曲线 $x=x(t),\;y=y(t),\;z=z(t)$，切点对应 $t_0$，则

      $$\vec\tau=\left(x'(t_0),\;y'(t_0),\;z'(t_0)\right)$$

      **切线**
      $$\frac{x-x_0}{x'(t_0)}=\frac{y-y_0}{y'(t_0)}=\frac{z-z_0}{z'(t_0)}$$

      **法平面**
      $$x'(t_0)(x-x_0)+y'(t_0)(y-y_0)+z'(t_0)(z-z_0)=0$$

      若曲线给成 $y=y(x),\;z=z(x)$，就把 $x$ 当参数，$\vec\tau=(1,y'(x_0),z'(x_0))$。
    ` },

    { t: 'key', id: 'tangent-intersect', title: '一般方程形式（交面式）', c: String.raw`
      曲线由两曲面相交而成
      $$\begin{cases}F(x,y,z)=0\\ G(x,y,z)=0\end{cases}$$

      交线躺在两张曲面上，所以它的切向量==同时垂直于两个法向量==，取叉乘：

      $$\vec\tau=\vec n_1\times\vec n_2=
      \begin{vmatrix}\vec i&\vec j&\vec k\\ F_x&F_y&F_z\\ G_x&G_y&G_z\end{vmatrix}$$

      ==先代切点坐标、再算行列式==，比先展开成符号式子再代入省一半力气。
    ` },

    { t: 'compare',
      id: 'duality',
      title: '两个模块是对称的，记一半即可',
      cols: ['', '曲面', '曲线'],
      rows: [
        ['核心向量', '法向量 $\\vec n$', '切向量 $\\vec\\tau$'],
        ['向量怎么来', '偏导数（梯度）', '对参数求导，或两法向量叉乘'],
        ['用点法式写的是', '==切==平面', '==法==平面'],
        ['用对称式写的是', '法线', '切线'],
        ['一句话', '$\\vec n$ 垂直于曲面', '$\\vec\\tau$ 沿着曲线'],
      ] },

    /* ================================================================== */
    { t: 'h', id: 'conditions', c: '三、几何条件 → 数学约束（本章真正的考点）' },

    { t: 'md', c: String.raw`
      考题极少直接给切点，而是给一句几何关系，要你翻译成对 $\vec n$ 或 $\vec\tau$ 的约束，
      再解出切点。==把下面这张表背成条件反射，这一章就没有难题了。==
    ` },

    { t: 'compare',
      id: 'condition-map',
      title: '条件识别与映射',
      cols: ['题目给的几何条件', '翻译成什么'],
      rows: [
        ['切平面**平行**于已知平面 $Ax+By+Cz+D=0$',
         '$\\vec n=(F_x,F_y,F_z)$ 与 $(A,B,C)$ ==共线==，即 $\\frac{F_x}{A}=\\frac{F_y}{B}=\\frac{F_z}{C}$'],
        ['切平面**垂直**于某直线', '$\\vec n$ 与直线方向向量 $\\vec s$ ==共线=='],
        ['切平面**平行**于某直线', '$\\vec n\\cdot\\vec s=0$（垂直，不是共线）'],
        ['切线**平行**于某直线', '$\\vec\\tau$ 与 $\\vec s$ ==共线=='],
        ['切平面**过**某已知点或某已知直线',
         '把点坐标代入含参的切平面方程，得到关于切点的约束（见[例题](#/calculus/multi-derivative/geometry-app?at=ex-through-points)）'],
        ['求曲面上到某平面**距离最短 / 最长**的点',
         '找切平面与该平面==平行==的切点，再用点到平面距离公式比较'],
        ['两曲面**正交**（交角为直角）', '两法向量点乘为零：$\\vec n_1\\cdot\\vec n_2=0$'],
      ] },

    { t: 'warn', id: 'parallel-vs-perp', title: '「平行」和「垂直」最容易译反', c: String.raw`
      注意区分说的是**平面**还是**平面的法向量**：

      - 切平面 $\parallel$ 已知平面 $\;\Longrightarrow\;$ ==两个法向量共线==；
      - 切平面 $\perp$ 已知平面 $\;\Longrightarrow\;$ ==两个法向量垂直==（点乘为零）；
      - 切平面 $\parallel$ 已知直线 $\;\Longrightarrow\;$ ==法向量 $\perp$ 方向向量==；
      - 切平面 $\perp$ 已知直线 $\;\Longrightarrow\;$ ==法向量 $\parallel$ 方向向量==。

      ==平面之间的关系，到了法向量层面就要反过来一次。== 这里错一次整道题全废。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'cross-product', c: '四、叉乘与三阶行列式：别在这里翻车' },

    { t: 'method', id: 'cross-cover', title: '叉乘的「遮盖法」，不写 $\\vec i,\\vec j,\\vec k$', c: String.raw`
      $\vec a=(a_1,a_2,a_3)$，$\vec b=(b_1,b_2,b_3)$，直接机械写出三个分量：

      | 分量 | 做法 | 结果 |
      |---|---|---|
      | 第 1 个 | 遮住第一列，算剩下的二阶行列式 | $a_2b_3-a_3b_2$ |
      | 第 2 个 | 遮住第二列，==必须变号== | $-(a_1b_3-a_3b_1)=a_3b_1-a_1b_3$ |
      | 第 3 个 | 遮住第三列 | $a_1b_2-a_2b_1$ |

      $$\vec a\times\vec b=\left(a_2b_3-a_3b_2,\;a_3b_1-a_1b_3,\;a_1b_2-a_2b_1\right)$$

      ==中间那一项的负号是全部错误的来源。== 记「正负正」比记完整行列式快得多。
    ` },

    { t: 'method', id: 'det-reduce', title: '三阶行列式：造零降阶，不要对角线法则', c: String.raw`
      交面式切向量常出现数字杂乱的三阶行列式。对角线法则要展开六项、正负交错，容错率极低。

      **稳妥做法**：

      1. 先扫一眼有没有 $0$ 多的行或列；
      2. 用「某行加上另一行的若干倍」==再制造几个 $0$==（行列式的值不变）；
      3. 按那一行 / 列展开成二阶行列式（代数余子式），只需算一两项。

      代数余子式的符号是棋盘格 $\begin{pmatrix}+&-&+\\-&+&-\\+&-&+\end{pmatrix}$，
      比六项展开好记。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '五、例题' },

    { t: 'example',
      id: 'ex-through-points',
      title: '切平面过两个已知点：设切点 → 列约束 → 解出来',
      source: '选择题',
      level: 2,
      problem: String.raw`
        过点 $(1,0,0)$、$(0,1,0)$，且与曲面 $z=x^{2}+y^{2}$ 相切的平面为（　　）。

        $\text{A}\quad z=0$ 与 $x+y-z=1$ $\qquad$ $\text{B}\quad z=0$ 与 $2x+2y-z=2$

        $\text{C}\quad x=y$ 与 $x+y-z=1$ $\qquad$ $\text{D}\quad x=y$ 与 $2x+2y-z=2$
      `,
      idea: String.raw`
        **这是"切平面过已知点"的标准模型**，链路是固定的：

        $$\text{设切点}\to\text{求}\vec n\to\text{写含参切平面}\to
        \text{代入已知点得约束}\to\text{解切点}\to\text{还原平面}$$

        关键认识：==切点是未知的，但切平面的形状完全由切点决定==，
        所以把切点设成 $(x_0,y_0,z_0)$ 之后，"过某点"就变成了关于 $x_0,y_0,z_0$ 的方程。
        再加上"切点在曲面上"这个天然约束，方程个数就够了。

        **化简的窍门**：展开后出现的 $2x_0^{2}+2y_0^{2}$，
        用曲面方程 $z_0=x_0^{2}+y_0^{2}$ 换成 $2z_0$，常数项立刻缩成 $-z_0$，
        整个式子变得非常干净。==不做这一步会多算好几行。==

        **客观题可以更快**：先看选项里的平面过不过那两个点。
        $x=y$ 不过 $(1,0,0)$，==C、D 直接排除==；A、B 只差常数项，验证一个即可。
      `,
      solution: String.raw`
        **第一步：设切点，写法向量。**

        设切平面与曲面的切点为 $M(x_0,y_0,z_0)$，则切点在曲面上：
        $$z_0=x_0^{2}+y_0^{2}. \qquad \text{①}$$

        令 $F(x,y,z)=x^{2}+y^{2}-z$，则 $F_x=2x$，$F_y=2y$，$F_z=-1$，故
        $$\vec n=(2x_0,\;2y_0,\;-1).$$

        **第二步：写含参的切平面方程。**

        由点法式
        $$2x_0(x-x_0)+2y_0(y-y_0)-(z-z_0)=0,$$
        展开整理
        $$2x_0x+2y_0y-z-\left(2x_0^{2}+2y_0^{2}-z_0\right)=0 .$$
        由①得 $2x_0^{2}+2y_0^{2}=2z_0$，故常数项为 $2z_0-z_0=z_0$，切平面化简为
        $$2x_0x+2y_0y-z-z_0=0. \qquad \text{②}$$

        **第三步：代入两个已知点。**

        代入 $(1,0,0)$：$\;2x_0-z_0=0\;\Rightarrow\;2x_0=z_0$；　③
        代入 $(0,1,0)$：$\;2y_0-z_0=0\;\Rightarrow\;2y_0=z_0$。　④

        **第四步：解出切点。**

        由③④得 $x_0=y_0=\dfrac{z_0}{2}$，代回①：
        $$z_0=\left(\frac{z_0}{2}\right)^{2}+\left(\frac{z_0}{2}\right)^{2}=\frac{z_0^{2}}{2}
        \;\Longrightarrow\; z_0^{2}-2z_0=0 ,$$
        解得 $z_0=0$ 或 $z_0=2$：

        - $z_0=0\Rightarrow x_0=y_0=0$，切点 $(0,0,0)$；
        - $z_0=2\Rightarrow x_0=y_0=1$，切点 $(1,1,2)$。

        **第五步：还原平面。** 分别代回②：

        - 切点 $(0,0,0)$：$\;-z=0$，即 $\;z=0$；
        - 切点 $(1,1,2)$：$\;2x+2y-z-2=0$，即 $\;2x+2y-z=2$。

        故所求为 $z=0$ 与 $2x+2y-z=2$，选 ==**B**==。
      `,
      comment: String.raw`
        **为什么 A 是错的（干扰项的设计）**：$x+y-z=1$ 确实同时过 $(1,0,0)$ 和 $(0,1,0)$，
        但它==不与曲面相切==。验算：把它写成 $z=x+y-1$，与②对照需 $2x_0=1,\;2y_0=1,\;z_0=1$，
        即 $x_0=y_0=\frac12$，但此时 $x_0^{2}+y_0^{2}=\frac12\neq 1=z_0$，与①矛盾。

        ==「过两点」的平面有无穷多，"相切"才是真正的约束==。
        只验证过点、不验证相切，就会掉进 A。

        **同族变式**：把"过两点"换成"过一条直线"，就在直线上取两个点，做法完全相同；
        换成"平行于某平面"，则改用[共线条件](#/calculus/multi-derivative/geometry-app?at=condition-map)，
        方程反而更少。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '六、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **显函数的 $-1$ 丢了**：$z=f(x,y)$ 的法向量是 $(f_x,f_y,==-1==)$，不是 $(f_x,f_y)$。
      2. **平行 / 垂直译反**：见[上面那条](#/calculus/multi-derivative/geometry-app?at=parallel-vs-perp)。
      3. **叉乘中间项忘记变号**：正负正。
      4. **偏导数没代切点就往方程里塞**：$\vec n$ 里必须是==数字==，不能留着 $x,y,z$。
      5. **只验证"过点"、不验证"相切"**：本题的 A 就是这么设计的。
      6. **法线写对称式时分母为零**：某个偏导数为 $0$ 时，对称式要写成
         $\frac{x-x_0}{0}$ 的形式（约定记法，表示 $x=x_0$），不能直接丢掉那一项。
    ` },

  ],
});
