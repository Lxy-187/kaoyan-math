/* ==========================================================================
   概率论 / 3 多维随机变量及其分布 / 联合分布、边缘分布与条件分布
   —— 本章地基。联合是全部信息，边缘是投影，条件是切片。
      独立性判定见 multi-random-var/independence。
   ========================================================================== */

KM.page({
  path: 'probability/multi-random-var/joint',
  title: '联合分布、边缘分布与条件分布',
  subtitle: '联合是**全部信息**，边缘是把它压扁的**投影**，条件是切出来的**切片**——三者的关系决定整章',
  tags: ['大题', '计算题', '高频'],
  updated: '2026-08-20',

  blocks: [

    { t: 'key', id: 'joint-cdf', title: '联合分布函数：左下角的概率', c: String.raw`
      $$\boxed{\ F(x,y)=P(X\le x,\ Y\le y)\ }$$
      其中逗号表示==两个事件同时发生==（即交事件）。

      **几何含义**：$F(x,y)$ 是随机点 $(X,Y)$ 落在
      ==以 $(x,y)$ 为右上角的那个无限大左下方区域==里的概率。

      **四条性质**：

      1. 对 $x$ 和对 $y$ ==分别==单调不减；
      2. 对 $x$ 和对 $y$ ==分别==右连续；
      3. $F(-\infty,y)=F(x,-\infty)=F(-\infty,-\infty)=0$，$F(+\infty,+\infty)=1$；
      4. **矩形不等式**（一维没有的新东西）：对任意 $x_1<x_2$、$y_1<y_2$，
         $$F(x_2,y_2)-F(x_1,y_2)-F(x_2,y_1)+F(x_1,y_1)\ge0.$$

      ==第 4 条正是"矩形区域的概率非负"==：
      $$P(x_1<X\le x_2,\ y_1<Y\le y_2)=F(x_2,y_2)-F(x_1,y_2)-F(x_2,y_1)+F(x_1,y_1)$$
      这个"加减加减"的模式就是[容斥原理](#/probability/events/operations?at=addition)在二维的样子：
      ==减两次大矩形，重叠的小角落被减了两遍，要补回来==。

      **前三条不能保证是分布函数**，必须有第 4 条。
      考研的选择题会给一个满足 1~3 却违反 4 的函数让你判断，==这是唯一的高阶考点==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'two-types', c: '一、两种类型的联合分布' },

    { t: 'key', id: 'discrete-joint', title: '离散型：一张联合分布表', c: String.raw`
      $$p_{ij}=P(X=x_i,\ Y=y_j),\qquad \sum_i\sum_j p_{ij}=1$$

      写成表格最直观：

      | $X\backslash Y$ | $y_1$ | $y_2$ | $\cdots$ | ==行和== |
      |---|---|---|---|---|
      | $x_1$ | $p_{11}$ | $p_{12}$ | $\cdots$ | $p_{1\cdot}$ |
      | $x_2$ | $p_{21}$ | $p_{22}$ | $\cdots$ | $p_{2\cdot}$ |
      | ==列和== | $p_{\cdot1}$ | $p_{\cdot2}$ | $\cdots$ | $1$ |

      ==边缘分布就是表格的行和与列和==——
      这是"边缘"这个名字的由来：它们被写在表格的边上。

      **求任何概率的办法只有一条**：==把满足条件的格子挑出来，概率相加==。
      比如 $P(X<Y)$ 就是把所有 $x_i<y_j$ 的格子加起来，
      $P(X+Y=2)$ 就是把所有和为 $2$ 的格子加起来。
      ==画个表、圈出格子，比列式子可靠。==
    ` },

    { t: 'key', id: 'continuous-joint', title: '连续型：联合密度与二重积分', c: String.raw`
      若存在 $f(x,y)\ge0$ 使得
      $$F(x,y)=\int_{-\infty}^{x}\!\!\int_{-\infty}^{y}f(u,v)\,\d v\,\d u,$$
      则称 $(X,Y)$ 为连续型，$f$ 为**联合密度**。

      **两条充要条件**：$f\ge0$ 且 $\displaystyle\iint_{\R^{2}}f\dxy=1$。

      **核心公式**：对平面上任何区域 $D$，
      $$\boxed{\ P\bigl((X,Y)\in D\bigr)=\iint_{D}f(x,y)\dxy\ }$$

      ==这一行把概率论的这一章直接变成了重积分==：
      剩下的全部工作是"画区域、定积分限"。
      所以本章的难点其实在高数——见[二重积分的换序与坐标选择](#/calculus/multi-integral/triple?at=projection-vs-section)
      那一套画图定限的功夫。

      **在 $f$ 的连续点处**：$f(x,y)=\dfrac{\partial^{2}F}{\partial x\,\partial y}$。
      ==混合偏导数的顺序无所谓==，这也是矩形不等式的微分版本。
    ` },

    { t: 'key', id: 'uniform-2d', title: '二维均匀分布：几何概型的正式写法', c: String.raw`
      设区域 $D$ 的面积为 $S_D>0$，称 $(X,Y)$ 在 $D$ 上服从**均匀分布**，若
      $$f(x,y)=\begin{cases}\dfrac{1}{S_D},&(x,y)\in D\\[4pt] 0,&\text{其他}\end{cases}$$

      此时对任何 $A\subset D$，
      $$P\bigl((X,Y)\in A\bigr)=\frac{S_A}{S_D},$$
      ==这就是第 1 章的[几何概型](#/probability/events/operations?at=geometric-prob)==，
      只是现在有了随机变量的语言。

      **两个必须知道的事实**：

      1. ==均匀分布的边缘一般不是均匀分布==。
         只有 $D$ 是**矩形**（边平行于坐标轴）时，边缘才均匀，$X,Y$ 才独立。
         $D$ 是圆盘时，$f_X(x)=\frac{2\sqrt{R^{2}-x^{2}}}{\pi R^{2}}$，==是个半圆形，不均匀==。
      2. ==条件分布一定是均匀的==：给定 $X=x$，
         $Y$ 在"竖直切线与 $D$ 的交集"上均匀分布。
         这一条能省掉大量计算，见[条件密度](#/probability/multi-random-var/joint?at=conditional-density)。

      **考研最爱的两个 $D$**：单位圆盘、由 $y=x$ 和坐标轴围成的三角形。
      ==三角形一定不独立==（支撑不是矩形），这是送分的判断题。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'marginal-sec', c: '二、边缘分布：把另一个变量「加掉」' },

    { t: 'key', id: 'marginal', title: '边缘分布的定义', c: String.raw`
      **离散型**：
      $$P(X=x_i)=\sum_j p_{ij}\quad(\text{行和}),\qquad P(Y=y_j)=\sum_i p_{ij}\quad(\text{列和})$$

      **连续型**：
      $$\boxed{\ f_X(x)=\int_{-\infty}^{+\infty}f(x,y)\dy,\qquad
      f_Y(y)=\int_{-\infty}^{+\infty}f(x,y)\dx\ }$$

      **由分布函数**：$F_X(x)=F(x,+\infty)$，$F_Y(y)=F(+\infty,y)$。

      ==一句话记法：要谁的边缘，就把另一个变量积掉（或加掉）。==
      $f_X$ 里==不能出现 $y$==，出现了就是积分限或者被积函数写错了。
      这是最快的自检。
    ` },

    { t: 'method', id: 'marginal-integrate', title: '定积分限：横着切一刀，竖着切一刀', c: String.raw`
      求边缘密度时，==全部难点在积分限==，而积分限由**支撑区域 $D$** 决定。

      **求 $f_X(x)$ 的动作**：

      1. 在图上==画一条竖直线 $X=x$==（固定 $x$）；
      2. 看这条线和 $D$ 相交的那一段，==它的上下端就是 $y$ 的积分限==；
      3. 再看这条竖直线在哪个范围内才与 $D$ 有交，==那就是 $f_X$ 的定义域==。

      求 $f_Y(y)$ 就换成==水平线==，同理。

      **第 3 步最容易漏**：算出表达式后必须写清 $x$ 的范围，
      范围外 $f_X(x)=0$。==不写范围等于没做完。==

      **一个能救命的检验**：
      $$\int f_X(x)\dx=1.$$
      积分限错、范围错，==这一步都会立刻暴露==。

      **实操提示**：$D$ 若是三角形或曲边区域，==积分限里必然含 $x$==
      （比如 $\int_0^{x}$），这正是边缘不再均匀、$X$ 与 $Y$ 不独立的根源。
      积分限里==是否含另一个变量==，是判断独立性的第一眼线索，
      详见[矩形支撑判据](#/probability/multi-random-var/independence?at=rect-support)。
    ` },

    { t: 'warn', id: 'marginal-loses', title: '边缘留不住联合：投影会丢信息', c: String.raw`
      $$\text{联合}\ \Longrightarrow\ \text{边缘}\qquad\text{但}\qquad
      \text{边缘}\ \not\Longrightarrow\ \text{联合}$$

      **一个最小的反例**：设 $X,Y$ 都只取 $0,1$，两个边缘都是"各 $\frac12$"。
      下面两张联合分布表的行和、列和==完全相同==，联合分布却截然不同：

      | 甲：$X,Y$ 独立 | $Y=0$ | $Y=1$ |
      |---|---|---|
      | $X=0$ | $\frac14$ | $\frac14$ |
      | $X=1$ | $\frac14$ | $\frac14$ |

      | 乙：$X\equiv Y$ | $Y=0$ | $Y=1$ |
      |---|---|---|
      | $X=0$ | $\frac12$ | $0$ |
      | $X=1$ | $0$ | $\frac12$ |

      甲中 $X,Y$ 相互独立，乙中 $Y$ ==完全由 $X$ 决定==——
      两者的相关性天差地别，边缘却一模一样。

      **这个"信息损失"贯穿整章**：

      - 边缘正态==推不出==联合正态，见[反例](#/probability/multi-random-var/normal-2d?at=marginal-not-enough)；
      - 知道 $f_X$ 与 $f_Y$ ==算不出== $P(X<Y)$、$\E(XY)$；
      - ==只有加上"独立"这个条件==，才能由边缘反推联合：$f=f_Xf_Y$。

      ==所以"独立"的真正价值是：它是唯一能把投影还原成原物的假设。==
    ` },

    /* ================================================================== */
    { t: 'h', id: 'conditional-sec', c: '三、条件分布：切一刀再归一化' },

    { t: 'key', id: 'conditional-dist', title: '离散型的条件分布律', c: String.raw`
      在 $P(X=x_i)>0$ 的前提下，
      $$P(Y=y_j\mid X=x_i)=\frac{p_{ij}}{p_{i\cdot}}$$

      ==就是把联合分布表的第 $i$ 行取出来，除以该行的行和==。
      除法的作用是**重新归一化**——让这一行的概率重新加成 $1$，
      与[条件概率的定义](#/probability/events/conditional?at=def-conditional)完全一致。

      **每一行都给出一个新的分布**，行数不同，条件分布也不同。
      ==如果所有行归一化之后完全相同，那就说明 $X$ 与 $Y$ 独立==——
      "$X$ 取什么值，对 $Y$ 的分布没有影响"。
    ` },

    { t: 'key', id: 'conditional-density', title: '条件密度：连续型的切片', c: String.raw`
      在 $f_X(x)>0$ 处定义
      $$\boxed{\ f_{Y\mid X}(y\mid x)=\frac{f(x,y)}{f_X(x)}\ }$$

      **它是一个关于 $y$ 的密度**（$x$ 只是参数），可以验证
      $\int f_{Y\mid X}(y\mid x)\dy=\frac{1}{f_X(x)}\int f(x,y)\dy=1$。

      **几何解释**：在联合密度的曲面上==沿 $X=x$ 竖直切一刀==，
      得到一条曲线 $f(x,\cdot)$；这条曲线的面积恰好是 $f_X(x)$，
      ==除以它就是把这一刀的截面重新标定成一个密度==。

      $$\underbrace{f(x,y)}_{\text{切面的高度}}\ \Big/\ \underbrace{f_X(x)}_{\text{切面的总面积}}$$

      **反过来用（乘法公式的连续版）**：
      $$f(x,y)=f_X(x)\,f_{Y\mid X}(y\mid x)$$
      ==题目若给"$X$ 的分布"和"给定 $X=x$ 时 $Y$ 的分布"，就用这条拼出联合密度==，
      这是大题里非常常见的开局。

      **注意 $P(X=x)=0$ 却还能谈"给定 $X=x$"**：
      条件密度是通过极限定义的，==不能用条件概率的定义式直接除==。
      这是概念题的一个考点。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '四、例题' },

    { t: 'example',
      id: 'ex-marginal',
      title: '三角形支撑：边缘与条件的完整一套',
      source: '标准例题',
      level: 3,
      problem: String.raw`
        设 $(X,Y)$ 的联合密度为
        $$f(x,y)=\begin{cases}8xy,&0<y<x<1\\0,&\text{其他}\end{cases}$$

        1. 验证归一性；
        2. 求边缘密度 $f_X(x)$、$f_Y(y)$；
        3. 求条件密度 $f_{Y\mid X}(y\mid x)$，并求 $P\left(Y<\frac14\ \middle|\ X=\frac12\right)$。
      `,
      idea: String.raw`
        **第一件事是画图**：$0<y<x<1$ 是以 $(0,0),(1,0),(1,1)$ 为顶点的
        ==直角三角形（在直线 $y=x$ 的下方）==。
        不画这个图，后面所有积分限都是猜的。

        **求 $f_X(x)$**：竖着切一刀。固定 $x\in(0,1)$，
        竖直线与三角形相交的部分是 $0<y<x$，==上限是 $x$ 而不是 $1$==。
        这个"上限含 $x$"就是三角形区域的标志。

        **求 $f_Y(y)$**：横着切一刀。固定 $y\in(0,1)$，
        水平线与三角形相交的部分是 $y<x<1$，==这次是下限含 $y$==。
        ==两个边缘的积分限"一个含变量在上、一个含变量在下"，别抄串了。==

        **第 3 问**：条件密度算出来会含参数 $x$，
        代入 $x=\frac12$ 得到一个具体的密度，再在 $(0,\frac14)$ 上积分。
        ==注意条件密度的定义域也随 $x$ 变化==（这里是 $0<y<x$），
        代入 $x=\frac12$ 后是 $0<y<\frac12$，而 $\frac14$ 落在里面，正常积分。
      `,
      solution: String.raw`
        **(1) 归一性**：
        $$\iint f\dxy=\int_0^1\!\!\left(\int_0^{x}8xy\dy\right)\dx
        =\int_0^1 8x\cdot\frac{x^{2}}{2}\dx=\int_0^1 4x^{3}\dx=1.\ \checkmark$$

        **(2) 边缘密度**：

        竖直切（$0<x<1$）：
        $$f_X(x)=\int_0^{x}8xy\dy=8x\cdot\frac{x^{2}}{2}=4x^{3},\qquad 0<x<1,$$
        其余为 $0$。

        水平切（$0<y<1$）：
        $$f_Y(y)=\int_y^{1}8xy\dx=8y\cdot\frac{1-y^{2}}{2}=4y\bigl(1-y^{2}\bigr),\qquad 0<y<1,$$
        其余为 $0$。

        **检验**：$\int_0^1 4x^{3}\dx=1\ \checkmark$；
        $\int_0^1(4y-4y^{3})\dy=2-1=1\ \checkmark$

        **(3) 条件密度**：对 $0<x<1$，
        $$f_{Y\mid X}(y\mid x)=\frac{8xy}{4x^{3}}=\frac{2y}{x^{2}},\qquad 0<y<x,$$
        其余为 $0$。

        取 $x=\frac12$：$f_{Y\mid X}\!\left(y\mid\frac12\right)=8y$（$0<y<\frac12$）。故
        $$P\!\left(Y<\frac14\ \middle|\ X=\frac12\right)=\int_0^{1/4}8y\dy
        =4y^{2}\Big|_0^{1/4}=\boxed{\frac14}.$$
      `,
      comment: String.raw`
        **一眼看出不独立**：支撑区域是三角形而不是矩形，
        所以 $X,Y$ ==必然不独立==，
        见[矩形支撑判据](#/probability/multi-random-var/independence?at=rect-support)。
        验证一下：$f_X(x)f_Y(y)=4x^{3}\cdot4y(1-y^{2})\ne8xy$。

        **条件密度的形状值得体会**：给定 $X=x$ 后，
        $Y$ 的密度是 $\frac{2y}{x^{2}}$——==在 $(0,x)$ 上线性增长==，
        说明 $Y$ 更倾向于靠近 $x$。
        而且这个条件分布的形状==只依赖 $x$ 的缩放==：
        令 $t=y/x$ 则密度变成 $2t$（$0<t<1$），与 $x$ 无关。
        ==这类"自相似"结构在解题时能省掉重复计算。==

        **两个常见的追问**：

        - $P(X+Y<1)$：把 $D$ 与 $x+y<1$ 求交，画图后分块积分；
        - $\E(XY)$：$\iint xy\cdot8xy\dxy$，
          用[无意识统计学家法则](#/probability/moments/expectation?at=lotus)==不必先求 $XY$ 的分布==。

        **这道题的模板价值**：
        ==三角形支撑 + 多项式密度==是二维连续型大题最标准的形态，
        第 3、4 章的例题有一大半长这样。把这一套（画图、两次切、两次检验）练熟。
      `,
    },

    { t: 'example',
      id: 'ex-discrete-table',
      title: '联合分布表：由边缘反推格子',
      source: '标准例题',
      level: 2,
      problem: String.raw`
        设 $(X,Y)$ 的联合分布律部分已知（表中 $a,b$ 待定）：

        | $X\backslash Y$ | $0$ | $1$ | 行和 |
        |---|---|---|---|
        | $0$ | $0.1$ | $a$ | $0.4$ |
        | $1$ | $b$ | $0.2$ | |
        | 列和 | $0.5$ | | $1$ |

        求 $a,b$，并求 $P(Y=1\mid X=0)$ 与 $P(X=Y)$。
      `,
      idea: String.raw`
        **这类题不需要任何理论，只需要"表格的行和列和必须自洽"**：

        - 每一行的格子加起来等于行和；
        - 每一列的格子加起来等于列和；
        - 所有行和之和 $=$ 所有列和之和 $=1$。

        ==三组约束往往是超定的，正好可以互相检验==。
        动笔顺序：先填约束最紧的那一格（只缺一个未知数的行或列）。

        这里第一行只缺 $a$，第一列只缺 $b$，==两个都是一步到位==。
      `,
      solution: String.raw`
        **由第一行**：$0.1+a=0.4\Rightarrow a=0.3$。

        **由第一列**：$0.1+b=0.5\Rightarrow b=0.4$。

        **补全表格**（第二行行和 $=0.4+0.2=0.6$，第二列列和 $=0.3+0.2=0.5$）：

        | $X\backslash Y$ | $0$ | $1$ | 行和 |
        |---|---|---|---|
        | $0$ | $0.1$ | $0.3$ | $0.4$ |
        | $1$ | $0.4$ | $0.2$ | $0.6$ |
        | 列和 | $0.5$ | $0.5$ | $1$ |

        **检验**：$0.4+0.6=1$，$0.5+0.5=1$。$\checkmark$

        **条件概率**：
        $$P(Y=1\mid X=0)=\frac{P(X=0,Y=1)}{P(X=0)}=\frac{0.3}{0.4}=\boxed{0.75}.$$

        **$P(X=Y)$**：把对角线上的格子加起来，
        $$P(X=Y)=p_{00}+p_{11}=0.1+0.2=\boxed{0.3}.$$
      `,
      comment: String.raw`
        **顺带判个独立**：若 $X,Y$ 独立，应有 $p_{00}=P(X=0)P(Y=0)=0.4\times0.5=0.2$，
        而实际是 $0.1$，==故不独立==。
        离散型判独立就是==逐格验证 $p_{ij}=p_{i\cdot}p_{\cdot j}$==，
        ==只要有一格不满足就不独立==，见[离散判据](#/probability/multi-random-var/independence?at=criterion-discrete)。

        **两个条件分布对比着看**：

        - 给定 $X=0$：$Y$ 取 $0,1$ 的概率为 $0.25,\ 0.75$；
        - 给定 $X=1$：$Y$ 取 $0,1$ 的概率为 $\frac{0.4}{0.6},\ \frac{0.2}{0.6}$，即 $\frac23,\frac13$。

        ==两行归一化后不同，这就是"不独立"的直观表现==：
        $X$ 的取值确实改变了对 $Y$ 的判断。

        **常见追问 $P(X+Y=1)$**：把 $x_i+y_j=1$ 的格子加起来，
        即 $p_{01}+p_{10}=0.3+0.4=0.7$。
        ==离散型的一切问题都是"圈格子求和"==，别把它想复杂。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '五、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **求边缘不写定义域**：$f_X(x)$ 必须注明 $x$ 在哪个范围有值，
         ==不写等于没做完==。
      2. **$f_X(x)$ 里残留 $y$**：一定是积分限或被积函数写错了，==这是最快的自检==。
      3. **积分限抄串**：求 $f_X$ 时限是 $y$ 的范围（可以含 $x$），
         求 $f_Y$ 时限是 $x$ 的范围（可以含 $y$）。==画图，切线，别硬记。==
      4. **不画支撑区域**：非矩形区域下所有积分限都依赖图形。
      5. **由边缘反推联合**：==除非独立，否则不成立==，
         见[投影会丢信息](#/probability/multi-random-var/joint?at=marginal-loses)。
      6. **矩形概率算成两项相减**：二维要==四项加减==，
         见[矩形不等式](#/probability/multi-random-var/joint?at=joint-cdf)。
      7. **条件密度忘记定义域随 $x$ 变**：三角形支撑下 $f_{Y\mid X}(y\mid x)$ 的
         有效范围是 $0<y<x$，==不是整条实轴==。
      8. **离散型忘了检验行列和**：填表题里这是免费的验算。
      9. **认为 $P(X=x)=0$ 就不能谈条件分布**：连续型的条件密度另有定义，
         ==不能用条件概率的定义式去除==。
    ` },

  ],
});
