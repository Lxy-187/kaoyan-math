/* ==========================================================================
   概率论 / 2 一维随机变量及其分布 / 分布函数与密度函数
   —— 分布函数 F 是唯一"通吃"离散、连续、混合三种类型的工具。
      具体分布见 random-var/distributions；求函数的分布见 function-of-rv。
   ========================================================================== */

KM.page({
  path: 'probability/random-var/cdf-pdf',
  title: '分布函数与密度函数',
  subtitle: '$F(x)=P(X\\le x)$ 是**唯一通吃三种类型**的描述方式；密度只是它的导数，而且**不是概率**',
  tags: ['小题', '计算题', '概念辨析', '易错'],
  updated: '2026-08-20',

  blocks: [

    { t: 'key', id: 'cdf-def', title: '定义：注意那个小于等于号', c: String.raw`
      $$\boxed{\ F(x)=P(X\le x),\qquad x\in\R\ }$$

      ==这里是"小于等于"，不是"小于"==。
      这个约定看似无关紧要，却决定了 $F$ 是**右连续**而不是左连续，
      也决定了离散型分布函数的阶梯在跳跃点上取的是==上面那个值==。

      **为什么要引入 $F$**：
      离散型有分布律、连续型有密度，两者形式完全不同；
      而 $F$ ==对任何随机变量都有定义==，是唯一的公共语言。
      多维、混合型、随机变量的函数——凡是"不知道该用哪套"的场合，
      都从 $F$ 出发，这也是[分布函数法](#/probability/random-var/function-of-rv?at=cdf-method)
      成为万能方法的原因。

      **几何含义**：$F(x)$ 是"落在 $x$ 左边（含 $x$）的概率总量"，
      ==是一个从 $0$ 爬到 $1$ 的累积过程==。
    ` },

    { t: 'key', id: 'cdf-props', title: '三条性质，而且是充要的', c: String.raw`
      一个函数 $F(x)$ 是某个随机变量的分布函数，==当且仅当==它满足：

      1. **单调不减**：$x_1<x_2\Rightarrow F(x_1)\le F(x_2)$；
      2. **右连续**：$F(x+0)=F(x)$，对每个 $x$ 都成立；
      3. **两端的极限**：$F(-\infty)=0$，$F(+\infty)=1$。

      =="充要"这两个字是考点==：
      给一个分段函数问"能否作为分布函数"，或者给含参数的 $F$ 求参数，
      靠的就是逐条验证这三点。

      **由它们立得的推论**：$0\le F(x)\le1$，且 $F$ 至多有可数个间断点，
      间断点必为==跳跃间断==（跳跃高度就是那一点的概率）。

      **只右连续、不左连续**是最容易被忽略的一条。
      左极限 $F(x-0)$ 一般不等于 $F(x)$，两者之差正是
      $$\boxed{\ P(X=x)=F(x)-F(x-0)\ }$$
      ==这条公式把"点概率"从分布函数里读了出来==，是本页最有用的一行。
    ` },

    { t: 'compare',
      id: 'read-off',
      title: '用 $F$ 表示各种概率：一张对照表',
      cols: ['要求的概率', '用 $F$ 写', '备注'],
      rows: [
        ['$P(X\\le a)$', '$F(a)$', '定义'],
        ['$P(X>a)$', '$1-F(a)$', '对立事件'],
        ['$P(a<X\\le b)$', '$F(b)-F(a)$', '==左开右闭，最标准的一条=='],
        ['$P(X=a)$', '$F(a)-F(a-0)$', '跳跃高度'],
        ['$P(X<a)$', '$F(a-0)$', '==左极限，不是 $F(a)$=='],
        ['$P(X\\ge a)$', '$1-F(a-0)$', ''],
        ['$P(a\\le X\\le b)$', '$F(b)-F(a-0)$', ''],
        ['$P(a<X<b)$', '$F(b-0)-F(a)$', ''],
      ] },

    { t: 'warn', id: 'endpoint', title: '端点归属：离散型必须斤斤计较，连续型可以随意', c: String.raw`
      **连续型**下 $P(X=a)=0$，于是
      $$P(a<X<b)=P(a\le X\le b)=P(a<X\le b)=F(b)-F(a),$$
      ==四种写法完全一样，端点可以不管==。

      **离散型 / 混合型**下就完全不同。例如 $X$ 只取 $1,2,3$，各以 $\frac13$ 的概率：
      $$P(X\le2)=\frac23,\qquad P(X<2)=\frac13.$$
      ==差了一整个 $\frac13$。==

      **实操建议**：拿到题先判类型。

      - 看到密度、看到"连续型"字样 $\Rightarrow$ 端点随便；
      - 看到分布律、看到 $F$ 有跳跃 $\Rightarrow$ ==每个不等号都要盯着看==。

      混合型最阴险：它在某些点连续、在某些点跳跃，
      ==只有跳跃点处的不等号才要紧==。
    ` },

    { t: 'key', id: 'discrete-cdf', title: '离散型：分布律与阶梯型 $F$', c: String.raw`
      设 $X$ 取值 $x_1<x_2<\cdots$，$P(X=x_k)=p_k$，则
      $$F(x)=\sum_{x_k\le x}p_k$$
      图像是一条**阶梯**：在每个 $x_k$ 处向上跳 $p_k$，其余地方水平。

      **三个必须记住的细节**：

      1. 跳跃点处==取右端的值==（右连续），画图时该点是实心的下一段起点；
      2. 跳跃高度 $=$ 该点的概率；
      3. 台阶总高度为 $1$，即 $\sum_k p_k=1$——==求待定参数时几乎总是先用这一条==。

      **反向读图**：给一张阶梯图，
      ==每个跳跃点的横坐标就是取值，跳跃高度就是概率==，分布律就写完了。
      这是选择题的常见形式。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'continuous', c: '一、连续型与密度函数' },

    { t: 'key', id: 'pdf-def', title: '密度的定义：$F$ 是 $f$ 的变限积分', c: String.raw`
      若存在非负可积函数 $f$ 使得
      $$\boxed{\ F(x)=\int_{-\infty}^{x}f(t)\dt,\qquad \forall x\in\R\ }$$
      则称 $X$ 是**连续型**随机变量，$f$ 是它的**概率密度**。

      **由此立刻得到三条**：

      - $F$ ==处处连续==（变限积分必连续），所以 $P(X=a)=0$；
      - 在 $f$ 的连续点处 ==$F'(x)=f(x)$==（微积分基本定理）；
      - $$P(a<X\le b)=\int_a^b f(x)\dx,$$
        ==概率就是密度曲线下的面积==。

      **$f$ 不唯一**：在有限多个点上改动 $f$ 的值不影响任何积分，
      ==所以密度可以在个别点随便定义==（通常在分段点取 $0$ 或取左右值之一均可）。
      考试中不必纠结分段点上密度写成什么。
    ` },

    { t: 'warn', id: 'pdf-not-prob', title: '密度不是概率：$f(x)$ 可以大于 $1$', c: String.raw`
      ==$f(x)\ne P(X=x)$==。事实上连续型下恒有 $P(X=x)=0$。

      **$f(x)$ 是"概率的线密度"**，量纲是"概率 / 长度"：
      $$f(x)\,\d x\approx P(x<X\le x+\d x)$$
      只有乘上一小段长度才变成概率。
      所以 ==$f$ 完全可以取到很大的值==：
      $U(0,\frac{1}{100})$ 的密度恒等于 $100$。

      **对 $f$ 的全部要求只有两条**（也是充要条件）：
      $$f(x)\ge0,\qquad \int_{-\infty}^{+\infty}f(x)\dx=1.$$
      ==没有"$f\le1$"这一条==，这是选择题最爱设的错项。

      **配套的一个反直觉结论**：
      $P(X=a)=0$ 但事件 $\set{X=a}$ ==并非不可能==。
      "概率为零"与"不可能"在连续场合彻底分家，
      这一点在[几何概型](#/probability/events/operations?at=geometric-prob)那里就埋下了。
    ` },

    { t: 'method', id: 'find-constant', title: '求待定常数：按顺序试这三条', c: String.raw`
      题目给一个含参数的 $F$ 或 $f$，问参数取值。==按下面的顺序找方程，几乎不会漏==：

      1. **归一性**（最常用）：
         $$\int_{-\infty}^{+\infty}f\dx=1\qquad\text{或}\qquad \sum_k p_k=1.$$
      2. **两端极限**：$F(-\infty)=0$，$F(+\infty)=1$。
         ==含 $\arctan$、$e^{-x}$、$\frac{1}{1+x^2}$ 的 $F$ 基本都靠这两条定系数==。
      3. **连续性 / 右连续性**：
         连续型的 $F$ ==处处连续==，所以在分段点处左右两段的值必须相等——
         这条能一口气定下好几个系数。
         若是离散或混合型，则只要求==右连续==。

      **顺序的道理**：第 2、3 条给出的是**线性方程**，往往能一眼解出；
      第 1 条要算积分，放在最后。
      ==两个未知数就找两个方程，别只用归一性硬凑。==

      **算完务必回代检查** $f\ge0$：
      有时方程解出两个根，只有一个能保证密度非负。
    ` },

    { t: 'key', id: 'mixed', title: '混合型：既有跳跃又有连续段', c: String.raw`
      $F$ 既不是纯阶梯、也不是处处连续，而是==连续段 + 若干跳跃==。

      **它从哪来**：最常见的来源是==对连续型随机变量做了"截断"==。
      例如 $X\sim U(0,2)$，令 $Y=\min(X,1)$，则
      $$P(Y=1)=P(X\ge1)=\tfrac12,$$
      $Y$ 在 $[0,1)$ 上有密度，在 $y=1$ 处有一个高度 $\frac12$ 的跳跃。
      ==排队问题的"等待时间"、保险的"赔付额"都是这种结构。==

      **处理原则**：==混合型不要去找密度==（它没有），
      一律用分布函数做，需要概率时用[对照表](#/probability/random-var/cdf-pdf?at=read-off)去读。

      $$\underbrace{F(a)-F(a-0)}_{\text{离散部分：跳跃}}\ +\ \underbrace{\int f}_{\text{连续部分：面积}}\ =\ 1$$

      **考研里的典型问法**："求 $Y$ 的分布函数"——
      ==答案必然是一个分段写的、在某点跳跃的函数==，
      如果你算出来处处连续，多半是漏掉了那一块被"压扁"的概率。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '二、例题' },

    { t: 'example',
      id: 'ex-cdf-from-pdf',
      title: '由密度求分布函数：分段积分的标准写法',
      source: '标准例题',
      level: 2,
      problem: String.raw`
        设随机变量 $X$ 的密度为
        $$f(x)=\begin{cases}kx,&0\le x<1\\[2pt]2-x,&1\le x<2\\[2pt]0,&\text{其他}\end{cases}$$

        1. 求常数 $k$；
        2. 求 $F(x)$；
        3. 求 $P(0.5<X<1.5)$。
      `,
      idea: String.raw`
        **第 1 问**用归一性。注意 $f$ 分了三段，
        ==积分要按段拆开==，其中 $0$ 段不用算。

        **第 2 问是本题的重点**，关键在于理解 $F(x)=\int_{-\infty}^{x}f$
        中的积分上限 $x$ ==会扫过不同的段==，所以 $F$ 也必须分段写，
        且==段数与 $f$ 相同==（这里是四段：$x<0$、$0\le x<1$、$1\le x<2$、$x\ge2$）。

        写第三段时最容易出错：积分要从 $-\infty$ 一路累到 $x$，
        ==前面第二段的面积必须整块加上==，不能只积当前这一段。
        实操上就是：**先把上一段的终值当作本段的起点**。

        **第 3 问**有两条路：用 $F$ 相减（推荐，因为 $F$ 已经算好），
        或者直接积分。==连续型不必管端点开闭==。
      `,
      solution: String.raw`
        **(1)** 由归一性
        $$\int_0^1 kx\dx+\int_1^2(2-x)\dx
        =\frac k2+\left[2x-\frac{x^{2}}{2}\right]_1^2=\frac k2+\frac12=1,$$
        故 $k=1$。（验证 $f\ge0$：在 $[0,1)$ 上 $x\ge0$，在 $[1,2)$ 上 $2-x>0$，成立。）

        **(2)** 分四段积分：

        - $x<0$：$F(x)=0$；
        - $0\le x<1$：$F(x)=\displaystyle\int_0^{x}t\dt=\frac{x^{2}}{2}$；
        - $1\le x<2$：$F(x)=\underbrace{\frac12}_{\text{第一段的全部面积}}
          +\int_1^{x}(2-t)\dt=\frac12+\left[2t-\frac{t^{2}}{2}\right]_1^{x}
          =2x-\frac{x^{2}}{2}-1$；
        - $x\ge2$：$F(x)=1$。

        即
        $$F(x)=\begin{cases}0,&x<0\\[3pt]\dfrac{x^{2}}{2},&0\le x<1\\[6pt]
        2x-\dfrac{x^{2}}{2}-1,&1\le x<2\\[6pt] 1,&x\ge2\end{cases}$$

        **检验衔接**（==这一步一定要做==）：
        $x=1$ 处两段都给 $\frac12$；$x=2$ 处第三段给 $4-2-1=1$。$F$ 处处连续 $\checkmark$

        **(3)**
        $$P(0.5<X<1.5)=F(1.5)-F(0.5)=\left(3-\frac{2.25}{2}-1\right)-\frac{0.25}{2}
        =0.875-0.125=\boxed{0.75}.$$
      `,
      comment: String.raw`
        **写 $F$ 的三条纪律**：

        1. ==段数与 $f$ 一致==，而且要写满整个实轴（$x<0$ 和 $x\ge2$ 两段不能漏）；
        2. ==每段都要把前面所有段的面积累加进来==；
        3. ==写完检查分段点处是否衔接==——
           连续型的 $F$ 必须处处连续，衔接不上就是算错了。
           这个检验几乎能抓住所有计算错误，==成本极低，务必养成习惯==。

        **一个常见的追问**："求 $Y=X^{2}$ 的密度"。
        这属于[随机变量函数的分布](#/probability/random-var/function-of-rv?at=cdf-method)，
        做法是先写 $F_Y(y)=P(X^{2}\le y)$ 再求导，
        ==而不是把 $f(x)$ 里的 $x$ 换成 $\sqrt y$==。
      `,
    },

    { t: 'example',
      id: 'ex-mixed',
      title: '截断产生混合型：$F$ 上出现一个跳跃',
      source: '标准例题',
      level: 3,
      problem: String.raw`
        设 $X\sim U(0,2)$，令
        $$Y=\begin{cases}X,&X<1\\ 1,&X\ge1\end{cases}$$
        求 $Y$ 的分布函数 $F_Y(y)$，并求 $P(Y=1)$。
      `,
      idea: String.raw`
        **先想清楚 $Y$ 长什么样**：$Y$ 的取值范围是 $[0,1]$；
        当 $X<1$ 时 $Y$ 照抄 $X$，当 $X\ge1$ 时 $Y$ ==被压成一个点 $1$==。

        $X\ge1$ 这件事的概率是 $\frac12$，
        ==这半个单位的概率被整块搬到了 $y=1$ 这一个点上==——
        所以 $Y$ 必然在 $y=1$ 处有一个高度 $\frac12$ 的跳跃，$Y$ 不是连续型。
        ==预先看出这个跳跃，是这道题不出错的关键。==

        **做法**：老老实实按定义 $F_Y(y)=P(Y\le y)$ 分区间讨论。
        分界点取在 $Y$ 的取值范围的两端（$0$ 和 $1$），
        因为区间外的答案是平凡的 $0$ 和 $1$。
      `,
      solution: String.raw`
        $X$ 的密度为 $f_X(x)=\frac12$（$0<x<2$）。

        - **$y<0$**：$Y\ge0$ 恒成立，故 $F_Y(y)=0$。
        - **$0\le y<1$**：此时 $\set{Y\le y}$ 只能由 $X<1$ 的那一支产生，
          即 $\set{Y\le y}=\set{X\le y}$，故
          $$F_Y(y)=P(X\le y)=\frac y2.$$
        - **$y\ge1$**：$Y\le1$ 恒成立，故 $F_Y(y)=1$。

        即
        $$F_Y(y)=\begin{cases}0,&y<0\\[3pt]\dfrac y2,&0\le y<1\\[6pt]1,&y\ge1\end{cases}$$

        **点概率**：
        $$P(Y=1)=F_Y(1)-F_Y(1-0)=1-\frac12=\boxed{\frac12}.$$
      `,
      comment: String.raw`
        **$Y$ 是标准的混合型**：
        在 $[0,1)$ 上有密度 $\frac12$（连续部分，总概率 $\frac12$），
        在 $y=1$ 处有一个概率原子 $\frac12$（离散部分）。两部分加起来正好是 $1$。

        **这类题的识别信号**：
        ==定义里出现 $\min$、$\max$、"超过就按……算"、"最多赔 $N$ 元"==，
        都会把一整片概率压到一个点上。
        典型的现实模型是**保险的赔付上限**：
        赔付额 $Y=\min(X,N)$，其中 $X$ 是损失额。

        **最容易犯的错**：算出 $F_Y$ 之后去求导，声称密度是 $\frac12$（$0<y<1$）。
        ==这样丢掉了 $y=1$ 处那半个单位的概率==，
        密度积分只有 $\frac12$，不满足归一性——
        这也是一个==现成的自检==：求完密度先积一下看是不是 $1$。

        **顺带一提**：如果题目问 $\E Y$，
        必须"连续部分积分 + 离散部分求和"两块相加：
        $$\E Y=\int_0^1 y\cdot\frac12\dy+1\cdot\frac12=\frac14+\frac12=\frac34.$$
        混合型的期望算法见[期望的三种口径](#/probability/moments/expectation?at=def-e)。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '三、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **把 $f(x)$ 当概率**：连续型下 $P(X=x)=0$，且 ==$f$ 可以大于 $1$==。
      2. **$P(X<a)$ 写成 $F(a)$**：正确的是 ==$F(a-0)$==；
         只有连续型下两者才相等。
      3. **$F$ 不写满整个实轴**：$x$ 很小和很大的两段必须写出 $0$ 和 $1$。
      4. **分段积分时不累加前面的面积**：$F$ 是==累积==的，
         第 $k$ 段要以第 $k-1$ 段的终值为起点。
      5. **写完 $F$ 不检查衔接**：连续型的 $F$ 必须处处连续，
         ==这是成本最低的自检==。
      6. **对混合型求密度**：混合型==没有密度==，只能用 $F$ 描述。
      7. **求参数只用归一性**：还有 $F(\pm\infty)$ 和连续性两组方程，
         见[三条路](#/probability/random-var/cdf-pdf?at=find-constant)。
      8. **忘了验 $f\ge0$**：解方程可能解出不合格的根。
      9. **左连续 / 右连续记反**：由 $F(x)=P(X\le x)$ 得到的是==右连续==。
    ` },

  ],
});
