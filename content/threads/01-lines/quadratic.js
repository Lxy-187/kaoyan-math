/* ==========================================================================
   跨章节手法主线 / 四条主线 / ④ 二次型与正交变换
   ========================================================================== */

KM.page({
  path: 'threads/lines/quadratic',
  title: '④ 二次型与正交变换：消掉交叉项',
  subtitle: '把线性代数焊到高数上的那条线 —— 极值判别、曲面定型、重积分换元，同一个动作',
  tags: ['专题', '高频'],
  updated: '2026-08-10',

  blocks: [

    { t: 'key', id: 'core', title: '底层动作：找主轴', c: String.raw`
      一个二次齐次式一旦带上 $xy,\;yz,\;zx$ 这些==交叉项==，它的几何含义就看不出来了。
      整条线只做一件事：

      $$\vec x^{\T}A\vec x\ \xrightarrow{\ \vec x=P\vec y\ }\ \vec y^{\T}\Lambda\vec y
      =\lambda_1y_1^{2}+\lambda_2y_2^{2}+\lambda_3y_3^{2}$$

      **交叉项消失 = 坐标系转到了主轴方向上。**
      $A$ 是实对称矩阵，[必可正交对角化](#/linear-algebra/eigen/symmetric?at=four-properties)，
      $P$ 是正交矩阵（各列是单位正交的特征向量），$\lambda_i$ 是特征值。

      消掉交叉项之后：==符号看正定性、大小看伸缩、方向看特征向量==，
      三件事全部一目了然。
    ` },

    { t: 'compare',
      id: 'battle-map',
      title: '同一个动作，五个战场',
      cols: ['战场', '二次型从哪来', '特征值告诉你什么'],
      rows: [
        ['线代：二次型标准化', '题目直接给', '正负惯性指数、规范形'],
        ['==高数：多元极值判别==', '泰勒展开的二次项', '正定 $\\Rightarrow$ 极小，负定 $\\Rightarrow$ 极大'],
        ['高数：二次曲面定型', '曲面方程本身', '符号组合决定是椭球面还是双曲面'],
        ['高数：重积分 / 曲面积分换元', '积分区域是斜的', '正交变换 $\\left|J\\right|=1$，把区域扶正'],
        ['线代：$\\vec x^{\\T}A\\vec x$ 在单位球上的最值', '条件极值', '最大值 $\\lambda_{\\max}$、最小值 $\\lambda_{\\min}$'],
      ] },

    /* ================================================================== */
    { t: 'h', id: 'hessian', c: '战场 1：多元极值 —— 高数与线代的第一个焊点' },

    { t: 'key', id: 'hessian-is-quadratic', title: 'Hessian 判别法就是二次型正定性判别', c: String.raw`
      在驻点 $\vec x_0$ 处（$\nabla f=\vec 0$）做泰勒展开：
      $$f(\vec x_0+\vec h)-f(\vec x_0)=\frac12\,\vec h^{\T}H\vec h+o\!\left(\left|\vec h\right|^{2}\right),
      \qquad H=\left(f_{x_ix_j}\right).$$

      增量的符号完全由二次型 $\vec h^{\T}H\vec h$ 决定：

      | Hessian $H$ | 二次型 | 结论 |
      |---|---|---|
      | 正定（特征值全正） | $\vec h^{\T}H\vec h>0$ | ==极小值== |
      | 负定（特征值全负） | $\vec h^{\T}H\vec h<0$ | ==极大值== |
      | 不定（有正有负） | 符号随方向变 | ==鞍点==，非极值 |
      | 半定（有零特征值） | 二阶信息不够 | 判别失效，要看更高阶 |

      **二元情形的验证**：$H=\begin{pmatrix}A&B\\ B&C\end{pmatrix}$，
      正定 $\iff$ 顺序主子式全正 $\iff A>0$ 且 $AC-B^{2}>0$ ——
      ==这正是[教材上的二元极值判别法](#/calculus/multi-derivative/extremum?at=hessian)==。

      所以那条"$AC-B^{2}>0$ 且 $A>0$"的口诀不是天上掉下来的，
      它是 $2\times2$ 矩阵正定的顺序主子式判据。
      ==记住线代那一套，高数这条就不用单独背了==，而且能自然推广到三元以上。
    ` },

    { t: 'md', c: String.raw`
      这个焊点同时也是[主线①](#/threads/lines/taylor?at=hessian-link)的终点：
      泰勒展开负责把函数增量化成二次型，二次型理论负责判断它的符号。
      ==两条主线在"多元极值"这一题上交汇。==
    ` },

    /* ================================================================== */
    { t: 'h', id: 'surface', c: '战场 2：二次曲面定型' },

    { t: 'method', id: 'surface-type', title: '交叉项让曲面认不出来，转一下就好了', c: String.raw`
      面对 $x^{2}+y^{2}+z^{2}+2xy+2yz=1$ 这类方程，直接看是认不出曲面类型的。

      **动作**：写出二次型矩阵 $A$ $\to$ 求特征值 $\lambda_1,\lambda_2,\lambda_3$
      $\to$ 正交变换后方程变成
      $$\lambda_1y_1^{2}+\lambda_2y_2^{2}+\lambda_3y_3^{2}=1 .$$

      **只看符号就能定型**：

      | 特征值符号 | 曲面 |
      |---|---|
      | 全正 $(+,+,+)$ | 椭球面 |
      | 两正一负 $(+,+,-)$ | ==单叶==双曲面 |
      | 一正两负 $(+,-,-)$ | ==双叶==双曲面 |
      | 有零特征值 | 柱面或退化情形 |

      ==正交变换是旋转，不改变图形的形状和大小==，只换了个观察角度，
      所以"变换后是椭球面"就等价于"原来也是椭球面"。
      这是为什么可以放心用它定型。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'integral', c: '战场 3：重积分与曲面积分换元' },

    { t: 'method', id: 'jacobian-one', title: '正交变换的雅可比行列式绝对值为 1', c: String.raw`
      $P$ 正交 $\Rightarrow$ $P^{\T}P=E$ $\Rightarrow$ $\left|P\right|^{2}=1$
      $\Rightarrow$ $\left|\det P\right|=1$。

      于是做变量代换 $\vec x=P\vec y$ 时
      $$\d x\,\d y\,\d z=\left|\det P\right|\d y_1\,\d y_2\,\d y_3=\d y_1\,\d y_2\,\d y_3 .$$

      ==体积微元原封不动==，这是正交变换在积分里最值钱的性质：
      可以把一个歪斜的积分区域（比如 $x^{2}+y^{2}+z^{2}+2xy\le1$）
      旋转成与坐标轴对齐的标准椭球，==而不需要为换元付出任何雅可比代价==。

      **对比**：一般的线性变换 $\left|J\right|=\left|\det P\right|\neq1$，
      要乘上这个因子；极坐标 $\left|J\right|=r$；球坐标 $\left|J\right|=\rho^{2}\sin\varphi$。
      ==正交变换是唯一"免费"的那一种==。

      同样的道理让它在曲面积分里也好用：旋转不改变面积元 $\d S$，
      配合[高斯公式](#/calculus/derivative-app/proof-overview)之类的工具时，
      可以先把曲面摆正再算。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'rayleigh', c: '战场 4：单位球面上的最值' },

    { t: 'key', id: 'eigen-extremum', title: '$\\vec x^{\\T}A\\vec x$ 在 $\\left|\\vec x\\right|=1$ 上的最值就是特征值', c: String.raw`
      $$\max_{\left|\vec x\right|=1}\vec x^{\T}A\vec x=\lambda_{\max},\qquad
      \min_{\left|\vec x\right|=1}\vec x^{\T}A\vec x=\lambda_{\min}$$

      **两种证法，正好是两条线各出一招**：

      - **线代视角**：正交变换后 $=\sum\lambda_iy_i^{2}$，
        而 $\sum y_i^{2}=1$（正交变换保模长），
        这是特征值的==加权平均==，自然夹在最大最小之间；
      - **高数视角**：用[拉格朗日乘数法](#/calculus/multi-derivative/extremum?at=lagrange-steps)，
        $L=\vec x^{\T}A\vec x-\lambda\left(\vec x^{\T}\vec x-1\right)$，
        求导得 ==$A\vec x=\lambda\vec x$== —— 拉格朗日乘子恰好就是特征值。

      ==第二种证法尤其值得玩味==：条件极值的乘子 $\lambda$ 和矩阵的特征值 $\lambda$
      用同一个字母，不是巧合。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'probability', c: '战场 5：概率论里的二次型（拓展）' },

    { t: 'md', c: String.raw`
      多维正态分布的密度函数指数上是一个二次型
      $-\frac12(\vec x-\vec\mu)^{\T}\Sigma^{-1}(\vec x-\vec\mu)$，
      其中协方差矩阵 $\Sigma$ ==对称且半正定==。

      对 $\Sigma$ 做正交对角化，就把相关的分量==解耦成互相独立的一维正态==——
      这在数学上和"消掉交叉项"是同一个动作，
      几何上就是把等密度的椭球转到主轴方向。

      考研不会直接考这个推导，但知道它能解释一个常考的事实：
      ==二维正态分布中，"不相关"与"独立"等价==——
      因为不相关意味着协方差矩阵已经是对角的，交叉项本来就没有。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '通用易错点' },

    { t: 'warn', id: 'pitfall-list', title: '这条线的固定失分点', c: String.raw`
      1. **二次型矩阵写错**：交叉项系数要==对半分==。
         $2xy$ 对应 $a_{12}=a_{21}=1$，不是 $2$。这是最高频的低级错误。
      2. **配方法和正交变换混为一谈**：配方法只保证化成平方和（==合同变换==），
         不保证系数是特征值；只有正交变换才能同时保持几何形状。
         题目说"用正交变换"就必须求特征向量并==单位化、正交化==。
      3. **特征向量忘记单位化**：$P$ 必须是正交矩阵，各列模长为 $1$。
         同一特征值的重根还要先施密特正交化。
      4. **正定性判据张冠李戴**：顺序主子式全正 $\iff$ 正定；
         ==负定不是"主子式全负"==，而是奇数阶为负、偶数阶为正。
      5. **Hessian 半定时硬下结论**：$AC-B^{2}=0$ 判别失效，必须另行讨论。
      6. **曲面定型只看特征值个数不看符号**：决定类型的是==符号组合==。
    ` },

  ],
});
