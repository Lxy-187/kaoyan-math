# 考研数学一知识库 —— 工作手册

这是一个**纯静态、无构建**的个人复习站。用户的使用方式是：
把他和 AI 的对话（含他自己的思考）丢给我 → 我判断归属层级 → 我更新对应内容文件 → 他刷新页面看。

**用户不会在网页上编辑任何内容。所有更新都由我通过改文件完成。**

---

## 1. 目录结构

```
index.html                  页面外壳（新增内容页不需要改它）
assets/css/                 tokens 变量 / layout 布局 / typography 排版 / blocks 块样式
assets/js/                  store 数据 / markdown 解析 / blocks 渲染 / sidebar 导航 / search 搜索 / app 路由
vendor/katex/               本地 KaTeX，保证离线和 file:// 直接双击可用
content/manifest.js         ★ 知识树：学科 → 章 → 专题，全站唯一的结构定义
content/<学科>/<章>/<专题>.js  ★ 内容页
```

路由是 `#/<subject.id>/<chapter.id>/<topic.id>`，和 manifest 里的 id 一一对应。

---

## 2. 新增 / 更新内容的标准流程

### 情况 A：专题已在 manifest 里且已有 `file`
直接编辑那个内容文件，往 `blocks` 数组里加块或改块。**改完把 `updated` 改成今天的日期。**

### 情况 B：专题在 manifest 里但没有 `file`（侧栏显示为灰色待补充）
1. 新建内容文件，路径用 `content/<学科id>/<章序号>-<章id>/<专题id>.js`
2. 回到 `content/manifest.js`，给那个 topic 补上 `file: '...'`

### 情况 C：manifest 里根本没有这个专题
先在对应章节的 `topics` 数组里按合适的位置插入一条（顺序 = 侧栏顺序 = 上一篇/下一篇顺序），再按情况 B 做。
**不要为了一段对话就新建一个章。** 章的划分已经对齐考研大纲，优先往已有章里加专题。

### 归类时的判断顺序
1. 先定**学科**（高数 / 线代 / 概率）
2. 再定**章**（对齐大纲，例如「导数应用类证明题」→ `calculus / derivative-app`）
3. 再定**专题**（题型粒度，例如「中值定理证明题」而不是「证明题」）
4. 如果内容跨专题，拆开写进各自的页，不要堆在一页里

判断不了归属时，问用户一句，不要瞎放。

---

## 3. 内容页文件格式

```js
KM.page({
  path: 'calculus/derivative-app/mvt-proof',   // 必须等于 manifest 里的 学科/章/专题 id
  title: '中值定理证明题',                      // 纯文本，会进 document.title，不能写公式
  subtitle: '一句话说清这页解决什么问题',       // 可选，支持行内 Markdown 与 $公式$
  tags: ['大题', '证明题', '高频'],             // 可选，见下方标签约定
  updated: '2026-08-10',                       // 每次改动都更新

  blocks: [ /* 见下 */ ],
});
```

**所有含 LaTeX 的字符串一律用 `` String.raw`...` ``**，否则 `\varphi`、`\frac`、`\d` 会被 JS 当转义字符吃掉。
`String.raw` 里不能出现反引号和 `${`。

---

## 4. 块类型（这是排版的全部词汇）

| `t` | 用途 | 视觉 | 字段 |
|---|---|---|---|
| `h` | 分节大标题，同时进入右侧目录 | 衬线大标题 + 下划线 | `c` |
| `md` | 普通正文 | 无框 | `c` |
| `key` | 定理、公式、必背结论 | **蓝**框 | `title`, `c` |
| `method` | 方法论、解题套路 | **紫**框 | `title`, `c` |
| `insight` | ★ **用户自己的思路、顿悟、私人口诀** | **绿**框 + 阴影 | `title`, `c` |
| `warn` | 易错点、陷阱、边界条件 | **红**框 | `title`, `c` |
| `quote` | 原始对话摘录、暂未消化的素材 | 灰框 | `title`, `c` |
| `steps` | 有序流程（带圆圈数字时间轴） | 紫色节点 | `title`, `items: [{title, c}]` |
| `example` | **例题卡** | 琥珀色卡片 | 见下 |
| `compare` | 对比表 | 数据表 | `title`, `cols: []`, `rows: [[]]` |
| `formulas` | 公式速查网格 | 蓝色格子 | `title`, `items: [{label, tex}]` |

### 每个块都可以有 `id`（重要）

任何块都接受一个 `id` 字段，别处就能用 `?at=` 精确链过来：

```js
{ t: 'key', id: 'darboux', title: '达布定理', c: ... }
```

**命名规则**：短、ASCII、小写连字符、==描述内容而不是位置==。
`darboux` / `integrating-factor` / `use-extremum` / `ex-cauchy` 都是好名字；
`block3`、`section-four` 是坏名字（改顺序就失真）。

**什么时候必须写 `id`**：
- 任何**可能被别的页引用**的块（定理、方法、例题）；
- 所有 `h` 分节标题；
- 所有 `example`（不写就退化成 `ex-1`、`ex-2` 这种位置编号，==中间插一道题就全错位==）。

不写 `id` 也能工作，会自动生成一个（`h` 用中文标题生成），但改标题就断链。

### 颜色语义不能混用
蓝 = 客观知识；紫 = 怎么做；**绿 = 用户自己说的话**；红 = 会丢分的地方。
用户复习时靠颜色区分「书上的」和「我自己的」，这是整站最重要的约定。

### example 块
```js
{ t: 'example',
  title: '这道题在考什么',      // 不要写「例题1」，编号自动生成
  source: '2019 数一 真题',     // 可选
  level: 3,                     // 可选，1~5 星难度
  problem: String.raw`题面`,
  idea:     String.raw`思路——为什么想到这么做，默认折叠`,
  solution: String.raw`完整解答，默认折叠`,
  comment:  String.raw`点评/拓展/同类题，默认折叠`,
  openIdea: true,               // 可选，让某段默认展开
}
```
思路 / 解答 / 点评默认折叠，方便用户先自测。顶栏 `⇕` 或页内按钮可一键全展开。
**`idea` 要写「为什么会想到」，不是解答的复述** —— 这是这个站最有价值的部分。

---

## 4.5 页间链接必须精确到块

**链到页面开头是不合格的。** 用户点过去要能直接看到那个定理，而不是自己在长页面里找。

```
[达布定理](#/calculus/derivative-app/mvt-proof?at=ex-darboux)     ✅ 落到那道例题
[达布定理](#/calculus/derivative-app/mvt-proof)                   ❌ 只到页面开头
[看这里](#ex-darboux)                                             ❌ 会被路由当成页面路径，跳到「待补充」页
```

格式固定为 `#/<学科>/<章>/<专题>?at=<块id>`。页内链接也用同样的全路径写法。

链接文字要写清**落到的是什么**（"达布定理例题"而不是"点这里"），因为跳过去之后
用户看到的是页面中部，需要文字先建立预期。

写完链接后**一定要跑一次自检**（见第 8 节），断链在静态页面上是无声失败的。

## 5. Markdown 与公式

支持：`# ~ #####` 标题、`**粗**`、`*着重(紫色)*`、`==高亮(黄底)==`、`~~删除~~`、`` `代码` ``、
有序/无序列表（缩进嵌套）、`>` 引用、`---` 分割线、表格、``` 代码块、`[文字](#/路径)`、`<br>`。

公式：行内 `$...$`，独立 `$$...$$`。

### 排版习惯（直接影响「快速找重点」的体验）
- 结论句里的关键词用 `==高亮==`，一段里最多标 1~2 处，标多了等于没标
- 「必须写出来的踩分点」用 `**粗体**`
- 三行以上的并列信息用表格，不要用长列表
- 每个 `h` 分节之间保持独立，用户经常只跳读某一节

### 自定义 KaTeX 宏（`assets/js/markdown.js` 里的 `MACROS`）
`\d`→d、`\dx \dt \du \ds \dy \dtheta \dxy`、`\R \N \Z \Q \C`、`\E \Var \Cov`、`\rank \tr \diag`、
`\abs{} \norm{} \set{}`、`\pd{f}{x}`、`\deriv{y}{x}`、`\xto{}`、`\eps`、`\T`（转置）、`\inv`。
需要新宏就往 `MACROS` 里加。

---

## 6. 标签约定

`tags` 会自动上色：含「高频/必考/重点」→ 红；含「大题/证明/解答题」→ 琥珀；含「数一/数二/数三」→ 蓝。
常用：`小题`、`大题`、`证明题`、`计算题`、`高频`、`易错`、`真题`、`概念辨析`。

---

## 7. 处理用户对话的方法

用户会粘贴一大段他和 AI 的聊天记录。拆解方式：

- **他的提问和困惑** → 往往说明卡点在哪，写进 `insight` 或例题的 `idea`
- **他自己的推理尝试** → `insight` 块，==保留他的原话语气==，只做精简和条理化，不要改写成教科书腔
- **AI 给的方法总结** → `method` 或 `key` 块，需要我核对正确性后再写
- **具体题目** → `example` 块
- **他说「原来如此 / 我懂了」的地方** → 这就是最该记的，单独成块

**不要把整段对话原样贴进去。** 用户要的是能快速复习的成品，不是聊天记录归档。
确实需要保留原始上下文时用 `quote` 块，并放在该节末尾。

写完给用户一句话说明：放到了哪个路径、加了哪些块。

---

## 8. 自检

改完内容后确认：
- [ ] `path` 和 manifest 里的 id 完全一致
- [ ] 含反斜杠的字符串都用了 `String.raw`
- [ ] 新增的块（尤其是例题和会被引用的定理）都写了 `id`
- [ ] 跨页链接都带 `?at=`，没有只链到页面开头的
- [ ] `updated` 改成今天

起服务后在浏览器控制台跑这两句：

```
KM.audit()                                   // 断链、锚点不存在、重复 id、公式漏渲染
document.querySelectorAll('.math-error')     // 当前页的公式语法错误，应为空
```

`KM.audit()` 会遍历全站所有页面，返回 `{total, broken, raw}`：
- `broken` 必须是空数组（断链 / 锚点不存在）；
- `raw` 必须是空数组 —— 它列出**正文里残留的字面 `$`**，也就是没被解析成公式的片段。
  这是一种==无声失败==：页面照常显示，只是把 LaTeX 源码当文本印出来，肉眼扫很容易漏。

浏览器会缓存 `assets/js/*`，改过解析器后跑自检前先硬刷新，否则测的是旧代码。

本地预览（`.claude/launch.json` 已配好，也可以直接双击 `index.html`）：

```bash
python -m http.server 5180
```
